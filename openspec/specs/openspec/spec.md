# Openspec Specification

> Generated by openlore v1.0.0 on 2026-04-05 10:50

## Purpose

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Handles compatibility and validation of
OpenSpec configurations and specifications.

## Requirements

### Requirement: Validateconfig

The system SHALL validates the openspec configuration file against the expected schema.

#### Scenario: ValidConfiguration
- **GIVEN** A valid OpenSpecConfig
- **WHEN** validateConfig is called
- **THEN** Returns a ValidationResult with valid: true and no errors

#### Scenario: InvalidConfiguration
- **GIVEN** An invalid OpenSpecConfig
- **WHEN** validateConfig is called
- **THEN** Returns a ValidationResult with valid: false and appropriate errors

### Requirement: Updatecontext

The system SHALL updates the context in the openspec configuration based on detected project context.

#### Scenario: PreserveUserContext
- **GIVEN** A config with existing context and preserveUserContext: true
- **WHEN** updateContext is called
- **THEN** Returns the config with user context preserved and detected context appended

#### Scenario: OverwriteUserContext
- **GIVEN** A config with existing context and preserveUserContext: false
- **WHEN** updateContext is called
- **THEN** Returns the config with user context overwritten by detected context

### Requirement: Loadconfig

The system SHALL loads the openspec configuration file from the specified directory.

#### Scenario: ConfigFileExists
- **GIVEN** A directory containing a valid config.yaml
- **WHEN** loadConfig is called
- **THEN** Returns the parsed OpenSpecConfig

#### Scenario: ConfigFileDoesNotExist
- **GIVEN** A directory without a config.yaml
- **WHEN** loadConfig is called
- **THEN** Throws an error indicating the file is missing

### Requirement: Saveconfig

The system SHALL saves the openspec configuration file to the specified directory.

#### Scenario: DirectoryExists
- **GIVEN** A valid directory path
- **WHEN** saveConfig is called
- **THEN** Saves the config.yaml file to the directory

#### Scenario: DirectoryDoesNotExist
- **GIVEN** An invalid directory path
- **WHEN** saveConfig is called
- **THEN** Throws an error indicating the directory is invalid

### Requirement: Validatespecstructure

The system SHALL validates the complete spec structure for compliance with openspec standards.

#### Scenario: ValidSpecStructure
- **GIVEN** A spec with all required sections and proper markdown structure
- **WHEN** validateSpecStructure is called
- **THEN** Returns a ValidationResult with valid: true and no errors or warnings

#### Scenario: InvalidSpecStructure
- **GIVEN** A spec missing required sections or with malformed markdown
- **WHEN** validateSpecStructure is called
- **THEN** Returns a ValidationResult with valid: false and appropriate errors or warnings

### Requirement: Validaterequirementformat

The system SHALL validates requirement format follows rfc 2119 standards.

#### Scenario: ValidRequirementFormat
- **GIVEN** A requirement using RFC 2119 keywords and proper imperative form
- **WHEN** validateRequirementFormat is called
- **THEN** Returns a ValidationResult with valid: true and no warnings

#### Scenario: InvalidRequirementFormat
- **GIVEN** A requirement missing RFC 2119 keywords or not in imperative form
- **WHEN** validateRequirementFormat is called
- **THEN** Returns a ValidationResult with valid: false and appropriate warnings

### Requirement: Validatescenarioformat

The system SHALL validates scenario format follows given/when/then structure.

#### Scenario: ValidScenarioFormat
- **GIVEN** A scenario with proper Given/When/Then structure and correct heading
- **WHEN** validateScenarioFormat is called
- **THEN** Returns a ValidationResult with valid: true and no errors or warnings

#### Scenario: InvalidScenarioFormat
- **GIVEN** A scenario missing Given/When/Then clauses or with incorrect heading
- **WHEN** validateScenarioFormat is called
- **THEN** Returns a ValidationResult with valid: false and appropriate errors or warnings

### Requirement: Validateconfigyaml

The system SHALL validates config.yaml structure for compliance with openspec standards.

#### Scenario: ValidConfig.yaml
- **GIVEN** A config.yaml with all required fields and proper structure
- **WHEN** validateConfigYaml is called
- **THEN** Returns a ValidationResult with valid: true and no errors or warnings

#### Scenario: InvalidConfig.yaml
- **GIVEN** A config.yaml missing required fields or with incorrect structure
- **WHEN** validateConfigYaml is called
- **THEN** Returns a ValidationResult with valid: false and appropriate errors or warnings

### Requirement: Checkheadinghierarchy

The system SHALL checks heading hierarchy is valid (no skipped levels).

#### Scenario: ValidHeadingHierarchy
- **GIVEN** Content with proper heading hierarchy
- **WHEN** checkHeadingHierarchy is called
- **THEN** Returns an object with valid: true and no issues

#### Scenario: InvalidHeadingHierarchy
- **GIVEN** Content with skipped heading levels
- **WHEN** checkHeadingHierarchy is called
- **THEN** Returns an object with valid: false and appropriate issues

### Requirement: Checkmarkdownlinks

The system SHALL checks for malformed markdown links.

#### Scenario: ValidMarkdownLinks
- **GIVEN** Content with properly formatted markdown links
- **WHEN** checkMarkdownLinks is called
- **THEN** Returns an empty array

#### Scenario: InvalidMarkdownLinks
- **GIVEN** Content with malformed markdown links
- **WHEN** checkMarkdownLinks is called
- **THEN** Returns an array of malformed links

### Requirement: Injectcontext

The system SHALL injects context into the openspec configuration.

#### Scenario: ValidContext
- **GIVEN** A valid context object
- **WHEN** injectContext is called
- **THEN** The context is successfully injected into the configuration

#### Scenario: InvalidContext
- **GIVEN** An invalid context object
- **WHEN** injectContext is called
- **THEN** An error is thrown indicating the invalid context

### Requirement: SpecCorpusContainsOnlyCodeBackedRequirements

Every requirement in `openspec/specs/` SHALL describe behavior that exists in the codebase (or is
an explicitly-marked PROPOSED delta inside a change). A requirement whose subject has no
corresponding implementation, whose scenarios are the generated placeholder template ("valid state
→ expected outcome"), or whose domain links are dead SHALL be treated as corpus corruption and
removed or rewritten — never left to be served to agents as authority. A CI corpus lint SHALL
reject: the vacuous-scenario template, dead intra-corpus links, duplicate requirement names within
a domain, and a domain-table entry with no spec file on disk. Decision-synced requirements SHALL
have one canonical copy in the domain that owns the decision's subject; other domains reference it
by pointer rather than duplicating the text.

#### Scenario: A phantom requirement cannot survive review

- **GIVEN** a spec requirement mandating behavior with no implementing symbol in `src/`
  (e.g. JWT bearer validation in a product with no HTTP auth)
- **WHEN** the corpus lint runs in CI
- **THEN** the build fails identifying the requirement and its domain

#### Scenario: The overview describes the product that exists

- **GIVEN** `openspec/specs/overview/spec.md`
- **WHEN** an agent loads it at session start via CLAUDE.md
- **THEN** its purpose, domain table, and architecture describe OpenLore's current substrate, every
  domain link resolves, and no generated placeholder text remains

#### Scenario: A synced decision lives in one domain

- **GIVEN** an approved decision whose subject is the MCP tool surface
- **WHEN** the decision is synced into the spec corpus
- **THEN** exactly one domain carries the requirement text and any other domain that needs it
  carries a one-line pointer, so a later edit cannot fork the copies

## Technical Notes

- **Dependencies**: fs/promises, path, yaml, logger, constants, node:fs/promises, node:path
