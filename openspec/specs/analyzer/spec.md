# Analyzer Specification

> Generated by openlore v1.0.0 on 2026-04-05 10:50
> Source files: src/core/analyzer/repository-mapper.ts, src/api/types.ts, src/core/analyzer/embedding-service.ts, src/core/analyzer/subgraph-extractor.ts, src/core/analyzer/architecture-writer.ts, src/core/analyzer/dependency-graph.ts, src/core/analyzer/spec-vector-index.ts, src/core/analyzer/file-walker.ts, src/core/analyzer/import-resolver-bridge.ts, src/core/analyzer/refactor-analyzer.ts, src/core/analyzer/vector-index.ts, src/core/analyzer/import-parser.ts, src/core/analyzer/signature-extractor.ts, src/core/analyzer/artifact-generator.ts, src/core/analyzer/cpp-header-resolver.ts, src/core/analyzer/call-graph.ts, src/core/analyzer/duplicate-detector.ts, src/core/analyzer/type-inference-engine.ts, src/core/analyzer/significance-scorer.ts, src/core/analyzer/http-route-parser.ts, src/core/analyzer/ast-chunker.ts, src/core/analyzer/codebase-digest.ts, src/utils/progress.ts, src/utils/prompts.ts, src/utils/logger.ts, src/utils/shutdown.ts, src/core/analyzer/iac/project.ts, src/core/services/edge-store.ts, src/core/services/mcp-handlers/analysis.ts, openspec/specs/analyzer/spec.md, src/core/analyzer/landmark-signals.ts, src/core/services/mcp-handlers/landmarks.ts, src/core/services/mcp-handlers/reachability.ts, src/core/services/mcp-handlers/tool-contract.ts, src/cli/commands/mcp.ts, src/core/services/tool-dispatch.ts, src/core/analyzer/cfg.ts, src/core/services/mcp-watcher.ts, src/core/services/mcp-handlers/graph.ts, src/core/analyzer/cfg.test.ts, src/core/services/mcp-handlers/graph.test.ts, src/core/decisions/anchor.ts, src/core/decisions/anchor-adapter.ts, src/core/services/mcp-handlers/memory.ts, src/types/index.ts, src/constants.ts, openspec/changes/add-content-addressed-stable-symbol-ids/specs/analyzer/spec.md, src/core/scip/stable-id.test.ts, src/core/analyzer/personalized-pagerank.ts, src/core/services/mcp-handlers/orient.ts, src/core/analyzer/domain-naming.ts, src/core/analyzer/schema-extractor.ts, src/core/services/mcp-handlers/memory-ranking.ts, src/core/services/mcp-handlers/blast-radius.ts, src/cli/commands/blast-radius.ts, src/cli/index.ts

## Purpose

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Analyzes codebase functions to identify
potential refactoring opportunities based on structural metrics and architectural patterns.

## Entities

### LanguageBreakdown

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the distribution of
programming languages in a repository.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| language | string | Name of the programming language |
| extension | string | File extension associated with the language |
| fileCount | number | Number of files using this language |
| percentage | number | Percentage of total files using this language |

### DetectedFramework

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Information about a detected framework or
technology in a repository.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the framework |
| category | string | Category of the framework (frontend, backend, etc.) |
| confidence | string | Detection confidence level (high, medium, low) |
| evidence | string[] | List of evidence supporting the detection |

### DirectoryStats

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Statistics about a directory in a
repository.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Path to the directory |
| fileCount | number | Number of files in the directory |
| purpose | string | Inferred purpose of the directory |
| avgScore | number | Average significance score of files in the directory |

### FileClusters

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Grouping of files by different criteria.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| byDirectory | Record<string, ScoredFile[]> | Files grouped by directory |
| byDomain | Record<string, ScoredFile[]> | Files grouped by domain |
| byLayer | object | Files grouped by architectural layer |

**Relationships:**

- contains ScoredFile (FileClusters contains multiple ScoredFile instances)

### RepositoryMapMetadata

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Metadata about a repository analysis.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| projectName | string | Name of the project |
| projectType | ProjectType | Type of the project |
| rootPath | string | Root path of the repository |
| analyzedAt | string | Timestamp of analysis |
| version | string | Version of the analysis tool |

### RepositoryMapSummary

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Summary statistics of a repository
analysis.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| totalFiles | number | Total number of files in the repository |
| analyzedFiles | number | Number of files analyzed |
| skippedFiles | number | Number of files skipped |
| languages | LanguageBreakdown[] | Language distribution |
| frameworks | DetectedFramework[] | Detected frameworks |
| directories | DirectoryStats[] | Directory statistics |

**Relationships:**

- contains LanguageBreakdown (RepositoryMapSummary contains multiple LanguageBreakdown instances)
- contains DetectedFramework (RepositoryMapSummary contains multiple DetectedFramework instances)
- contains DirectoryStats (RepositoryMapSummary contains multiple DirectoryStats instances)

### RepositoryMap

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Complete map of a repository's structure
and contents.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| metadata | RepositoryMapMetadata | Repository metadata |
| summary | RepositoryMapSummary | Repository summary |
| highValueFiles | ScoredFile[] | High-value files |
| entryPoints | ScoredFile[] | Entry point files |
| schemaFiles | ScoredFile[] | Schema files |
| configFiles | ScoredFile[] | Configuration files |
| clusters | FileClusters | File clusters |
| allFiles | ScoredFile[] | All analyzed files |

**Relationships:**

- contains RepositoryMapMetadata (RepositoryMap contains RepositoryMapMetadata)
- contains RepositoryMapSummary (RepositoryMap contains RepositoryMapSummary)
- contains ScoredFile (RepositoryMap contains multiple ScoredFile instances)
- contains FileClusters (RepositoryMap contains FileClusters)

### RepositoryMapperOptions

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Configuration options for repository
mapping.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| maxFiles | number | Maximum files to process |
| includePatterns | string[] | Patterns to force-include |
| excludePatterns | string[] | Additional patterns to exclude |
| scoringConfig | ScoringConfig | Custom scoring configuration |
| onProgress | function | Progress callback |
| outputDir | string | Output directory for analysis files |

### FrameworkDetector

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Detects and identifies frameworks used in
a project based on file patterns and dependencies.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the framework |
| category | string | Category of the framework (e.g., frontend, backend, testing) |
| detect | function | Function to detect the framework based on files and package dependencies |

**Relationships:**

- uses FileMetadata (Uses file metadata to detect frameworks)
- uses PackageDependencies (Uses package dependencies to detect frameworks)

### FileMetadata

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Metadata about files in the project,
including name, extension, and directory.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the file |
| extension | string | File extension |
| directory | string | Directory path of the file |

**Relationships:**

- used-by FrameworkDetector (Used by FrameworkDetector to detect frameworks)

### PackageDependencies

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Dependencies and devDependencies from
package.json.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| dependencies | Record<string, string> | Production dependencies |
| devDependencies | Record<string, string> | Development dependencies |

**Relationships:**

- used-by FrameworkDetector (Used by FrameworkDetector to detect frameworks)

### AnalyzeResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Result of the analysis process.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| repoMap | CoreRepositoryMap | Repository mapping information |
| depGraph | DependencyGraphResult | Dependency graph information |
| artifacts | AnalysisArtifacts | Generated analysis artifacts |
| duration | number | Duration of the analysis in milliseconds |

### EmbeddingConfig

Configuration for the EmbeddingService, specifying the OpenAI-compatible API endpoint and model
details.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| baseUrl | string | Base URL of the OpenAI-compatible API |
| model | string | Embedding model name |
| apiKey | string | API key for authentication |
| batchSize | number | Maximum number of texts per API call |
| skipSslVerify | boolean | Disable SSL certificate verification |

### EmbeddingService

Service for computing text embeddings via any OpenAI-compatible `/embeddings` endpoint.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| baseUrl | string | Base URL of the OpenAI-compatible API |
| model | string | Embedding model name |
| apiKey | string | API key for authentication |
| batchSize | number | Maximum number of texts per API call |

### SubGraphNode

Represents a node in a subgraph, containing function details and connectivity metrics.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the function |
| file | string | File path where the function is defined |
| fanIn | number | Number of incoming calls to this function |
| fanOut | number | Number of outgoing calls from this function |

### SubGraph

Represents a subgraph extracted from a call graph, containing nodes and edges.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| root | SubGraphNode | Root node of the subgraph |
| nodes | SubGraphNode[] | Nodes reachable within the specified depth |
| edges | Array<[string, string]> | Edges representing function calls between nodes |

**Relationships:**

- contains SubGraphNode (SubGraph contains multiple SubGraphNodes)

### SubgraphOptions

Options for configuring subgraph extraction.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| maxDepth | number | Maximum depth to traverse in the call graph |
| maxNodes | number | Maximum number of nodes to include in the subgraph |
| tokenBudget | number | Token budget for the subgraph, used to derive maxDepth and maxNodes |

### ClusterSummary

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a summary of a module cluster
in the architecture.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the cluster |
| name | string | Name of the cluster |
| fileCount | number | Number of files in the cluster |
| role | string | Architectural role of the cluster |
| entryPointCount | number | Number of entry points in the cluster |
| hubCount | number | Number of hubs in the cluster |
| dependsOn | string[] | List of cluster IDs this cluster depends on |
| keyFiles | string[] | List of key files in the cluster (hubs and entries) |

### ArchitectureOverview

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents an overview of the software
architecture.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| generatedAt | string | Timestamp when the overview was generated |
| summary | object | Summary statistics of the architecture |
| clusters | ClusterSummary[] | List of cluster summaries |
| globalEntryPoints | object[] | List of global entry points |
| criticalHubs | object[] | List of critical hubs |

**Relationships:**

- contains ClusterSummary (ArchitectureOverview contains multiple ClusterSummaries)

### DependencyNode

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Node in the dependency graph representing
a file and its exports.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the node |
| file | ScoredFile | File information including score and path |
| exports | ExportInfo[] | List of exports from the file |
| metrics | object | Metrics for the node including degrees and centrality measures |
| cluster | object | Cluster information if the node belongs to one |

**Relationships:**

- has-a ScoredFile (DependencyNode contains a ScoredFile)
- has-many ExportInfo (DependencyNode contains multiple ExportInfo items)
- belongs to FileCluster (DependencyNode may belong to a FileCluster)

### DependencyEdge

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Edge in the dependency graph representing
a relationship between nodes.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| source | string | Source node ID |
| target | string | Target node ID |
| importedNames | string[] | Names of imported items |
| isTypeOnly | boolean | Indicates if the import is type-only |
| weight | number | Weight of the edge |
| httpEdge | HttpEdge | HTTP edge information if derived from an HTTP call |

**Relationships:**

- connects DependencyNode (DependencyEdge connects two DependencyNodes)
- has-a HttpEdge (DependencyEdge may contain an HttpEdge)

### FileCluster

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Cluster of related files in the
dependency graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the cluster |
| name | string | Name of the cluster |
| files | string[] | List of file IDs in the cluster |
| internalEdges | number | Number of internal edges |
| externalEdges | number | Number of external edges |
| cohesion | number | Cohesion metric for the cluster |
| coupling | number | Coupling metric for the cluster |
| suggestedDomain | string | Suggested domain for the cluster |
| color | string | Color for visual representation |
| isStructural | boolean | Indicates if the cluster has internal edges |

**Relationships:**

- contains DependencyNode (FileCluster contains multiple DependencyNodes)

### DependencyGraphResult

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Complete dependency graph result
including nodes, edges, clusters, and rankings.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodes | DependencyNode[] | List of nodes in the graph |
| edges | DependencyEdge[] | List of edges in the graph |
| clusters | FileCluster[] | List of all clusters |
| structuralClusters | FileCluster[] | List of structural clusters |
| rankings | object | Rankings of nodes and clusters |
| cycles | string[][] | List of cycles in the graph |
| statistics | object | Statistics about the graph |

**Relationships:**

- contains DependencyNode (DependencyGraphResult contains multiple DependencyNodes)
- contains DependencyEdge (DependencyGraphResult contains multiple DependencyEdges)
- contains FileCluster (DependencyGraphResult contains multiple FileClusters)

### DependencyGraphOptions

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Options for building the dependency
graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootDir | string | Root directory of the project |
| extensions | string[] | File extensions to consider for import resolution |
| minClusterSize | number | Minimum cluster size to report |
| dampingFactor | number | PageRank damping factor |
| maxIterations | number | Maximum PageRank iterations |

### DependencyGraph

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents a graph of file dependencies
within a codebase.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodes | Map<string, DependencyNode> | Map of file paths to dependency nodes |
| edges | DependencyEdge[] | Array of edges representing dependencies between files |
| adjacencyList | Map<string, Set<string>> | Map of file paths to sets of dependent file paths |
| reverseAdjacencyList | Map<string, Set<string>> | Map of file paths to sets of files that depend on them |
| httpEdgeCount | number | Count of HTTP-based edges in the graph |
| parser | ImportExportParser | Parser for extracting imports and exports from files |
| options | Required<DependencyGraphOptions> | Configuration options for the graph builder |

**Relationships:**

- contains DependencyNode (Graph contains multiple dependency nodes)
- contains DependencyEdge (Graph contains multiple dependency edges)
- contains FileCluster (Graph contains multiple file clusters)
- uses FileAnalysis (Graph uses file analysis data to build the graph)

### SpecRecord

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a record in the specification
vector index, containing metadata and embedding information for a specification section.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the specification record |
| domain | string | Domain or category of the specification |
| section | string | Section type of the specification (e.g., requirements, purpose, entities) |
| title | string | Title of the specification section |
| text | string | Concatenated text used for embedding |
| linkedFiles | string | JSON-encoded string array of source files linked to this specification |
| vector | number[] | Embedding vector for the specification text |

### SpecSearchResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the result of a search query
in the specification vector index, including the matched record and its relevance score.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| record | object | The matched specification record |
| score | number | Relevance score of the search result |

**Relationships:**

- contains SpecRecord (SpecSearchResult contains a SpecRecord)

### ParsedSection

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a parsed section from a
specification Markdown file, including its section type, title, content, and requirement key.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| section | string | Section type of the specification |
| title | string | Title of the section |
| content | string | Content of the section |
| requirementKey | string | Slugified requirement name for mapping lookup |

### SpecVectorIndex

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Builds and queries a LanceDB vector index
over OpenSpec spec files. Each record represents a logical section (requirement, purpose, design
note...) parsed from the Markdown spec files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| outputDir | string | Path to .openlore/analysis/ |
| specsDir | string | Path to openspec/specs/ (or any directory containing domain/spec.md files) |
| mappingJsonPath | string | Path to mapping.json (optional -- no enrichment if absent) |
| embedSvc | EmbeddingService | Service for generating embeddings |
| query | string | Search query for semantic search |
| limit | number | Maximum number of results to return |
| domain | string | Filter results by domain |
| section | string | Filter results by section |

**Relationships:**

- uses EmbeddingService (SpecVectorIndex uses EmbeddingService to generate embeddings)

### FileWalker

[PARTIAL SPEC — file too large to fully analyze (3 parts)] A utility for traversing directories and
collecting file metadata based on specified patterns and filters.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| maxFiles | number | Maximum number of files to process |
| includePatterns | string[] | Additional glob patterns to include |
| excludePatterns | string[] | Additional glob patterns to exclude |
| onProgress | function | Progress callback for UI updates |
| signal | AbortSignal | AbortController signal for cancellation |
| concurrency | number | Maximum concurrent file reads |

### FileWalkerProgress

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Progress information during file walking,
including counts of files found, directories scanned, and the current path being processed.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filesFound | number | Number of files found during walking |
| directoriesScanned | number | Number of directories scanned during walking |
| currentPath | string | Current path being processed |

### FileWalkerResult

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Result of file walking, including
collected file metadata and statistics.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| files | FileMetadata[] | Array of file metadata objects |
| stats | object | Statistics about the file walking process |

### ImportMap

A map that stores the resolved source file paths for local names in each file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filePath | string | The path of the file |
| localName | string | The local name of the imported entity |
| resolvedSourceFilePath | string | The resolved path of the source file |

### FileAnalysis

Represents the analysis of a file, including its imports.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filePath | string | The path of the file |
| imports | array | List of imports in the file |

**Relationships:**

- used-by ImportMap (FileAnalysis objects are used to build an ImportMap)

### Import

Represents an import statement in a file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| isRelative | boolean | Indicates if the import is relative |
| source | string | The source path of the import |
| importedNames | array | List of names imported |

**Relationships:**

- part-of FileAnalysis (Import objects are part of FileAnalysis objects)

### FunctionNode

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a function in the call graph
with its properties and relationships.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the function |
| fanIn | number | Number of incoming calls to the function |
| fanOut | number | Number of outgoing calls from the function |
| filePath | string | File path where the function is located |

**Relationships:**

- belongs to SerializedCallGraph (FunctionNode is part of a SerializedCallGraph)

### RefactorEntry

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a function with refactoring
issues and their details.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| function | string | Name of the function |
| file | string | File path where the function is located |
| className | string | Name of the class containing the function |
| fanIn | number | Number of incoming calls to the function |
| fanOut | number | Number of outgoing calls from the function |
| depth | number | Hops from nearest entry point. -1 indicates unreachable |
| sccSize | number | Size of the strongly connected component. 1 means no cycle |
| requirements | string[] | List of requirement names this function is mapped to |
| issues | RefactorIssue[] | List of refactoring issues identified for the function |
| priorityScore | number | Composite priority score for sorting (higher is more urgent) |

**Relationships:**

- has-many RefactorIssue (RefactorEntry can have multiple RefactorIssues)

### CycleSummary

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Summary of a strongly connected component
(cycle) in the call graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| sccId | number | Unique identifier for the strongly connected component |
| size | number | Number of functions in the cycle |
| participants | Array<{ function: string; file: string }> | List of functions and their file paths participating in the cycle |

### RefactorReport

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Report of refactoring issues and
statistics for the codebase.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| generatedAt | string | Timestamp when the report was generated |
| stats | object | Statistics about the codebase and refactoring issues |
| priorities | RefactorEntry[] | List of functions with issues, sorted by priorityScore descending |
| cycles | CycleSummary[] | List of strongly connected components (cycles) detected |

**Relationships:**

- has-many RefactorEntry (RefactorReport contains multiple RefactorEntries)
- has-many CycleSummary (RefactorReport contains multiple CycleSummaries)

### FunctionRecord

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a function with its metadata
and embedding vector for semantic search.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the function |
| name | string | Name of the function |
| filePath | string | Path to the file containing the function |
| className | string | Name of the class containing the function |
| language | string | Programming language of the function |
| signature | string | Function signature |
| docstring | string | Documentation string for the function |
| fanIn | number | Number of incoming calls to the function |
| fanOut | number | Number of outgoing calls from the function |
| isHub | boolean | Indicates if the function is a hub in the call graph |
| isEntryPoint | boolean | Indicates if the function is an entry point |
| text | string | Concatenated text used for embedding |
| vector | number[] | Embedding vector for the function |

### SearchResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a search result with a
function record and relevance score.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| record | Omit<FunctionRecord, 'vector'> | Function record without the vector field |
| score | number | Relevance score for the search result |

**Relationships:**

- contains FunctionRecord (Search result contains a function record)

### Bm25Corpus

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a corpus for BM25 sparse
retrieval.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| docs | Array<{ id: string; tfMap: Map<string, number>; length: number }> | Documents in the corpus |
| df | Map<string, number> | Term frequency map |
| avgLength | number | Average document length |
| N | number | Number of documents |

### FileSignatureMap

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a map of file paths to their
signature entries.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | File path |
| entries | Array<{ name: string; signature: string; docstring: string }> | Signature entries for the file |

### VectorIndex

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Builds and queries a LanceDB vector index
over the call graph functions.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| outputDir | string | Directory where the vector index is stored |
| nodes | FunctionNode[] | Array of function nodes from the call graph |
| signatures | FileSignatureMap[] | Array of file signature maps |
| hubIds | Set<string> | Set of hub function IDs |
| entryPointIds | Set<string> | Set of entry point function IDs |
| embedSvc | EmbeddingService | Service for generating embeddings |
| fileContents | Map<string, string> | Optional map of file paths to source content |
| incremental | boolean | Flag to enable incremental builds |
| query | string | Search query for the vector index |
| limit | number | Maximum number of results to return |
| language | string | Optional language filter for search results |
| minFanIn | number | Optional minimum fan-in filter for search results |
| hybrid | boolean | Flag to enable hybrid search (dense + sparse) |

**Relationships:**

- uses FunctionNode (VectorIndex uses FunctionNode data to build the index)
- uses FileSignatureMap (VectorIndex uses FileSignatureMap data to build the index)
- uses EmbeddingService (VectorIndex uses EmbeddingService to generate embeddings)

### ImportInfo

[PARTIAL SPEC — file too large to fully analyze (5 parts)] Information about an import statement in
a file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| source | string | The source path of the import |
| isRelative | boolean | Whether the import is relative |
| isPackage | boolean | Whether the import is from a package |
| isBuiltin | boolean | Whether the import is a Node.js builtin |
| importedNames | string[] | List of imported names |
| hasDefault | boolean | Whether there's a default import |
| hasNamespace | boolean | Whether there's a namespace import |
| isTypeOnly | boolean | Whether the import is type-only |
| isDynamic | boolean | Whether the import is dynamic |
| line | number | Line number where the import appears |

### ExportInfo

[PARTIAL SPEC — file too large to fully analyze (5 parts)] Information about an export statement in
a file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the exported entity |
| isDefault | boolean | Whether it's a default export |
| isType | boolean | Whether it's a type export |
| isReExport | boolean | Whether it's a re-export |
| reExportSource | string | Source of the re-export |
| kind | 'function' | 'class' | 'variable' | 'type' | 'interface' | 'enum' | 'unknown' | Type of the exported entity |
| line | number | Line number where the export appears |

### ResolveOptions

[PARTIAL SPEC — file too large to fully analyze (5 parts)] Options for import resolution.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| baseDir | string | Base directory for resolving relative imports |
| pathAliases | Record<string, string[]> | TypeScript paths aliases from tsconfig |
| extensions | string[] | File extensions to try when resolving |

### ImportExportParser

[PARTIAL SPEC — file too large to fully analyze (5 parts)] Parses source files to extract imports
and exports, supporting JavaScript/TypeScript and Python.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| cache | Map<string, FileAnalysis> | Cache for storing parsed file analyses |

**Relationships:**

- contains FileAnalysis (ImportExportParser contains and processes FileAnalysis objects)

### Export

[PARTIAL SPEC — file too large to fully analyze (5 parts)] Represents an export statement in a
source file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the exported entity |
| isDefault | boolean | Indicates if the export is a default export |

### ExtractedSignature

Represents a signature extracted from a source code file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the signature |
| type | string | Type of the signature (function, interface, etc.) |
| parameters | array | List of parameters for the signature |
| returnType | string | Return type of the signature |
| docstring | string | Documentation string for the signature |

### SignatureExtractor

Service for extracting and processing code signatures from source files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| STAGE1_MAX_CHARS | number | Maximum number of characters for Stage 1 processing |

**Relationships:**

- produces ExtractedSignature (SignatureExtractor produces ExtractedSignatures)
- produces FileSignatureMap (SignatureExtractor produces FileSignatureMaps)

### ArchitectureLayer

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents an architectural layer in the
codebase

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the layer |
| purpose | string | Purpose of the layer |
| files | string[] | Files belonging to this layer |
| representativeFile | string | null | Representative file for the layer |

### DetectedDomain

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents a detected domain in the
codebase

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the domain |
| suggestedSpecPath | string | Suggested path for the domain specification |
| files | string[] | Files belonging to this domain |
| entities | string[] | Entities detected in the domain |
| keyFile | string | null | Key file for the domain |

### EntryPointInfo

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Information about an entry point in the
codebase

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| file | string | File path of the entry point |
| type | 'application-entry' | 'api-entry' | 'test-entry' | 'build-entry' | Type of the entry point |
| initializes | string[] | Components initialized by this entry point |

### DataFlowInfo

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Information about data flow in the
codebase

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| sources | string[] | Source files in the data flow |
| sinks | string[] | Sink files in the data flow |
| transformers | string[] | Transformer files in the data flow |

### KeyFiles

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Key files categorized by their purpose

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| schemas | string[] | Schema files |
| config | string[] | Configuration files |
| auth | string[] | Authentication files |
| database | string[] | Database files |
| routes | string[] | Route files |
| services | string[] | Service files |

### RepoStructure

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Structure of the repository

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| projectName | string | Name of the project |
| projectType | string | Type of the project |
| frameworks | string[] | Frameworks used in the project |
| architecture | object | Architecture details |
| domains | DetectedDomain[] | Detected domains in the project |
| entryPoints | EntryPointInfo[] | Entry points in the project |
| dataFlow | DataFlowInfo | Data flow information |
| keyFiles | KeyFiles | Key files categorized by purpose |
| statistics | object | Statistics about the project |

**Relationships:**

- contains DetectedDomain (RepoStructure contains multiple DetectedDomain instances)
- contains EntryPointInfo (RepoStructure contains multiple EntryPointInfo instances)
- contains DataFlowInfo (RepoStructure contains a DataFlowInfo instance)
- contains KeyFiles (RepoStructure contains a KeyFiles instance)

### LLMContextPhase

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Phase of LLM context preparation

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| purpose | string | Purpose of the phase |
| files | object[] | Files included in the phase |
| totalTokens | number | undefined | Total tokens in the phase |
| estimatedTokens | number | undefined | Estimated tokens in the phase |

### LLMContext

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Context for LLM analysis

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| phase1_survey | LLMContextPhase | Survey phase context |
| phase2_deep | LLMContextPhase | Deep analysis phase context |
| phase3_validation | LLMContextPhase | Validation phase context |
| signatures | FileSignatureMap[] | undefined | File signatures |
| callGraph | SerializedCallGraph | undefined | Call graph |

**Relationships:**

- contains LLMContextPhase (LLMContext contains multiple LLMContextPhase instances)

### AnalysisArtifacts

[PARTIAL SPEC — file too large to fully analyze (3 parts)] All generated analysis artifacts

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| repoStructure | RepoStructure | Repository structure |
| summaryMarkdown | string | Summary in Markdown format |
| dependencyDiagram | string | Dependency diagram |
| llmContext | LLMContext | LLM context |

**Relationships:**

- contains RepoStructure (AnalysisArtifacts contains a RepoStructure instance)
- contains LLMContext (AnalysisArtifacts contains an LLMContext instance)

### ArtifactGeneratorOptions

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Options for artifact generation

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootDir | string | Root directory of the project |
| outputDir | string | Output directory for artifacts |
| maxDeepAnalysisFiles | number | undefined | Maximum files to include in LLM deep analysis |
| maxValidationFiles | number | undefined | Maximum files for validation phase |
| tokensPerChar | number | undefined | Approximate tokens per character for estimation |

### AnalysisArtifactGenerator

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Generates structured output files from
analysis results for LLM generation and human consumption.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| options | Required<ArtifactGeneratorOptions> | Configuration options for artifact generation |

**Relationships:**

- uses RepositoryMap (Uses repository map data for analysis)
- uses DependencyGraphResult (Uses dependency graph data for analysis)

### CppInclude

Represents a C++ #include directive parsed from a source file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| headerPath | string | Path to the included header file |
| isRelative | boolean | Whether the include is relative ("...") or system (<...>) |
| isSystem | boolean | Whether the include is a system header |

### CallGraphResult

Represents the result of a call graph analysis, containing nodes and edges representing function
calls and relationships between them.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodes | FunctionNode[] | Collection of function nodes in the graph |
| edges | CallEdge[] | Collection of call edges between functions |
| layerViolations | LayerViolation[] | Violations of architectural layer boundaries |

**Relationships:**

- contains FunctionNode (CallGraphResult contains multiple FunctionNodes)
- contains CallEdge (CallGraphResult contains multiple CallEdges)
- contains LayerViolation (CallGraphResult may contain LayerViolations)

### CallEdge

Represents a call relationship between two functions in the call graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| source | string | Qualified name of the calling function |
| target | string | Qualified name of the called function |
| lineNumber | number | Line number where the call occurs |
| confidence | EdgeConfidence | Confidence level of the call relationship |

**Relationships:**

- source FunctionNode (CallEdge connects to a source FunctionNode)
- target FunctionNode (CallEdge connects to a target FunctionNode)

### LayerViolation

Represents a violation of architectural layer boundaries in the call graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| sourceLayer | string | Name of the source layer |
| targetLayer | string | Name of the target layer |
| sourceFunction | string | Qualified name of the violating function |
| targetFunction | string | Qualified name of the called function |
| lineNumber | number | Line number where the violation occurs |

**Relationships:**

- references FunctionNode (LayerViolation references source and target FunctionNodes)

### ClassNode

Represents a class or interface in the call graph, grouping its methods and relationships.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| qualifiedName | string | Fully qualified name of the class |
| filePath | string | Path to the source file containing the class |
| startLine | number | Starting line number in the source file |
| endLine | number | Ending line number in the source file |
| methods | FunctionNode[] | Methods belonging to the class |
| docstring | string | Documentation string for the class |

**Relationships:**

- contains FunctionNode (ClassNode contains multiple FunctionNodes representing methods)
- source InheritanceEdge (ClassNode can be the source of InheritanceEdges)
- target InheritanceEdge (ClassNode can be the target of InheritanceEdges)

### InheritanceEdge

Represents an inheritance or implementation relationship between two classes in the call graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| source | string | Qualified name of the subclass |
| target | string | Qualified name of the superclass or interface |
| type | string | Type of relationship ('extends' or 'implements') |

**Relationships:**

- source ClassNode (InheritanceEdge connects to a source ClassNode)
- target ClassNode (InheritanceEdge connects to a target ClassNode)

### CloneInstance

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents an instance of a cloned code
block within a file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| file | string | Path to the source file containing the clone |
| functionName | string | Name of the function containing the clone |
| className | string | Name of the class containing the clone (optional) |
| startLine | number | Starting line number of the clone |
| endLine | number | Ending line number of the clone |

### CloneGroup

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a group of similar code
clones.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| type | CloneType | Type of clone (exact, structural, or near) |
| similarity | number | Similarity score between 0 and 1 |
| instances | CloneInstance[] | List of clone instances in this group |
| lineCount | number | Number of lines in the smallest instance |

**Relationships:**

- contains CloneInstance (A CloneGroup contains multiple CloneInstances)

### DuplicateDetectionResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the result of duplicate code
detection.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| cloneGroups | CloneGroup[] | List of detected clone groups |
| stats | object | Statistics about the duplicate detection process |

**Relationships:**

- contains CloneGroup (A DuplicateDetectionResult contains multiple CloneGroups)

### Entry

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Intermediate representation of a function
for duplicate detection.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| instance | CloneInstance | CloneInstance object |
| t1Hash | string | Type 1 hash of the function body |
| t2Hash | string | Type 2 hash of the function body |
| shingles | Set<string> | Set of shingles for the function body |

**Relationships:**

- contains-one CloneInstance (An Entry contains one CloneInstance object)

### TypeInferenceEngine

Engine for inferring types from source code using regex patterns for multiple programming languages.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| source | string | Source code to analyze |
| language | string | Programming language of the source code |

**Relationships:**

- uses FunctionRegistryTrie (Uses FunctionRegistryTrie to resolve method calls)
- returns FunctionNode (Returns FunctionNode instances for resolved method calls)

### InferredTypes

Map of variable names to inferred class names.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| variableName | string | Name of the variable |
| className | string | Inferred class name of the variable |

### ScoringConfig

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Configuration for custom scoring rules in
the significance scorer

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| highValueNames | Record<string, number> | Custom high-value name patterns with scores |
| negativeNames | Record<string, number> | Custom negative name patterns with scores |
| highValuePaths | Record<string, number> | Custom high-value path patterns with scores |
| minScore | number | Minimum score to include in results |

### FileRelationship

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Import/export relationship for
connectivity scoring

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filePath | string | Path to the file |
| imports | string[] | List of files imported by this file |
| importedBy | string[] | List of files that import this file |

### ScoredFile

[PARTIAL SPEC — file too large to fully analyze (2 parts)] A file with its calculated significance
score

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Absolute path to the file |
| score | number | Calculated significance score |
| nameScore | number | Name-based score component |
| pathScore | number | Path-based score component |
| structureScore | number | Structure-based score component |
| connectivityScore | number | Connectivity-based score component |

### SignificanceScorer

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Class responsible for scoring files by
their significance in the codebase.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| config | ScoringConfig | Configuration for the scoring algorithm |
| relationships | Map<string, FileRelationship> | Map of file relationships |

### HttpCall

[PARTIAL SPEC — file too large to fully analyze (4 parts)] Represents an HTTP call found in a
JavaScript/TypeScript source file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| file | string | Absolute path of the file containing the call |
| method | string | HTTP method, upper-cased. 'UNKNOWN' when it cannot be determined. |
| url | string | URL as written in source — may be a template literal or variable ref |
| normalizedUrl | string | Normalised, static portion of the URL (params stripped, prefix removed) |
| line | number | 1-based source line |
| client | string | axios / fetch / ky / got / custom |

### RouteDefinition

[PARTIAL SPEC — file too large to fully analyze (4 parts)] Represents a route handler found in a
Python source file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| file | string | Absolute path of the file containing the handler |
| method | string | HTTP method, upper-cased |
| path | string | Path pattern as declared (may contain {param} or <param> placeholders) |
| normalizedPath | string | Normalised path for matching |
| handlerName | string | Name of the handler function |
| framework | string | fastapi / flask / django / starlette |
| line | number | 1-based source line |

### HttpEdge

[PARTIAL SPEC — file too large to fully analyze (4 parts)] Represents a resolved cross-language edge
between an HTTP call and a route handler.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| callerFile | string | Absolute path of the JS/TS caller file |
| handlerFile | string | Absolute path of the Python handler file |
| method | string | HTTP method |
| path | string | Normalised path used for the match |
| call | HttpCall | The HTTP call details |
| route | RouteDefinition | The route handler details |
| confidence | string | How confident the match is (exact, path, fuzzy) |

**Relationships:**

- has-one HttpCall (HttpEdge has one HttpCall)
- has-one RouteDefinition (HttpEdge has one RouteDefinition)

### HTTP Call

[PARTIAL SPEC — file too large to fully analyze (4 parts)] Represents an HTTP request made from a
frontend file to a backend endpoint.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| file | string | Path to the file containing the HTTP call |
| method | string | HTTP method (GET, POST, etc.) |
| url | string | Original URL as written in the code |
| normalizedUrl | string | URL normalized for comparison |
| line | number | Line number in the source file where the call occurs |
| client | string | HTTP client library used (fetch, axios, etc.) |

**Relationships:**

- calls HTTP Route (HTTP Call is made to an HTTP Route)

### CodeChunk

A segment of code that represents a logical unit of code, typically a function, class, or interface.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| content | string | The actual code content of the chunk |
| filePath | string | The path to the file the chunk belongs to |
| maxChars | number | The maximum number of characters allowed in the chunk |
| overlapLines | number | The number of lines to overlap between chunks |

**Relationships:**

- belongs to CodeFile (A code chunk belongs to a code file)

### CodeFile

A file containing code that needs to be analyzed or processed.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | The file path |
| content | string | The content of the file |
| language | string | The programming language of the file |

**Relationships:**

- has-many CodeChunk (A code file can have many code chunks)

### Parser

A parser that uses tree-sitter to parse code and split it into chunks.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| language | string | The programming language the parser supports |

**Relationships:**

- creates CodeChunk (A parser creates code chunks)

### DigestOptions

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Configuration options for generating a
codebase digest.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Absolute path to the project root |
| outputDir | string | Absolute path to the .openlore/analysis/ output directory |
| maxEntryPoints | number | Max entry points to list |
| maxHubs | number | Max hub functions to list |
| maxGodFunctions | number | Max god functions to list |
| maxCoupledFiles | number | Max most-coupled files to list |

### CodebaseDigest

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Generates a compact, agent-readable
Markdown summary of the codebase architecture.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root directory of the codebase |
| outputDir | string | Directory where the digest will be saved |
| maxEntryPoints | number | Maximum number of entry points to display |
| maxHubs | number | Maximum number of hub functions to display |
| maxGodFunctions | number | Maximum number of god functions to display |
| maxCoupledFiles | number | Maximum number of coupled files to display |

**Relationships:**

- uses LLMContext (Uses LLMContext for call graph data)
- uses DependencyGraphResult (Uses DependencyGraphResult for file coupling data)

### ProgressIndicator

Manages progress indicators for CLI operations, including spinners and progress bars.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| spinner | Ora | null | The spinner instance from the ora library |
| enabled | boolean | Whether the spinner is enabled |
| prefix | string | Prefix for log messages |
| verbose | boolean | Whether to show verbose logs |
| logs | string[] | Collection of verbose logs |

### FileDiscoveryProgress

Tracks progress of file discovery operations.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| found | number | Number of files found |
| directories | number | Number of directories scanned |
| currentFile | string | undefined | Currently processing file |

### AnalysisProgress

Tracks progress of analysis operations.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| phase | 'imports' | 'scoring' | 'graph' | 'clustering' | Current analysis phase |
| current | string | undefined | Currently processing item |
| processed | number | undefined | Number of items processed |
| total | number | undefined | Total items to process |

### GenerationProgress

Tracks progress of generation operations.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| stage | number | Current stage number |
| totalStages | number | Total stages |
| stageName | string | Name of the current stage |
| tokensUsed | number | undefined | Number of tokens used |

### WritingProgress

Tracks progress of file writing operations.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| current | number | Current file number |
| total | number | Total files to write |
| currentFile | string | undefined | Currently writing file |

### Prompt

Represents an interactive prompt in the CLI tool

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| message | string | The message displayed to the user |
| choices | array | Available options for selection prompts |
| default | boolean|string|number | Default value for confirmation prompts |
| validate | function | Validation function for input prompts |

### SampleCount

Represents the number of files to verify

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| count | number | The number of files to verify |

### Logger

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Handles semantic logging with different
log levels and spinner support for long-running operations.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| options | LoggerOptions | Configuration options for the logger |
| activeSpinner | Ora | null | Reference to the currently active spinner |

### SpinnerController

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Manages spinner operations and delegates
to the Logger for non-spinner messages.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| spinner | Ora | null | Spinner instance for visual feedback |
| logger | Logger | Logger instance for non-spinner messages |

**Relationships:**

- uses Logger (SpinnerController delegates non-spinner messages to the Logger)

### ShutdownState

Represents the state of the application when it was interrupted during a shutdown process.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| phase | string | Current phase when interrupted |
| currentFiles | string[] | Files that were being processed |
| savedResults | string | Partial results that were saved |
| timestamp | number | Timestamp of interruption |

### ShutdownManager

Manages graceful shutdown processes, including state management and cleanup callbacks.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| callbacks | CleanupCallback[] | List of cleanup callbacks |
| isShuttingDown | boolean | Flag indicating if shutdown is in progress |
| state | ShutdownState | null | Current shutdown state |
| stateFile | string | Path to the state file |
| handlers | Map<string, Function> | Map of signal handlers |
| handlersAttached | boolean | Flag indicating if handlers are attached |

**Relationships:**

- contains ShutdownState (Manages instances of ShutdownState)

### CleanupCallback

Function type for cleanup operations during shutdown.

## Requirements

### Requirement: LanguageBreakdownValidation

The system SHALL validate LanguageBreakdown according to these rules:
- fileCount must be a non-negative integer
- percentage must be between 0 and 100

#### Scenario: CalculateLanguageDistribution
- **GIVEN** A repository with multiple file types
- **WHEN** LanguageBreakdown is calculated
- **THEN** Returns accurate counts and percentages for each language

### Requirement: DetectedFrameworkValidation

The system SHALL validate DetectedFramework according to these rules:
- category must be one of: frontend, backend, database, testing, auth, ci, other
- confidence must be one of: high, medium, low

#### Scenario: DetectFramework
- **GIVEN** A repository with framework-specific files
- **WHEN** Framework detection runs
- **THEN** Returns correct framework information with evidence

### Requirement: DirectoryStatsValidation

The system SHALL validate DirectoryStats according to these rules:
- fileCount must be a non-negative integer
- avgScore must be between 0 and 1

#### Scenario: AnalyzeDirectoryStructure
- **GIVEN** A repository with multiple directories
- **WHEN** DirectoryStats is calculated
- **THEN** Returns accurate statistics for each directory

### Requirement: RepositoryMapMetadataValidation

The system SHALL validate RepositoryMapMetadata according to these rules:
- analyzedAt must be a valid ISO 8601 timestamp

#### Scenario: GenerateRepositoryMetadata
- **GIVEN** A repository to analyze
- **WHEN** RepositoryMapMetadata is created
- **THEN** Returns complete metadata about the repository

### Requirement: RepositoryMapSummaryValidation

The system SHALL validate RepositoryMapSummary according to these rules:
- totalFiles must be equal to analyzedFiles + skippedFiles

#### Scenario: GenerateRepositorySummary
- **GIVEN** An analyzed repository
- **WHEN** RepositoryMapSummary is generated
- **THEN** Returns comprehensive summary statistics

### Requirement: RepositoryMapperOptionsValidation

The system SHALL validate RepositoryMapperOptions according to these rules:
- maxFiles must be a positive integer if provided

#### Scenario: ConfigureRepositoryMapping
- **GIVEN** Repository mapping options
- **WHEN** RepositoryMapper is configured
- **THEN** Applies the specified configuration

### Requirement: FrameworkDetectorValidation

The system SHALL validate FrameworkDetector according to these rules:
- Each framework detector must have a unique name
- Each framework detector must have a valid category
- The detect function must return an object with detected, confidence, and evidence properties

#### Scenario: DetectReactFramework
- **GIVEN** A project with react in dependencies and .jsx/.tsx files
- **WHEN** FrameworkDetector for React is called
- **THEN** Returns detected: true, confidence: high, and evidence: ['react in dependencies', '.jsx/.tsx files
found']

#### Scenario: DetectNext.jsFramework
- **GIVEN** A project with next in dependencies and next.config.* file
- **WHEN** FrameworkDetector for Next.js is called
- **THEN** Returns detected: true, confidence: high, and evidence: ['next in dependencies', 'next.config.*
found']

### Requirement: FileMetadataValidation

The system SHALL validate FileMetadata according to these rules:
- File name must be a non-empty string
- File extension must be a valid file extension
- Directory path must be a valid path string

#### Scenario: FileMetadataCreation
- **GIVEN** A file named 'index.js' in the 'src' directory
- **WHEN** File metadata is created
- **THEN** Creates FileMetadata with name: 'index.js', extension: '.js', directory: 'src'

### Requirement: PackageDependenciesValidation

The system SHALL validate PackageDependencies according to these rules:
- Dependencies must be a Record<string, string>
- DevDependencies must be a Record<string, string>

#### Scenario: PackageDependenciesDetection
- **GIVEN** A package.json with react and next in dependencies
- **WHEN** Package dependencies are read
- **THEN** Creates PackageDependencies with dependencies: { 'react': '^18.0.0', 'next': '^13.0.0' }

### Requirement: EmbeddingConfigValidation

The system SHALL validate EmbeddingConfig according to these rules:
- baseUrl must be a valid URL
- model must be a non-empty string
- batchSize must be a positive integer if provided

#### Scenario: CreateEmbeddingconfigFromEnvironmentVariables
- **GIVEN** Environment variables EMBED_BASE_URL and EMBED_MODEL are set
- **WHEN** EmbeddingService.fromEnv() is called
- **THEN** EmbeddingService is created with the specified configuration

#### Scenario: CreateEmbeddingconfigFromOpenLoreConfig
- **GIVEN** OpenLoreConfig contains embedding configuration
- **WHEN** EmbeddingService.fromConfig() is called
- **THEN** EmbeddingService is created with the specified configuration

### Requirement: EmbeddingServiceValidation

The system SHALL validate EmbeddingService according to these rules:
- texts must be an array of strings
- Each text must be truncated to MAX_CHARS_PER_TEXT characters
- API response must contain a data array with embeddings

#### Scenario: ComputeEmbeddingsForAListOfTexts
- **GIVEN** A list of texts
- **WHEN** embed() is called
- **THEN** Embeddings are computed and returned in the same order as the input texts

#### Scenario: HandleApiError
- **GIVEN** The API returns an error response
- **WHEN** callEmbeddingsApi() is called
- **THEN** An error is thrown with the API error details

### Requirement: ClusterSummaryValidation

The system SHALL validate ClusterSummary according to these rules:
- fileCount must be a positive integer
- entryPointCount must be a non-negative integer
- hubCount must be a non-negative integer
- dependsOn must contain valid cluster IDs
- keyFiles must contain relative file paths

#### Scenario: CreateClustersummary
- **GIVEN** A module cluster with files, hubs, and entry points
- **WHEN** buildArchitectureOverview is called
- **THEN** A ClusterSummary is created with the cluster's metrics and dependencies

### Requirement: ArchitectureOverviewValidation

The system SHALL validate ArchitectureOverview according to these rules:
- generatedAt must be a valid ISO string
- summary must contain valid statistics
- clusters must be an array of ClusterSummary objects
- globalEntryPoints must be an array of entry point objects
- criticalHubs must be an array of hub objects

#### Scenario: GenerateArchitectureoverview
- **GIVEN** Dependency graph and LLM context data
- **WHEN** buildArchitectureOverview is called
- **THEN** An ArchitectureOverview is generated with cluster summaries, entry points, and critical hubs

### Requirement: DependencyNodeValidation

The system SHALL validate DependencyNode according to these rules:
- id must be a non-empty string
- file must be a valid ScoredFile object
- exports must be an array of ExportInfo objects
- metrics must contain inDegree, outDegree, betweenness, and pageRank

#### Scenario: CreateDependencynode
- **GIVEN** A file with exports and metrics
- **WHEN** DependencyNode is created
- **THEN** Node is initialized with file, exports, and metrics
- **AND** Cluster information is optional

### Requirement: DependencyEdgeValidation

The system SHALL validate DependencyEdge according to these rules:
- source and target must be non-empty strings
- importedNames must be an array of strings
- weight must be a positive number
- httpEdge is optional

#### Scenario: CreateDependencyedge
- **GIVEN** Source and target nodes
- **WHEN** DependencyEdge is created
- **THEN** Edge is initialized with source, target, importedNames, and weight
- **AND** httpEdge is optional

### Requirement: FileClusterValidation

The system SHALL validate FileCluster according to these rules:
- id and name must be non-empty strings
- files must be an array of strings
- internalEdges and externalEdges must be non-negative numbers
- cohesion and coupling must be numbers between 0 and 1
- isStructural must be a boolean

#### Scenario: CreateFilecluster
- **GIVEN** A set of related files
- **WHEN** FileCluster is created
- **THEN** Cluster is initialized with files, internalEdges, externalEdges, cohesion, coupling, and
suggestedDomain
- **AND** isStructural is set based on internalEdges

### Requirement: DependencyGraphResultValidation

The system SHALL validate DependencyGraphResult according to these rules:
- nodes must be an array of DependencyNode objects
- edges must be an array of DependencyEdge objects
- clusters must be an array of FileCluster objects
- structuralClusters must be a subset of clusters
- rankings must contain byImportance, byConnectivity, clusterCenters, leafNodes, bridgeNodes, and orphanNodes
- cycles must be an array of arrays of strings
- statistics must contain nodeCount, edgeCount, importEdgeCount, httpEdgeCount, avgDegree, density, clusterCount, structuralClusterCount, and cycleCount

#### Scenario: CreateDependencygraphresult
- **GIVEN** Nodes, edges, clusters, rankings, cycles, and statistics
- **WHEN** DependencyGraphResult is created
- **THEN** Result is initialized with all provided data
- **AND** structuralClusters are derived from clusters

### Requirement: DependencyGraphOptionsValidation

The system SHALL validate DependencyGraphOptions according to these rules:
- rootDir must be a non-empty string
- extensions must be an array of strings if provided
- minClusterSize must be a positive number if provided
- dampingFactor must be a number between 0 and 1 if provided
- maxIterations must be a positive number if provided

#### Scenario: CreateDependencygraphoptions
- **GIVEN** Root directory and optional parameters
- **WHEN** DependencyGraphOptions is created
- **THEN** Options are initialized with rootDir and optional parameters
- **AND** Default values are used for optional parameters if not provided

### Requirement: DependencyGraphValidation

The system SHALL validate DependencyGraph according to these rules:
- Graph must be built before metrics can be calculated
- Nodes must be created before edges can be built
- Clusters must be detected before they can be assigned to nodes

#### Scenario: BuildDependencyGraph
- **GIVEN** A set of scored files
- **WHEN** build() is called
- **THEN** A complete dependency graph is created with nodes, edges, clusters, and metrics

#### Scenario: CalculateMetrics
- **GIVEN** A built dependency graph
- **WHEN** calculateDegrees(), calculateBetweenness(), or calculatePageRank() is called
- **THEN** The corresponding metrics are calculated and stored in the nodes

#### Scenario: DetectClusters
- **GIVEN** A built dependency graph
- **WHEN** detectClusters() is called
- **THEN** Clusters of related files are identified and stored in the graph

### Requirement: SpecRecordValidation

The system SHALL validate SpecRecord according to these rules:
- id must be unique
- section must be one of: requirements, purpose, entities, design, architecture, other
- linkedFiles must be a valid JSON-encoded string array

#### Scenario: CreateSpecrecord
- **GIVEN** Valid specification data
- **WHEN** SpecRecord is created
- **THEN** A new SpecRecord is created with the provided data

### Requirement: SpecSearchResultValidation

The system SHALL validate SpecSearchResult according to these rules:
- score must be a positive number
- record must be a valid SpecRecord object

#### Scenario: SearchSpecifications
- **GIVEN** A query and a populated vector index
- **WHEN** Search is performed
- **THEN** A list of SpecSearchResult objects is returned with relevant records and scores

### Requirement: ParsedSectionValidation

The system SHALL validate ParsedSection according to these rules:
- section must be a non-empty string
- title must be a non-empty string
- content must be a non-empty string

#### Scenario: ParseMarkdownFile
- **GIVEN** A Markdown file with specification content
- **WHEN** parseSpecFile is called
- **THEN** An array of ParsedSection objects is returned with the parsed sections

### Requirement: SpecVectorIndexValidation

The system SHALL validate SpecVectorIndex according to these rules:
- outputDir must be a valid path
- specsDir must contain spec.md files
- mappingJsonPath must be a valid JSON file if provided
- embedSvc must be an instance of EmbeddingService
- query must be a non-empty string
- limit must be a positive integer if provided
- domain must be a valid domain name if provided
- section must be a valid section name if provided

#### Scenario: BuildSpecvectorindex
- **GIVEN** A valid outputDir, specsDir, and embedSvc
- **WHEN** SpecVectorIndex.build() is called
- **THEN** A LanceDB table named 'specs' is created with embedded records
- **AND** If mappingJsonPath is provided, records are enriched with mapping data
- **AND** If no spec.md files are found, an error is thrown

#### Scenario: SearchSpecvectorindex
- **GIVEN** An existing SpecVectorIndex
- **WHEN** SpecVectorIndex.search() is called with a query
- **THEN** A list of matching records is returned
- **AND** Results are ranked by semantic similarity to the query
- **AND** Results can be filtered by domain and section if provided
- **AND** If the index does not exist, an error is thrown

### Requirement: FileWalkerValidation

The system SHALL validate FileWalker according to these rules:
- maxFiles must be a positive integer
- includePatterns and excludePatterns must be arrays of strings
- onProgress must be a function that accepts a FileWalkerProgress object
- signal must be an AbortSignal instance
- concurrency must be a positive integer

#### Scenario: WalkDirectoryWithDefaultOptions
- **GIVEN** A directory path
- **WHEN** FileWalker is instantiated with default options
- **THEN** All files in the directory are processed according to default patterns and filters

#### Scenario: WalkDirectoryWithCustomPatterns
- **GIVEN** A directory path and custom include/exclude patterns
- **WHEN** FileWalker is instantiated with custom patterns
- **THEN** Only files matching the custom patterns are processed

#### Scenario: WalkDirectoryWithProgressCallback
- **GIVEN** A directory path and a progress callback function
- **WHEN** FileWalker is instantiated with the progress callback
- **THEN** The progress callback is invoked with progress updates during processing

#### Scenario: CancelFileWalking
- **GIVEN** A FileWalker instance with an AbortSignal
- **WHEN** The AbortSignal is aborted
- **THEN** File walking is stopped immediately

### Requirement: FileWalkerProgressValidation

The system SHALL validate FileWalkerProgress according to these rules:
- filesFound must be a non-negative integer
- directoriesScanned must be a non-negative integer
- currentPath must be a valid file path string

#### Scenario: ValidFileWalkerProgressAccepted
- **GIVEN** A valid FileWalkerProgress object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: FileWalkerResultValidation

The system SHALL validate FileWalkerResult according to these rules:
- files must be an array of FileMetadata objects
- stats must be an object with valid statistics properties

#### Scenario: ValidFileWalkerResultAccepted
- **GIVEN** A valid FileWalkerResult object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: ImportMapValidation

The system SHALL validate ImportMap according to these rules:
- Each filePath must be unique in the map
- Each localName must be unique within a file's map

#### Scenario: BuildImportmap
- **GIVEN** A list of FileAnalysis objects
- **WHEN** buildImportMap is called
- **THEN** An ImportMap is created with resolved paths for each local name in each file

### Requirement: FileAnalysisValidation

The system SHALL validate FileAnalysis according to these rules:
- filePath must be a valid file path
- imports must be an array of Import objects

#### Scenario: ValidFileAnalysisAccepted
- **GIVEN** A valid FileAnalysis object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: ImportValidation

The system SHALL validate Import according to these rules:
- source must be a valid path
- importedNames must be an array of strings

#### Scenario: ValidImportAccepted
- **GIVEN** A valid Import object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: FunctionNodeValidation

The system SHALL validate FunctionNode according to these rules:
- fanIn must be a non-negative integer
- fanOut must be a non-negative integer
- filePath must be a valid file path string

#### Scenario: IdentifyCrossCuttingUtilitySink
- **GIVEN** A FunctionNode with fanOut === 0 and filePath matching UTILITY_PATH_PATTERNS
- **WHEN** isCrossCuttingHub is called
- **THEN** Returns true

### Requirement: RefactorEntryValidation

The system SHALL validate RefactorEntry according to these rules:
- fanIn must be a non-negative integer
- fanOut must be a non-negative integer
- depth must be a non-negative integer or -1 for unreachable
- sccSize must be a positive integer
- priorityScore must be a non-negative number

#### Scenario: CreateRefactorentry
- **GIVEN** Function details including name, file, fanIn, fanOut, depth, sccSize, requirements, issues, and
priorityScore
- **WHEN** RefactorEntry is created
- **THEN** RefactorEntry is initialized with the provided details

### Requirement: CycleSummaryValidation

The system SHALL validate CycleSummary according to these rules:
- sccId must be a non-negative integer
- size must be a positive integer
- participants must be a non-empty array of objects with function and file properties

#### Scenario: CreateCyclesummary
- **GIVEN** Cycle details including sccId, size, and participants
- **WHEN** CycleSummary is created
- **THEN** CycleSummary is initialized with the provided details

### Requirement: RefactorReportValidation

The system SHALL validate RefactorReport according to these rules:
- generatedAt must be a valid ISO 8601 timestamp string
- stats must be an object with specific properties
- priorities must be an array of RefactorEntry objects
- cycles must be an array of CycleSummary objects

#### Scenario: GenerateRefactorreport
- **GIVEN** Codebase analysis results including generatedAt, stats, priorities, and cycles
- **WHEN** RefactorReport is generated
- **THEN** RefactorReport is created with the provided details

### Requirement: VectorIndexValidation

The system SHALL validate VectorIndex according to these rules:
- Nodes array must not be empty when building the index
- Embedding count must match the number of records to embed
- Vector index must exist before performing a search

#### Scenario: BuildVectorIndex
- **GIVEN** Function nodes, signatures, hub IDs, and entry point IDs
- **WHEN** build() is called
- **THEN** A vector index is created or updated with the provided data

#### Scenario: SearchVectorIndex
- **GIVEN** A built vector index and a search query
- **WHEN** search() is called
- **THEN** Relevant function records are returned based on the query

#### Scenario: IncrementalBuild
- **GIVEN** An existing vector index and updated function data
- **WHEN** build() is called with incremental: true
- **THEN** Only changed functions are re-embedded, and unchanged functions reuse their cached vectors

### Requirement: ImportInfoValidation

The system SHALL validate ImportInfo according to these rules:
- source must be a non-empty string
- line must be a positive integer

#### Scenario: ParseRelativeImport
- **GIVEN** A file with a relative import statement
- **WHEN** the import is parsed
- **THEN** isRelative is true and source contains the relative path

#### Scenario: ParseBuiltinModule
- **GIVEN** A file with an import from a Node.js builtin module
- **WHEN** the import is parsed
- **THEN** isBuiltin is true and source matches a Node.js builtin

### Requirement: ExportInfoValidation

The system SHALL validate ExportInfo according to these rules:
- name must be a non-empty string
- kind must be one of the specified values
- line must be a positive integer

#### Scenario: ParseDefaultExport
- **GIVEN** A file with a default export
- **WHEN** the export is parsed
- **THEN** isDefault is true and name is the exported entity's name

#### Scenario: ParseReExport
- **GIVEN** A file with a re-export statement
- **WHEN** the export is parsed
- **THEN** isReExport is true and reExportSource contains the source path

### Requirement: ResolveOptionsValidation

The system SHALL validate ResolveOptions according to these rules:
- baseDir must be a non-empty string
- pathAliases must be an object with string keys and string array values
- extensions must be an array of strings

#### Scenario: ResolveWithPathAliases
- **GIVEN** ResolveOptions with pathAliases provided
- **WHEN** import resolution is performed
- **THEN** path aliases are used to resolve import paths

#### Scenario: ResolveWithCustomExtensions
- **GIVEN** ResolveOptions with extensions provided
- **WHEN** import resolution is performed
- **THEN** custom extensions are tried when resolving paths

### Requirement: ImportExportParserValidation

The system SHALL validate ImportExportParser according to these rules:
- File type must be supported (JavaScript/TypeScript or Python)
- Cache is cleared when clearCache() is called

#### Scenario: ParseASingleFile
- **GIVEN** A valid file path
- **WHEN** parseFile() is called
- **THEN** Returns a FileAnalysis object with imports and exports extracted

#### Scenario: ParseMultipleFiles
- **GIVEN** An array of valid file paths
- **WHEN** parseFiles() is called
- **THEN** Returns a Map of file paths to FileAnalysis objects

#### Scenario: ResolveAnImport
- **GIVEN** An import source and a file path
- **WHEN** resolveImport() is called
- **THEN** Returns the resolved file path or null if not found

### Requirement: ExportValidation

The system SHALL validate Export according to these rules:
- name must be a non-empty string
- isDefault must be a boolean value

#### Scenario: ValidExportAccepted
- **GIVEN** A valid Export object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: AnalysisArtifactGeneratorValidation

The system SHALL validate AnalysisArtifactGenerator according to these rules:
- Output directory must exist before saving artifacts
- File content must be readable for LLM context generation

#### Scenario: GenerateArtifacts
- **GIVEN** Repository map and dependency graph data
- **WHEN** generate() is called
- **THEN** Artifacts are generated and returned

#### Scenario: GenerateAndSaveArtifacts
- **GIVEN** Repository map and dependency graph data
- **WHEN** generateAndSave() is called
- **THEN** Artifacts are generated, saved to disk, and returned

#### Scenario: GenerateLlmContext
- **GIVEN** Repository map and dependency graph data
- **WHEN** generateLLMContext() is called
- **THEN** LLM context is generated with three phases of analysis

### Requirement: CppIncludeValidation

The system SHALL validate CppInclude according to these rules:
- headerPath must be a valid file path
- isRelative must be true for relative includes and false for system includes
- isSystem must be true for system headers and false for relative includes

#### Scenario: ParseRelativeInclude
- **GIVEN** A C++ source file with a relative include directive
- **WHEN** parseCppIncludes is called
- **THEN** A CppInclude object is created with isRelative set to true and isSystem set to false

#### Scenario: ParseSystemInclude
- **GIVEN** A C++ source file with a system include directive
- **WHEN** parseCppIncludes is called
- **THEN** A CppInclude object is created with isRelative set to false and isSystem set to true

### Requirement: CallGraphResultValidation

The system SHALL validate CallGraphResult according to these rules:
- Nodes must be unique by qualified name
- Edges must connect existing nodes
- Layer violations must reference valid layers

#### Scenario: GenerateCallGraph
- **GIVEN** A set of source files and optional layer definitions
- **WHEN** CallGraphBuilder.build() is called
- **THEN** A CallGraphResult is generated with nodes and edges representing function calls
- **AND** Layer violations are detected if layers are provided

### Requirement: CallEdgeValidation

The system SHALL validate CallEdge according to these rules:
- source and target must reference existing FunctionNodes
- lineNumber must be within the source function's line range
- confidence must be a valid EdgeConfidence value

#### Scenario: CreateCallEdge
- **GIVEN** A function call between two functions in the source code
- **WHEN** CallGraphBuilder processes the source file
- **THEN** A CallEdge is created connecting the source and target FunctionNodes

### Requirement: LayerViolationValidation

The system SHALL validate LayerViolation according to these rules:
- sourceLayer and targetLayer must be different
- sourceFunction and targetFunction must reference existing FunctionNodes
- lineNumber must be within the source function's line range

#### Scenario: DetectLayerViolation
- **GIVEN** A call between functions in different layers
- **WHEN** CallGraphBuilder.detectLayerViolations() is called
- **THEN** A LayerViolation is created documenting the architectural violation

### Requirement: ClassNodeValidation

The system SHALL validate ClassNode according to these rules:
- qualifiedName must be unique across the codebase
- startLine must be less than or equal to endLine
- filePath must point to an existing file

#### Scenario: CreateClassNode
- **GIVEN** Source code containing a class definition
- **WHEN** CallGraphBuilder processes the source file
- **THEN** A ClassNode is created with metadata extracted from the class definition

### Requirement: InheritanceEdgeValidation

The system SHALL validate InheritanceEdge according to these rules:
- source and target must reference existing ClassNodes
- type must be either 'extends' or 'implements'

#### Scenario: CreateInheritanceEdge
- **GIVEN** A class that extends or implements another class
- **WHEN** CallGraphBuilder processes the source file
- **THEN** An InheritanceEdge is created connecting the source and target ClassNodes

### Requirement: CloneInstanceValidation

The system SHALL validate CloneInstance according to these rules:
- startLine must be less than or equal to endLine
- startLine and endLine must be positive integers

#### Scenario: CreateCloneinstance
- **GIVEN** A code block identified as a clone
- **WHEN** CloneInstance is created
- **THEN** A new CloneInstance object is created with the provided file, functionName, className, startLine,
and endLine

### Requirement: CloneGroupValidation

The system SHALL validate CloneGroup according to these rules:
- similarity must be between 0 and 1
- lineCount must be a positive integer
- instances must contain at least two CloneInstances

#### Scenario: CreateClonegroup
- **GIVEN** A set of similar code clones
- **WHEN** CloneGroup is created
- **THEN** A new CloneGroup object is created with the provided type, similarity, instances, and lineCount

### Requirement: DuplicateDetectionResultValidation

The system SHALL validate DuplicateDetectionResult according to these rules:
- cloneGroups must be an array of CloneGroup objects
- stats must be an object with the required properties

#### Scenario: GenerateDuplicatedetectionresult
- **GIVEN** A set of detected clone groups and statistics
- **WHEN** DuplicateDetectionResult is generated
- **THEN** A new DuplicateDetectionResult object is created with the provided cloneGroups and stats

### Requirement: TypeInferenceEngineValidation

The system SHALL validate TypeInferenceEngine according to these rules:
- Only supports 7 programming languages (Python, C++, TypeScript, JavaScript, Go, Rust, Java, Ruby)
- Only tracks class names starting with uppercase letters to avoid primitive type false positives

#### Scenario: InferTypesFromPythonSource
- **GIVEN** Python source code with variable declarations
- **WHEN** inferTypesFromSource is called with language 'Python'
- **THEN** Returns a map of variable names to inferred class names
- **AND** Matches patterns like 'var = ClassName(...)'
- **AND** Matches patterns like 'var: ClassName ='
- **AND** Matches patterns like 'param: ClassName' in function signatures

#### Scenario: InferTypesFromC++Source
- **GIVEN** C++ source code with variable declarations
- **WHEN** inferTypesFromSource is called with language 'C++'
- **THEN** Returns a map of variable names to inferred class names
- **AND** Matches patterns like 'ClassName var;'
- **AND** Matches patterns like 'ClassName* var = new ClassName(...)'
- **AND** Matches patterns like 'auto var = make_shared<ClassName>(...)'
- **AND** Matches patterns like 'shared_ptr<ClassName> var'

#### Scenario: ResolveMethodCallViaTypeInference
- **GIVEN** A variable name and method name, and a map of inferred types
- **WHEN** resolveViaTypeInference is called
- **THEN** Returns the corresponding FunctionNode if the type is inferred
- **AND** Looks up the inferred type of the variable
- **AND** Searches the FunctionRegistryTrie for the method in the inferred class

### Requirement: HttpCallValidation

The system SHALL validate HttpCall according to these rules:
- method must be upper-cased
- client must be one of axios, fetch, ky, got, or custom

#### Scenario: CreateHttpCall
- **GIVEN** A JavaScript/TypeScript file with an HTTP call
- **WHEN** The file is parsed
- **THEN** An HttpCall object is created with the call details

### Requirement: RouteDefinitionValidation

The system SHALL validate RouteDefinition according to these rules:
- method must be upper-cased
- framework must be one of fastapi, flask, django, or starlette

#### Scenario: CreateRouteDefinition
- **GIVEN** A Python file with a route handler
- **WHEN** The file is parsed
- **THEN** A RouteDefinition object is created with the handler details

### Requirement: HttpEdgeValidation

The system SHALL validate HttpEdge according to these rules:
- confidence must be one of exact, path, or fuzzy

#### Scenario: CreateHttpEdge
- **GIVEN** An HttpCall and a RouteDefinition with matching method and path
- **WHEN** The edge is resolved
- **THEN** An HttpEdge object is created with the call, route, and confidence details

### Requirement: HTTP CallValidation

The system SHALL validate HTTP Call according to these rules:
- Method must be one of GET, POST, PUT, PATCH, DELETE, HEAD, OPTIONS
- URL must be a valid URL string
- Normalized URL must be a valid URL string
- Line number must be a positive integer

#### Scenario: ExtractHttpCallsFromAFile
- **GIVEN** A file with HTTP calls using fetch, axios, ky, or got
- **WHEN** extractHttpCalls is called with the file path
- **THEN** An array of HTTP Call objects is returned
- **AND** Each call includes the file path, method, URL, normalized URL, line number, and client
- **AND** Comments are stripped from the file content before parsing

### Requirement: CodeChunkValidation

The system SHALL validate CodeChunk according to these rules:
- Content must not be empty
- MaxChars must be a positive number
- OverlapLines must be a non-negative number

#### Scenario: ChunkCodeUsingAst
- **GIVEN** A code file with supported language and valid content
- **WHEN** astChunkContent is called
- **THEN** The code is split into chunks at AST declaration boundaries
- **AND** Each chunk after the first is prefixed with the file's import/header block
- **AND** Falls back to blank-line chunking if the language is unsupported or parsing fails

#### Scenario: ChunkCodeUsingBlankLines
- **GIVEN** A code file with unsupported language or invalid content
- **WHEN** astChunkContent is called
- **THEN** The code is split into chunks at blank lines
- **AND** Each chunk is limited to maxChars characters
- **AND** OverlapLines lines are included at the beginning of each chunk after the first

### Requirement: CodeFileValidation

The system SHALL validate CodeFile according to these rules:
- Path must not be empty
- Content must not be empty
- Language must be a valid programming language

#### Scenario: DetectLanguage
- **GIVEN** A code file with a valid path
- **WHEN** detectLanguage is called
- **THEN** The programming language of the file is detected

### Requirement: ParserValidation

The system SHALL validate Parser according to these rules:
- Language must be a valid programming language

#### Scenario: GetParserForLanguage
- **GIVEN** A supported programming language
- **WHEN** getParserForLanguage is called
- **THEN** A parser for the specified language is returned
- **AND** If the parser does not exist, it is created and stored for future use

### Requirement: CodebaseDigestValidation

The system SHALL validate CodebaseDigest according to these rules:
- rootPath must be a valid directory path
- outputDir must be a valid directory path
- maxEntryPoints, maxHubs, maxGodFunctions, and maxCoupledFiles must be positive integers

#### Scenario: GenerateCodebaseDigest
- **GIVEN** A codebase with analyzed call graph and dependency graph data
- **WHEN** generateCodebaseDigest is called with valid options
- **THEN** A CODEBASE.md file is generated in the specified output directory
- **AND** The digest includes entry points, critical hubs, god functions, most coupled files, layer
violations, and spec domains
- **AND** The digest is formatted as Markdown with appropriate sections and tables

#### Scenario: HandleMissingData
- **GIVEN** A codebase with missing call graph or dependency graph data
- **WHEN** generateCodebaseDigest is called
- **THEN** The digest is still generated but with limited sections
- **AND** Sections that require missing data are omitted
- **AND** The digest includes only available data

### Requirement: ProgressIndicatorValidation

The system SHALL validate ProgressIndicator according to these rules:
- Spinner can only be updated if enabled
- Logs are only stored when verbose mode is enabled

#### Scenario: StartProgressIndicator
- **GIVEN** A CLI operation begins
- **WHEN** start() is called with a message
- **THEN** A spinner is displayed with the provided message
- **AND** If not enabled, the message is logged instead

#### Scenario: UpdateProgress
- **GIVEN** A progress indicator is active
- **WHEN** update() is called with a new message
- **THEN** The spinner text is updated with the new message
- **AND** If not enabled, the message is logged instead

#### Scenario: CompleteProgressSuccessfully
- **GIVEN** A progress indicator is active
- **WHEN** succeed() is called
- **THEN** The spinner is marked as successful
- **AND** The spinner is cleared

#### Scenario: FailProgress
- **GIVEN** A progress indicator is active
- **WHEN** fail() is called
- **THEN** The spinner is marked as failed
- **AND** The spinner is cleared

#### Scenario: ShowWarning
- **GIVEN** A progress indicator is active
- **WHEN** warn() is called
- **THEN** A warning message is displayed
- **AND** The spinner is cleared

#### Scenario: ShowInfoMessage
- **GIVEN** A progress indicator is active
- **WHEN** info() is called
- **THEN** An info message is displayed
- **AND** The spinner is cleared

#### Scenario: StopProgress
- **GIVEN** A progress indicator is active
- **WHEN** stop() is called
- **THEN** The spinner is stopped without a status
- **AND** The spinner is cleared

### Requirement: PromptValidation

The system SHALL validate Prompt according to these rules:
- Message must be a non-empty string
- Choices must be an array of objects with value and name properties for selection prompts
- Default value must match the expected type for the prompt type

#### Scenario: OverwriteExistingFile
- **GIVEN** User is prompted to handle an existing spec file
- **WHEN** User selects 'backup-replace' option
- **THEN** The existing file is backed up and replaced with new content

#### Scenario: MergeWithExistingFile
- **GIVEN** User is prompted to handle an existing spec file
- **WHEN** User selects 'merge' option
- **THEN** Generated content is appended to the existing file

#### Scenario: SkipFile
- **GIVEN** User is prompted to handle an existing spec file
- **WHEN** User selects 'skip' option
- **THEN** The existing file remains unchanged

#### Scenario: SkipAllFiles
- **GIVEN** User is prompted to handle multiple existing spec files
- **WHEN** User selects 'skip-all' option
- **THEN** All existing files remain unchanged

### Requirement: SampleCountValidation

The system SHALL validate SampleCount according to these rules:
- Count must be a positive integer
- Count must not exceed 50

#### Scenario: SelectSampleCount
- **GIVEN** User is prompted to select the number of files to verify
- **WHEN** User selects a predefined option or enters a custom number
- **THEN** The selected sample count is used for verification

### Requirement: LoggerValidation

The system SHALL validate Logger according to these rules:
- Log levels must be one of: discovery, analysis, inference, success, warning, error, debug
- Spinner operations are disabled in quiet mode or when timestamps are enabled

#### Scenario: LogADiscoveryMessage
- **GIVEN** Logger instance with default options
- **WHEN** discovery() is called with a message
- **THEN** Message is logged with discovery prefix and color

#### Scenario: StartASpinner
- **GIVEN** Logger instance not in quiet mode
- **WHEN** spinner() is called with a message
- **THEN** Spinner is displayed with the provided message

#### Scenario: LogAnError
- **GIVEN** Logger instance
- **WHEN** error() is called with a message
- **THEN** Message is logged to stderr with error prefix and color

### Requirement: SpinnerControllerValidation

The system SHALL validate SpinnerController according to these rules:
- Spinner operations must be valid for the current log level
- Logger must be configured before use

#### Scenario: UpdateSpinnerText
- **GIVEN** SpinnerController instance with active spinner
- **WHEN** update() is called
- **THEN** Spinner text is updated with the provided message

#### Scenario: MarkSpinnerAsSucceeded
- **GIVEN** SpinnerController instance with active spinner
- **WHEN** succeed() is called
- **THEN** Spinner is marked as succeeded and message is logged via Logger

#### Scenario: MarkSpinnerAsFailed
- **GIVEN** SpinnerController instance with active spinner
- **WHEN** fail() is called
- **THEN** Spinner is marked as failed and error message is logged via Logger

### Requirement: ShutdownStateValidation

The system SHALL validate ShutdownState according to these rules:
- Phase must be one of: 'init', 'analyze', 'generate', 'verify'
- Timestamp must be a valid number

#### Scenario: SaveStateOnShutdown
- **GIVEN** Application is in the middle of a process
- **WHEN** Shutdown signal is received
- **THEN** Current state is saved to a file
- **AND** Cleanup callbacks are executed

#### Scenario: LoadPreviousState
- **GIVEN** Previous interrupted session exists
- **WHEN** Application starts
- **THEN** Previous state is loaded from file
- **AND** Resume suggestion is displayed

### Requirement: ShutdownManagerValidation

The system SHALL validate ShutdownManager according to these rules:
- State file path must be valid
- Cleanup callbacks must be functions
- Signal handlers must be properly registered

#### Scenario: HandleShutdownSignal
- **GIVEN** Shutdown signal is received
- **WHEN** handleShutdown is called
- **THEN** Cleanup callbacks are executed
- **AND** State is saved if available
- **AND** Process exits with appropriate code

#### Scenario: RegisterCleanupCallback
- **GIVEN** Application needs cleanup on shutdown
- **WHEN** onCleanup is called
- **THEN** Callback is added to the list of callbacks
- **AND** Callback will be executed during shutdown

### Requirement: CleanupCallbackValidation

The system SHALL validate CleanupCallback according to these rules:
- Must be a function that returns void or Promise<void>

#### Scenario: ValidCleanupCallbackAccepted
- **GIVEN** A valid CleanupCallback object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: Maprepository

The system SHALL analyzes repository structure and generates a comprehensive map of the codebase.

#### Scenario: SuccessfulMapping
- **GIVEN** Valid repository path and options
- **WHEN** mapRepository is called
- **THEN** Repository map is generated and returned

#### Scenario: ExceedsMaxFiles
- **GIVEN** Repository with more files than maxFiles option
- **WHEN** mapRepository is called
- **THEN** Only maxFiles are processed and included in the map

### Requirement: Detectframeworks

The system SHALL identifies frameworks used in the repository.

#### Scenario: FrameworksDetected
- **GIVEN** Repository with known frameworks
- **WHEN** detectFrameworks is called
- **THEN** Detected frameworks are returned with confidence levels

#### Scenario: NoFrameworksDetected
- **GIVEN** Repository with no known frameworks
- **WHEN** detectFrameworks is called
- **THEN** Empty array is returned

### Requirement: Clusterfiles

The system SHALL groups files by directory, domain, and layer.

#### Scenario: FilesClustered
- **GIVEN** List of scored files
- **WHEN** clusterFiles is called
- **THEN** Files are grouped by directory, domain, and layer

#### Scenario: NoFilesToCluster
- **GIVEN** Empty list of files
- **WHEN** clusterFiles is called
- **THEN** Empty clusters are returned

### Requirement: Generatesummary

The system SHALL generates a summary of the repository analysis.

#### Scenario: SummaryGenerated
- **GIVEN** Analyzed files, frameworks, and directories
- **WHEN** generateSummary is called
- **THEN** Summary is generated and returned

#### Scenario: NoDataToSummarize
- **GIVEN** Empty inputs
- **WHEN** generateSummary is called
- **THEN** Empty summary is returned

### Requirement: Embed

The system SHALL computes embeddings for a list of texts.

#### Scenario: EmptyInput
- **GIVEN** An empty array of texts
- **WHEN** embed is called
- **THEN** An empty array is returned

#### Scenario: SingleBatch
- **GIVEN** An array of texts with length less than or equal to batchSize
- **WHEN** embed is called
- **THEN** A single API call is made and embeddings are returned

#### Scenario: MultipleBatches
- **GIVEN** An array of texts with length greater than batchSize
- **WHEN** embed is called
- **THEN** Multiple API calls are made and embeddings are returned in the same order as the input texts

### Requirement: Fromenv

The system SHALL builds an embeddingservice from environment variables.

#### Scenario: ValidEnvironmentVariables
- **GIVEN** EMBED_BASE_URL and EMBED_MODEL environment variables are set
- **WHEN** fromEnv is called
- **THEN** An EmbeddingService instance is returned

#### Scenario: MissingEnvironmentVariables
- **GIVEN** EMBED_BASE_URL or EMBED_MODEL environment variables are not set
- **WHEN** fromEnv is called
- **THEN** An error is thrown

### Requirement: Fromconfig

The system SHALL builds an embeddingservice from a openloreconfig.

#### Scenario: ValidConfig
- **GIVEN** OpenLoreConfig with valid embedding configuration
- **WHEN** fromConfig is called
- **THEN** An EmbeddingService instance is returned

#### Scenario: InvalidConfig
- **GIVEN** OpenLoreConfig without valid embedding configuration
- **WHEN** fromConfig is called
- **THEN** null is returned

### Requirement: Getfilegodfunctions

The system SHALL identifies all god functions (fanout >= threshold) defined in a given file.

#### Scenario: FileWithGodFunctions
- **GIVEN** A file with functions having fanOut >= threshold
- **WHEN** getFileGodFunctions is called
- **THEN** Returns an array of FunctionNode objects representing the god functions

#### Scenario: FileWithoutGodFunctions
- **GIVEN** A file with no functions having fanOut >= threshold
- **WHEN** getFileGodFunctions is called
- **THEN** Returns an empty array

### Requirement: Extractsubgraph

The system SHALL extracts a depth-limited subgraph around a root node, following only outgoing (callee) edges.

#### Scenario: SubgraphExtractionWithinDepthLimit
- **GIVEN** A call graph and a root node
- **WHEN** extractSubgraph is called with a maxDepth that is not exceeded
- **THEN** Returns a SubGraph object containing nodes and edges within the specified depth

#### Scenario: SubgraphExtractionExceedingDepthLimit
- **GIVEN** A call graph and a root node
- **WHEN** extractSubgraph is called with a maxDepth that is exceeded
- **THEN** Returns a SubGraph object containing nodes and edges up to the specified depth

### Requirement: Formatsubgraph

The system SHALL formats a single subgraph as a compact ascii block for llm prompts.

#### Scenario: SubgraphWithDirectCallees
- **GIVEN** A subgraph with direct callees of the root node
- **WHEN** formatSubgraph is called
- **THEN** Returns a formatted string representing the subgraph with direct callees

#### Scenario: SubgraphWithoutDirectCallees
- **GIVEN** A subgraph without direct callees of the root node
- **WHEN** formatSubgraph is called
- **THEN** Returns a formatted string representing the subgraph without direct callees

### Requirement: Buildgraphpromptsection

The system SHALL builds a graph-based prompt section for a large file, including signatures and subgraphs for god functions.

#### Scenario: FileWithGodFunctionsAndCallGraphData
- **GIVEN** A file with god functions and available call graph data
- **WHEN** buildGraphPromptSection is called
- **THEN** Returns a formatted string representing the graph-based prompt section

#### Scenario: FileWithoutGodFunctionsOrCallGraphData
- **GIVEN** A file without god functions or available call graph data
- **WHEN** buildGraphPromptSection is called
- **THEN** Returns null, signaling the caller to fall back to raw source chunking

### Requirement: Inferclusterrole

The system SHALL determines the architectural role of a module cluster based on its metrics.

#### Scenario: EntryLayerCluster
- **GIVEN** Entry count is more than half of file count
- **WHEN** inferClusterRole is called
- **THEN** Returns 'entry_layer'

#### Scenario: OrchestratorCluster
- **GIVEN** Both hub and entry counts are greater than zero
- **WHEN** inferClusterRole is called
- **THEN** Returns 'orchestrator'

#### Scenario: CoreUtilitiesCluster
- **GIVEN** Hub count is greater than zero and entry count is zero
- **WHEN** inferClusterRole is called
- **THEN** Returns 'core_utilities'

#### Scenario: ApiLayerCluster
- **GIVEN** Entry count is greater than zero and hub count is zero
- **WHEN** inferClusterRole is called
- **THEN** Returns 'api_layer'

#### Scenario: InternalCluster
- **GIVEN** Neither hub nor entry counts are significant
- **WHEN** inferClusterRole is called
- **THEN** Returns 'internal'

### Requirement: Buildarchitectureoverview

The system SHALL constructs an architecture overview from dependency graph and llm context data.

#### Scenario: CompleteDataAvailable
- **GIVEN** Both dependency graph and LLM context are provided
- **WHEN** buildArchitectureOverview is called
- **THEN** Returns a complete ArchitectureOverview with all metrics populated

#### Scenario: MissingDependencyGraph
- **GIVEN** Dependency graph is null but LLM context is provided
- **WHEN** buildArchitectureOverview is called
- **THEN** Returns an ArchitectureOverview with cluster data from LLM context

#### Scenario: MissingLlmContext
- **GIVEN** LLM context is null but dependency graph is provided
- **WHEN** buildArchitectureOverview is called
- **THEN** Returns an ArchitectureOverview with cluster data from dependency graph

### Requirement: Builddependencygraph

The system SHALL constructs a dependency graph from file analysis data, including nodes, edges, clusters, and rankings.

#### Scenario: SuccessfulGraphConstruction
- **GIVEN** Valid project directory with supported file extensions
- **WHEN** buildDependencyGraph is called with valid options
- **THEN** Returns a complete DependencyGraphResult with nodes, edges, clusters, and rankings

#### Scenario: EmptyProjectDirectory
- **GIVEN** Empty project directory
- **WHEN** buildDependencyGraph is called
- **THEN** Returns an empty DependencyGraphResult with zero nodes and edges

### Requirement: Identifyclusters

The system SHALL identifies clusters of related files based on dependency patterns.

#### Scenario: ClustersIdentified
- **GIVEN** Nodes and edges with interconnected files
- **WHEN** identifyClusters is called
- **THEN** Returns an array of FileCluster objects with suggested domains and metrics

#### Scenario: NoClustersFound
- **GIVEN** Nodes and edges with no interconnected files
- **WHEN** identifyClusters is called
- **THEN** Returns an empty array

### Requirement: Calculatemetrics

The system SHALL calculates various metrics for nodes and edges in the graph.

#### Scenario: MetricsCalculated
- **GIVEN** Nodes and edges with valid connections
- **WHEN** calculateMetrics is called
- **THEN** Returns nodes and edges with updated metrics including inDegree, outDegree, betweenness, and
pageRank

#### Scenario: EmptyGraph
- **GIVEN** Empty nodes and edges arrays
- **WHEN** calculateMetrics is called
- **THEN** Returns empty nodes and edges arrays

### Requirement: Detectcycles

The system SHALL identifies cycles in the dependency graph.

#### Scenario: CyclesDetected
- **GIVEN** Nodes and edges forming a cycle
- **WHEN** detectCycles is called
- **THEN** Returns an array of cycles, each represented as an array of node IDs

#### Scenario: NoCyclesFound
- **GIVEN** Nodes and edges with no cycles
- **WHEN** detectCycles is called
- **THEN** Returns an empty array

### Requirement: Build

The system SHALL builds the dependency graph from scored files and returns the graph result.

#### Scenario: SuccessfulGraphBuilding
- **GIVEN** A list of scored files
- **WHEN** build is called
- **THEN** A complete dependency graph is built and returned

#### Scenario: EmptyFileList
- **GIVEN** An empty list of files
- **WHEN** build is called
- **THEN** An empty graph result is returned

### Requirement: Parsefiles

The system SHALL parses all files to extract imports/exports.

#### Scenario: SuccessfulParsing
- **GIVEN** A list of files
- **WHEN** parseFiles is called
- **THEN** A map of file analyses is returned

#### Scenario: FailedParsing
- **GIVEN** A list of files that cannot be parsed
- **WHEN** parseFiles is called
- **THEN** An empty map is returned

### Requirement: Buildhttpcrossedges

The system SHALL builds cross-language edges by matching http calls in js/ts files to fastapi/flask/django route definitions in python files.

#### Scenario: SuccessfulEdgeBuilding
- **GIVEN** A list of files with HTTP calls and route definitions
- **WHEN** buildHttpCrossEdges is called
- **THEN** Cross-language edges are built and added to the graph

#### Scenario: NoCrossLanguageEdges
- **GIVEN** A list of files without HTTP calls or route definitions
- **WHEN** buildHttpCrossEdges is called
- **THEN** No edges are added to the graph

### Requirement: Buildedges

The system SHALL builds edges from import relationships.

#### Scenario: SuccessfulEdgeBuilding
- **GIVEN** A list of files and their analyses
- **WHEN** buildEdges is called
- **THEN** Edges are built and added to the graph

#### Scenario: NoEdges
- **GIVEN** A list of files without imports
- **WHEN** buildEdges is called
- **THEN** No edges are added to the graph

### Requirement: Calculatedegrees

The system SHALL calculates in-degree and out-degree for each node.

#### Scenario: SuccessfulDegreeCalculation
- **GIVEN** A graph with nodes and edges
- **WHEN** calculateDegrees is called
- **THEN** In-degree and out-degree are calculated for each node

#### Scenario: EmptyGraph
- **GIVEN** An empty graph
- **WHEN** calculateDegrees is called
- **THEN** No degrees are calculated

### Requirement: Calculatebetweenness

The system SHALL calculates betweenness centrality using brandes' algorithm.

#### Scenario: SuccessfulBetweennessCalculation
- **GIVEN** A graph with nodes and edges
- **WHEN** calculateBetweenness is called
- **THEN** Betweenness centrality is calculated for each node

#### Scenario: EmptyGraph
- **GIVEN** An empty graph
- **WHEN** calculateBetweenness is called
- **THEN** No betweenness centrality is calculated

### Requirement: Calculatepagerank

The system SHALL calculates pagerank-style importance scores.

#### Scenario: SuccessfulPagerankCalculation
- **GIVEN** A graph with nodes and edges
- **WHEN** calculatePageRank is called
- **THEN** PageRank scores are calculated for each node

#### Scenario: EmptyGraph
- **GIVEN** An empty graph
- **WHEN** calculatePageRank is called
- **THEN** No PageRank scores are calculated

### Requirement: Detectclusters

The system SHALL detects clusters using a simple community detection approach.

#### Scenario: SuccessfulClusterDetection
- **GIVEN** A graph with nodes and edges
- **WHEN** detectClusters is called
- **THEN** Clusters are detected and returned

#### Scenario: NoClusters
- **GIVEN** A graph without clusters
- **WHEN** detectClusters is called
- **THEN** An empty list is returned

### Requirement: Suggestdomainname

The system SHALL suggests a domain name based on directory and file contents.

#### Scenario: SuccessfulDomainNameSuggestion
- **GIVEN** A directory and a list of files
- **WHEN** suggestDomainName is called
- **THEN** A domain name is suggested and returned

#### Scenario: NoDomainNameSuggestion
- **GIVEN** An empty directory and an empty list of files
- **WHEN** suggestDomainName is called
- **THEN** A default domain name is returned

### Requirement: Detectcycles

The system SHALL detects cycles in the dependency graph using dfs.

#### Scenario: SuccessfulCycleDetection
- **GIVEN** A graph with nodes and edges
- **WHEN** detectCycles is called
- **THEN** Cycles are detected and returned

#### Scenario: NoCycles
- **GIVEN** A graph without cycles
- **WHEN** detectCycles is called
- **THEN** An empty list is returned

### Requirement: Isduplicatecycle

The system SHALL checks if a cycle is a duplicate or rotation of an existing cycle.

#### Scenario: DuplicateCycle
- **GIVEN** A list of existing cycles and a new cycle
- **WHEN** isDuplicateCycle is called
- **THEN** True is returned if the new cycle is a duplicate or rotation of an existing cycle

#### Scenario: UniqueCycle
- **GIVEN** A list of existing cycles and a new cycle
- **WHEN** isDuplicateCycle is called
- **THEN** False is returned if the new cycle is unique

### Requirement: Normalizecycle

The system SHALL normalizes a cycle for comparison.

#### Scenario: SuccessfulNormalization
- **GIVEN** A cycle
- **WHEN** normalizeCycle is called
- **THEN** The cycle is normalized and returned

#### Scenario: EmptyCycle
- **GIVEN** An empty cycle
- **WHEN** normalizeCycle is called
- **THEN** An empty string is returned

### Requirement: Generaterankings

The system SHALL generates various rankings of nodes.

#### Scenario: SuccessfulRankingGeneration
- **GIVEN** A list of clusters
- **WHEN** generateRankings is called
- **THEN** Rankings are generated and returned

#### Scenario: EmptyClusters
- **GIVEN** An empty list of clusters
- **WHEN** generateRankings is called
- **THEN** Empty rankings are returned

### Requirement: Calculatestatistics

The system SHALL calculates overall statistics.

#### Scenario: SuccessfulStatisticsCalculation
- **GIVEN** A list of clusters and a list of cycles
- **WHEN** calculateStatistics is called
- **THEN** Statistics are calculated and returned

#### Scenario: EmptyClustersAndCycles
- **GIVEN** An empty list of clusters and an empty list of cycles
- **WHEN** calculateStatistics is called
- **THEN** Default statistics are returned

### Requirement: Builddependencygraph

The system SHALL builds a dependency graph from a list of scored files using the provided options.

#### Scenario: SuccessfulGraphBuilding
- **GIVEN** A list of scored files and valid options
- **WHEN** buildDependencyGraph is called
- **THEN** A dependency graph result is returned

### Requirement: Tod3format

The system SHALL converts the dependency graph result to d3.js force graph format.

#### Scenario: SuccessfulConversionToD3Format
- **GIVEN** A valid dependency graph result
- **WHEN** toD3Format is called
- **THEN** A D3.js force graph format object is returned

### Requirement: Tomermaidformat

The system SHALL converts the dependency graph result to mermaid diagram syntax.

#### Scenario: SuccessfulConversionToMermaidFormat
- **GIVEN** A valid dependency graph result and maxNodes value
- **WHEN** toMermaidFormat is called
- **THEN** A Mermaid diagram syntax string is returned

### Requirement: Todotformat

The system SHALL converts the dependency graph result to dot format (graphviz).

#### Scenario: SuccessfulConversionToDotFormat
- **GIVEN** A valid dependency graph result
- **WHEN** toDotFormat is called
- **THEN** A DOT format string is returned

### Requirement: Parsespecfile

The system SHALL parses a markdown specification file into logical sections.

#### Scenario: ValidMarkdownFile
- **GIVEN** A valid Markdown file with H2 and H3 headings
- **WHEN** parseSpecFile is called
- **THEN** The file is parsed into an array of ParsedSection objects

#### Scenario: MarkdownFileWithGeneratorComments
- **GIVEN** A Markdown file with generator comments
- **WHEN** parseSpecFile is called
- **THEN** The generator comments are skipped and the file is parsed into an array of ParsedSection objects

### Requirement: Buildmappingindex

The system SHALL builds a lookup index for mapping requirements to source files.

#### Scenario: ValidMappings
- **GIVEN** An array of valid MappingEntry objects
- **WHEN** buildMappingIndex is called
- **THEN** A Map object is returned with keys in the format 'domain:requirementKey' and values as arrays of
source file paths

#### Scenario: MappingsWithWildcardEntries
- **GIVEN** An array of MappingEntry objects with wildcard entries
- **WHEN** buildMappingIndex is called
- **THEN** Wildcard entries are filtered out and a Map object is returned with keys in the format
'domain:requirementKey' and values as arrays of source file paths

### Requirement: Buildtext

The system SHALL builds the text content for a specification section.

#### Scenario: ValidSection
- **GIVEN** A valid ParsedSection object
- **WHEN** buildText is called
- **THEN** The text content for the section is returned as a string

### Requirement: Walkdirectory

The system SHALL recursively traverse a directory and collect metadata for all relevant files.

#### Scenario: BasicDirectoryWalk
- **GIVEN** A directory with files and subdirectories
- **WHEN** walkDirectory is called
- **THEN** All relevant files are collected with their metadata

#### Scenario: WithIgnorePatterns
- **GIVEN** A directory with .gitignore and .openlore-ignore files
- **WHEN** walkDirectory is called
- **THEN** Files matching ignore patterns are excluded from results

#### Scenario: WithProgressCallback
- **GIVEN** A large directory structure
- **WHEN** walkDirectory is called with onProgress callback
- **THEN** Progress updates are sent during traversal

### Requirement: Hasshebang

The system SHALL determine if a file has a shebang line.

#### Scenario: FileWithShebang
- **GIVEN** A file starting with #!
- **WHEN** hasShebang is called
- **THEN** Returns true

#### Scenario: FileWithoutShebang
- **GIVEN** A regular text file
- **WHEN** hasShebang is called
- **THEN** Returns false

### Requirement: Countlines

The system SHALL count the number of lines in a file.

#### Scenario: SmallFile
- **GIVEN** A file smaller than MAX_READ_SIZE
- **WHEN** countLines is called
- **THEN** Returns the correct line count

#### Scenario: LargeFile
- **GIVEN** A file larger than MAX_READ_SIZE
- **WHEN** countLines is called
- **THEN** Returns -1

### Requirement: Istestfile

The system SHALL determine if a file appears to be a test file based on its path and name.

#### Scenario: TestDirectory
- **GIVEN** A file in a test directory
- **WHEN** isTestFile is called
- **THEN** Returns true

#### Scenario: TestFile
- **GIVEN** A file with a test suffix
- **WHEN** isTestFile is called
- **THEN** Returns true

### Requirement: Isconfigfile

The system SHALL determine if a file appears to be a configuration file based on its name.

#### Scenario: ConfigFile
- **GIVEN** A file matching config patterns
- **WHEN** isConfigFile is called
- **THEN** Returns true

#### Scenario: RegularFile
- **GIVEN** A regular source file
- **WHEN** isConfigFile is called
- **THEN** Returns false

### Requirement: Isgeneratedfile

The system SHALL determine if a file appears to be generated based on its name and path.

#### Scenario: GeneratedFile
- **GIVEN** A file with generated patterns
- **WHEN** isGeneratedFile is called
- **THEN** Returns true

#### Scenario: RegularFile
- **GIVEN** A regular source file
- **WHEN** isGeneratedFile is called
- **THEN** Returns false

### Requirement: Isentrypoint

The system SHALL determine if a file appears to be an entry point based on its name, path, and content.

#### Scenario: EntryPointFile
- **GIVEN** A file matching entry point patterns
- **WHEN** isEntryPoint is called
- **THEN** Returns true

#### Scenario: RegularFile
- **GIVEN** A regular source file
- **WHEN** isEntryPoint is called
- **THEN** Returns false

### Requirement: Loadignorepatterns

The system SHALL load and combine ignore patterns from various sources.

#### Scenario: With.gitignore
- **GIVEN** A directory with a .gitignore file
- **WHEN** loadIgnorePatterns is called
- **THEN** Patterns from .gitignore are included

#### Scenario: With.openloreIgnore
- **GIVEN** A directory with a .openlore-ignore file
- **WHEN** loadIgnorePatterns is called
- **THEN** Patterns from .openlore-ignore are included

### Requirement: Buildimportmap

The system SHALL constructs an importmap from fileanalysis objects for typescript/javascript/python files.

#### Scenario: ValidRelativeImports
- **GIVEN** FileAnalysis objects with relative imports
- **WHEN** buildImportMap is called
- **THEN** An ImportMap is created with resolved paths for each imported name

#### Scenario: NoRelativeImports
- **GIVEN** FileAnalysis objects without relative imports
- **WHEN** buildImportMap is called
- **THEN** An empty ImportMap is returned

### Requirement: Findcalleefileviaimport

The system SHALL finds the source file path for a callee name based on the caller file and importmap.

#### Scenario: CalleeFoundViaImport
- **GIVEN** An ImportMap with the callee name mapped to a source file
- **WHEN** findCalleeFileViaImport is called
- **THEN** The resolved source file path is returned

#### Scenario: CalleeNotFound
- **GIVEN** An ImportMap without the callee name
- **WHEN** findCalleeFileViaImport is called
- **THEN** undefined is returned

### Requirement: Parsegoimports

The system SHALL parses go import statements and maps local names to resolved source file paths.

#### Scenario: ValidGoImports
- **GIVEN** Go file content with valid relative imports
- **WHEN** parseGoImports is called
- **THEN** A map of local names to resolved source file paths is returned

#### Scenario: NoValidImports
- **GIVEN** Go file content without relative imports
- **WHEN** parseGoImports is called
- **THEN** An empty map is returned

### Requirement: Parserustimports

The system SHALL parses rust use statements and maps local names to resolved source file paths.

#### Scenario: ValidRustImports
- **GIVEN** Rust file content with valid use statements
- **WHEN** parseRustImports is called
- **THEN** A map of local names to resolved source file paths is returned

#### Scenario: NoValidImports
- **GIVEN** Rust file content without valid use statements
- **WHEN** parseRustImports is called
- **THEN** An empty map is returned

### Requirement: Parserubyimports

The system SHALL parses ruby require_relative statements and maps local names to resolved source file paths.

#### Scenario: ValidRubyImports
- **GIVEN** Ruby file content with valid require_relative statements
- **WHEN** parseRubyImports is called
- **THEN** A map of local names to resolved source file paths is returned

#### Scenario: NoValidImports
- **GIVEN** Ruby file content without valid require_relative statements
- **WHEN** parseRubyImports is called
- **THEN** An empty map is returned

### Requirement: Parsejavaimports

The system SHALL parses java import statements and maps local names to resolved source file paths.

#### Scenario: ValidJavaImports
- **GIVEN** Java file content with valid import statements
- **WHEN** parseJavaImports is called
- **THEN** A map of local names to resolved source file paths is returned

#### Scenario: NoValidImports
- **GIVEN** Java file content without valid import statements
- **WHEN** parseJavaImports is called
- **THEN** An empty map is returned

### Requirement: Computedepths

The system SHALL calculates the depth of each function from entry points using bfs traversal.

#### Scenario: EntryPointFunctions
- **GIVEN** Functions with fanIn === 0
- **WHEN** computeDepths is called
- **THEN** Depths are set to 0 for entry points

#### Scenario: UnreachableFunctions
- **GIVEN** Functions not reachable from any entry point
- **WHEN** computeDepths is called
- **THEN** Depths are set to -1 for unreachable functions

### Requirement: Computesccs

The system SHALL identifies strongly connected components (cycles) in the call graph using tarjan's algorithm.

#### Scenario: CycleDetection
- **GIVEN** Functions forming a cycle
- **WHEN** computeSCCs is called
- **THEN** Functions are assigned the same SCC ID

#### Scenario: NoCycles
- **GIVEN** Functions with no cycles
- **WHEN** computeSCCs is called
- **THEN** Each function gets a unique SCC ID

### Requirement: Iscrosscuttinghub

The system SHALL determines if a high-fanin node is a cross-cutting utility sink by design.

#### Scenario: UtilityFunction
- **GIVEN** Function in a utility/logging path with fanOut === 0
- **WHEN** isCrossCuttingHub is called
- **THEN** Returns true

#### Scenario: RegularFunction
- **GIVEN** Function not in a utility/logging path
- **WHEN** isCrossCuttingHub is called
- **THEN** Returns false

### Requirement: Buildbm25corpus

The system SHALL builds a bm25 corpus from function records for sparse retrieval.

#### Scenario: ValidRecords
- **GIVEN** An array of function records with text content
- **WHEN** buildBm25Corpus is called
- **THEN** A BM25 corpus is built and returned

### Requirement: Bm25score

The system SHALL calculates the bm25 score for a document given a query.

#### Scenario: ValidQueryAndDocument
- **GIVEN** A BM25 corpus, query tokens, and document index
- **WHEN** bm25Score is called
- **THEN** The BM25 score is calculated and returned

### Requirement: Rrfscore

The system SHALL calculates the reciprocal rank fusion score for merging two ranked lists.

#### Scenario: ValidRanks
- **GIVEN** Two ranks and an optional k value
- **WHEN** rrfScore is called
- **THEN** The RRF score is calculated and returned

### Requirement: Buildtext

The system SHALL builds the text to embed for a function, combining language, path, qualified name, signature, docstring, and skeleton body.

#### Scenario: ValidFunctionNode
- **GIVEN** A function node, signature, docstring, and optional file contents
- **WHEN** buildText is called
- **THEN** The text to embed is built and returned

### Requirement: Buildsignatureindex

The system SHALL builds a lookup map for file paths to signature entries.

#### Scenario: ValidSignatures
- **GIVEN** An array of file signature maps
- **WHEN** buildSignatureIndex is called
- **THEN** A lookup map is built and returned

### Requirement: Findsignatureentry

The system SHALL finds the best matching signature entry for a function node.

#### Scenario: ValidFunctionNodeAndSignatureIndex
- **GIVEN** A function node and a signature index
- **WHEN** findSignatureEntry is called
- **THEN** The best matching signature entry is found and returned

### Requirement: Detectlanguage

The system SHALL determines the programming language of a given file based on its extension and content.

#### Scenario: PythonFileDetection
- **GIVEN** A file with .py extension
- **WHEN** detectLanguage is called
- **THEN** Returns 'python'

#### Scenario: TypescriptFileDetection
- **GIVEN** A file with .ts extension
- **WHEN** detectLanguage is called
- **THEN** Returns 'typescript'

### Requirement: Extractsignatures

The system SHALL extracts code signatures from a file's content based on its detected language.

#### Scenario: PythonSignatureExtraction
- **GIVEN** Python file content with docstrings
- **WHEN** extractSignatures is called
- **THEN** Extracts function and class signatures with docstrings

#### Scenario: TypescriptSignatureExtraction
- **GIVEN** TypeScript file content with JSDoc comments
- **WHEN** extractSignatures is called
- **THEN** Extracts function and class signatures with JSDoc comments

### Requirement: Formatsignaturemaps

The system SHALL formats extracted signature maps into compact text blocks for stage 1 llm processing.

#### Scenario: DefaultFormatting
- **GIVEN** Signature maps with default maxChars
- **WHEN** formatSignatureMaps is called
- **THEN** Returns formatted text within 40,000 characters

#### Scenario: CustomFormatting
- **GIVEN** Signature maps with custom maxChars
- **WHEN** formatSignatureMaps is called
- **THEN** Returns formatted text within specified character limit

### Requirement: ConfidenceWeightedCallDistance

The analyzer SHALL expose a deterministic `callDistance` for each call edge, derived solely from the
edge's resolution confidence, and a weighted traversal that accumulates this distance. The metric
SHALL be a pure function of static analysis with no learned or stochastic component.

#### Scenario: Strongly resolved edges are nearer than heuristic ones

- **GIVEN** two callees of the same function, one reached by an `import`-resolved edge and one by a
  `name_only` edge
- **WHEN** call-distance is computed
- **THEN** the `import`-resolved callee has a strictly smaller distance than the `name_only` callee

#### Scenario: Weighted traversal reconstructs the cheapest path

- **GIVEN** a call graph with multiple paths between two functions
- **WHEN** `weightedBfs` runs from the source
- **THEN** each reached node carries its minimal accumulated distance, hop count, and a predecessor
  sufficient to reconstruct the cheapest path

### Requirement: MinimalContextScopedByNearestDistance

`get_minimal_context` SHALL rank neighbors by nearest call-distance within a risk-derived distance
budget and SHALL report each returned neighbor's distance and hop count. The budget SHALL floor at the
maximum direct-edge cost so that a function's direct neighbors are retained regardless of resolution
confidence; the budget governs how far past direct a tightly-coupled chain is surfaced, and the k-cap
together with distance ranking decides which neighbors survive when they are plentiful.

#### Scenario: A tightly coupled two-hop chain is surfaced and ranked ahead of farther neighbors

- **GIVEN** a target with a strongly-resolved direct caller, a strongly-resolved caller two hops away,
  and a weakly-resolved direct caller, plus a weakly-resolved caller beyond the budget
- **WHEN** `get_minimal_context` is invoked
- **THEN** the three within-budget callers are all returned, ordered nearest-first (the direct strong
  caller, then the two-hop strong chain, then the weak direct caller), each carrying its `distance`
  and `hops`, while the caller beyond the budget is excluded

#### Scenario: Direct neighbors are not dropped for weak resolution

- **GIVEN** a low-risk function whose only direct caller is reached by a weakly-resolved (`name_only`)
  edge
- **WHEN** `get_minimal_context` is invoked
- **THEN** that direct caller is still returned (the budget floors at the maximum direct-edge cost),
  not an empty caller list

### Requirement: StructuralLandmarkSignals

The analyzer SHALL label each function with the set of structural-interest **signals** it earns —
drawn from `hub`, `orchestrator`, `chokepoint`, `volatile`, `entrypoint`, `dead`. Each signal SHALL
be produced by an existing deterministic classifier (the same thresholds already behind
`get_critical_hubs`, `get_god_functions`, leaf/dead-code analysis, and change-coupling), and SHALL
carry the raw evidence that earned it. The analyzer SHALL NOT blend signals into a composite salience
score and SHALL NOT introduce any new threshold or weighting constant; ranking is the caller's
responsibility. The `get_landmarks` tool SHALL expose the whole-repo union of these labels, optionally
filtered to a single label, and SHALL ship in the opt-in `navigation` preset only — not the minimal
default surface.

#### Scenario: Each landmark carries its earned labels with evidence, and no score

- **GIVEN** a call graph containing a high fan-in hub, a high fan-out orchestrator, and a volatile
  (high-churn) function
- **WHEN** landmark signals are computed
- **THEN** the hub entry carries a `hub` label with its real `fanIn`, the orchestrator carries an
  `orchestrator` label with its real `fanOut`, and the volatile function carries a `volatile` label
  with its churn evidence
- **AND** no entry carries a composite `score` or rank field — only labels and evidence

#### Scenario: Chokepoint is a parameter-free composition, not a new cutoff

- **GIVEN** a function that is in the hub set but not in the god-function set
- **WHEN** landmark signals are computed
- **THEN** it carries the `chokepoint` label, derived solely from the conjunction `hub ∧ ¬orchestrator`
- **AND** the computation introduces no numeric threshold that is not already defined by the hub and
  god-function classifiers

### Requirement: OrientSurfacesTaskScopedLandmarks

The `orient` response SHALL include a bounded `landmarks` list: the labeled functions nearest to the
functions matched for the task, **ordered by structural proximity only** (call-distance / hop count),
each entry carrying its signal labels with evidence plus its `distance` and `hops`. The list SHALL be
present in full mode and omitted when `lean` is requested. The ordering SHALL NOT use a blended
salience score, and the matched functions themselves SHALL NOT appear as their own landmarks.

#### Scenario: Lean orient omits landmarks

- **GIVEN** an `orient` call with `lean = true`
- **WHEN** the response is produced
- **THEN** it contains no `landmarks` section, preserving the lean path's cost

#### Scenario: Full orient surfaces nearby anchors with labels, ordered by proximity

- **GIVEN** an `orient` call for a task that matches a set of functions, with labeled landmarks nearby
  in the call graph
- **WHEN** the full response is produced
- **THEN** `landmarks` lists the labeled functions nearest those matches, ordered by structural
  proximity, each entry carrying its `signals` (e.g. `hub`, `volatile`) with evidence and its
  `distance`/`hops`


## Sub-components

> `SignatureExtractor` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Detectlanguage

> Implements: `detectLanguage`

Determines the programming language of a source file based on its file extension and content.

#### Requirement: Languagedetection

The system SHALL the function shall detect the programming language of a given file by analyzing its file extension and content.

#### Scenario: Pythonfiledetection
- **GIVEN** A file with the path '/path/to/file.py'
- **WHEN** The detectLanguage function is called with the file path
- **THEN** The function SHALL return 'python'

### Sub-component: Extractpython

> Implements: `extractPython`

Extracts function signatures and docstrings from Python source files.

#### Requirement: Pythonsignatureextraction

The system SHALL the function shall extract function signatures and docstrings from python source code.

#### Scenario: Pythondocstringextraction
- **GIVEN** A Python file with a function definition and docstring
- **WHEN** The extractPython function is called with the file content
- **THEN** The function SHALL return the extracted function signatures and docstrings

### Sub-component: Extracttypescript

> Implements: `extractTypeScript`

Extracts function signatures and JSDoc comments from TypeScript source files.

#### Requirement: Typescriptsignatureextraction

The system SHALL the function shall extract function signatures and jsdoc comments from typescript source code.

#### Scenario: Typescriptjsdocextraction
- **GIVEN** A TypeScript file with a function definition and JSDoc comment
- **WHEN** The extractTypeScript function is called with the file content
- **THEN** The function SHALL return the extracted function signatures and JSDoc comments

### Sub-component: Extractgo

> Implements: `extractGo`

Extracts function signatures and comments from Go source files.

#### Requirement: Gosignatureextraction

The system SHALL the function shall extract function signatures and comments from go source code.

#### Scenario: Gocommentextraction
- **GIVEN** A Go file with a function definition and comment
- **WHEN** The extractGo function is called with the file content
- **THEN** The function SHALL return the extracted function signatures and comments

### Sub-component: Extractrust

> Implements: `extractRust`

Extracts function signatures and comments from Rust source files.

#### Requirement: Rustsignatureextraction

The system SHALL the function shall extract function signatures and comments from rust source code.

#### Scenario: Rustcommentextraction
- **GIVEN** A Rust file with a function definition and comment
- **WHEN** The extractRust function is called with the file content
- **THEN** The function SHALL return the extracted function signatures and comments

### Sub-component: Extractruby

> Implements: `extractRuby`

Extracts method signatures and comments from Ruby source files.

#### Requirement: Rubysignatureextraction

The system SHALL the function shall extract method signatures and comments from ruby source code.

#### Scenario: Rubycommentextraction
- **GIVEN** A Ruby file with a method definition and comment
- **WHEN** The extractRuby function is called with the file content
- **THEN** The function SHALL return the extracted method signatures and comments

### Sub-component: Extractcpp

> Implements: `extractCpp`

Extracts function signatures and comments from C++ source files.

#### Requirement: Cppsignatureextraction

The system SHALL the function shall extract function signatures and comments from c++ source code.

#### Scenario: Cppcommentextraction
- **GIVEN** A C++ file with a function definition and comment
- **WHEN** The extractCpp function is called with the file content
- **THEN** The function SHALL return the extracted function signatures and comments

### Sub-component: Extractgeneric

> Implements: `extractGeneric`

Extracts function signatures and comments from source files of unknown or unsupported languages.

#### Requirement: Genericsignatureextraction

The system SHALL the function shall extract function signatures and comments from source code of unknown or unsupported languages.

#### Scenario: Genericcommentextraction
- **GIVEN** A file with a function definition and comment in an unsupported language
- **WHEN** The extractGeneric function is called with the file content
- **THEN** The function SHALL return the extracted function signatures and comments

### Requirement: Generateartifacts

The system SHALL generates all analysis artifacts including repository structure, dependency diagrams, and llm context.

#### Scenario: SuccessfulArtifactGeneration
- **GIVEN** Valid project directory and output directory
- **WHEN** generateArtifacts is called
- **THEN** All artifacts are generated and saved to the output directory

#### Scenario: InvalidProjectDirectory
- **GIVEN** Non-existent project directory
- **WHEN** generateArtifacts is called
- **THEN** An error is thrown indicating the directory does not exist

### Requirement: Istestfile

The system SHALL determines if a file is a test/spec file based on its path and name.

#### Scenario: TestFileDetected
- **GIVEN** A file path matching test/spec patterns
- **WHEN** isTestFile is called
- **THEN** Returns true

#### Scenario: NonTestFileDetected
- **GIVEN** A file path not matching test/spec patterns
- **WHEN** isTestFile is called
- **THEN** Returns false

### Requirement: Repostructuretorepomap

The system SHALL converts a serialized repostructure to a minimal repositorymap-compatible object.

#### Scenario: SuccessfulConversion
- **GIVEN** Valid RepoStructure object
- **WHEN** repoStructureToRepoMap is called
- **THEN** Returns a RepositoryMap object with populated metadata and summary fields

#### Scenario: InvalidRepostructure
- **GIVEN** Invalid RepoStructure object
- **WHEN** repoStructureToRepoMap is called
- **THEN** An error is thrown indicating invalid input

### Requirement: Generate

The system SHALL generates all analysis artifacts including repository structure, summary markdown, dependency diagram, and llm context.

#### Scenario: GenerateArtifacts
- **GIVEN** Valid repository map and dependency graph
- **WHEN** generate is called
- **THEN** All artifacts are generated and returned

### Requirement: Generateandsave

The system SHALL generates all analysis artifacts and saves them to disk.

#### Scenario: GenerateAndSaveArtifacts
- **GIVEN** Valid repository map and dependency graph
- **WHEN** generateAndSave is called
- **THEN** All artifacts are generated, saved to disk, and returned

### Requirement: Generaterepostructure

The system SHALL generates the repository structure including architecture pattern, layers, domains, entry points, data flow, and key files.

#### Scenario: GenerateRepositoryStructure
- **GIVEN** Valid repository map and dependency graph
- **WHEN** generateRepoStructure is called
- **THEN** Repository structure is generated and returned

### Requirement: Generatesummarymarkdown

The system SHALL generates a summary markdown file with project overview, architecture pattern, language breakdown, detected domains, dependency insights, and recommendations.

#### Scenario: GenerateSummaryMarkdown
- **GIVEN** Valid repository map, dependency graph, and repository structure
- **WHEN** generateSummaryMarkdown is called
- **THEN** Summary markdown is generated and returned

### Requirement: Generatedependencydiagram

The system SHALL generates a dependency diagram in mermaid format.

#### Scenario: GenerateDependencyDiagram
- **GIVEN** Valid dependency graph
- **WHEN** generateDependencyDiagram is called
- **THEN** Dependency diagram in Mermaid format is generated and returned

### Requirement: Generatellmcontext

The system SHALL generates llm context preparation including survey, deep analysis, validation, signatures, call graph, and refactoring priorities.

#### Scenario: GenerateLlmContext
- **GIVEN** Valid repository map and dependency graph
- **WHEN** generateLLMContext is called
- **THEN** LLM context is generated and returned

### Requirement: Parsecppincludes

The system SHALL parses #include directives from a c++ source file and categorizes them as relative, system, or unknown.

#### Scenario: RelativeInclude
- **GIVEN** A C++ file with a relative #include directive
- **WHEN** parseCppIncludes is called
- **THEN** The directive is parsed and marked as relative

#### Scenario: SystemInclude
- **GIVEN** A C++ file with a system #include directive
- **WHEN** parseCppIncludes is called
- **THEN** The directive is parsed and marked as system

### Requirement: Buildheadertoimplmap

The system SHALL builds a map from header file paths to their corresponding implementation file paths based on naming conventions.

#### Scenario: MatchingHeaderAndImplementationFiles
- **GIVEN** A list of file paths with matching header and implementation files
- **WHEN** buildHeaderToImplMap is called
- **THEN** A map is created with header paths as keys and implementation paths as values

### Requirement: Buildcppimportmap

The system SHALL builds a map from c++ source files to sets of implementation files reachable via their #include directives.

#### Scenario: NonSystemIncludes
- **GIVEN** A C++ file with non-system #include directives
- **WHEN** buildCppImportMap is called
- **THEN** A map is created with the source file path as key and a set of accessible implementation files as
value

### Requirement: Buildcallgraph

The system SHALL constructs a call graph from source files, including function calls, class relationships, and http edges.

#### Scenario: BuildGraphWithLayers
- **GIVEN** Source files and layer definitions
- **WHEN** build is called
- **THEN** Call graph with nodes and edges populated

#### Scenario: BuildGraphWithoutLayers
- **GIVEN** Source files without layer definitions
- **WHEN** build is called
- **THEN** Call graph with nodes and edges populated without layer violations

### Requirement: Detectlayerviolations

The system SHALL identifies architectural violations where calls cross layer boundaries.

#### Scenario: ViolationsDetected
- **GIVEN** Calls between layers that violate defined boundaries
- **WHEN** detectLayerViolations is called
- **THEN** Array of LayerViolation objects is returned

#### Scenario: NoViolations
- **GIVEN** All calls respect layer boundaries
- **WHEN** detectLayerViolations is called
- **THEN** Empty array is returned

### Requirement: Extractclassrelationships

The system SHALL extracts inheritance and implementation relationships between classes.

#### Scenario: RelationshipsExtracted
- **GIVEN** Files containing class definitions
- **WHEN** extractClassRelationships is called
- **THEN** Class nodes and inheritance edges are returned

#### Scenario: NoRelationships
- **GIVEN** Files without class definitions
- **WHEN** extractClassRelationships is called
- **THEN** Empty arrays are returned


## Sub-components

> `CallGraphBuilder` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Gettsparser

> Implements: `getTSParser`

Provides a TypeScript parser instance for parsing TypeScript code.

#### Requirement: Providetsparser

The system SHALL the function shall provide a typescript parser instance when called.

#### Scenario: Gettsparser
- **GIVEN** The function is called
- **WHEN** The function is invoked
- **THEN** A TypeScript parser instance is returned

### Sub-component: Safequery

> Implements: `safeQuery`

Safely queries a parser to avoid runtime errors.

#### Requirement: Safequeryparser

The system SHALL the function shall safely query a parser to avoid runtime errors.

#### Scenario: Safequery
- **GIVEN** A parser and a query are provided
- **WHEN** The function is called with the parser and query
- **THEN** The query result is returned if successful, otherwise null is returned

### Sub-component: Merge

> Implements: `merge`

Merges multiple call graphs into a single call graph.

#### Requirement: Mergecallgraphs

The system SHALL the function shall merge multiple call graphs into a single call graph.

#### Scenario: Merge
- **GIVEN** Multiple call graphs are provided
- **WHEN** The function is called with the call graphs
- **THEN** A merged call graph is returned

### Sub-component: Getpyparser

> Implements: `getPyParser`

Provides a Python parser instance for parsing Python code.

#### Requirement: Providepyparser

The system SHALL the function shall provide a python parser instance when called.

#### Scenario: Getpyparser
- **GIVEN** The function is called
- **WHEN** The function is invoked
- **THEN** A Python parser instance is returned

### Sub-component: Getjavaparser

> Implements: `getJavaParser`

Provides a Java parser instance for parsing Java code.

#### Requirement: Providejavaparser

The system SHALL the function shall provide a java parser instance when called.

#### Scenario: Getjavaparser
- **GIVEN** The function is called
- **WHEN** The function is invoked
- **THEN** A Java parser instance is returned

### Sub-component: Getcppparser

> Implements: `getCppParser`

Provides a C++ parser instance for parsing C++ code.

#### Requirement: Providecppparser

The system SHALL the function shall provide a c++ parser instance when called.

#### Scenario: Getcppparser
- **GIVEN** The function is called
- **WHEN** The function is invoked
- **THEN** A C++ parser instance is returned

### Sub-component: Getrubyparser

> Implements: `getRubyParser`

Provides a Ruby parser instance for parsing Ruby code.

#### Requirement: Providerubyparser

The system SHALL the function shall provide a ruby parser instance when called.

#### Scenario: Getrubyparser
- **GIVEN** The function is called
- **WHEN** The function is invoked
- **THEN** A Ruby parser instance is returned

### Sub-component: Getgoparser

> Implements: `getGoParser`

Provides a Go parser instance for parsing Go code.

#### Requirement: Providegoparser

The system SHALL the function shall provide a go parser instance when called.

#### Scenario: Getgoparser
- **GIVEN** The function is called
- **WHEN** The function is invoked
- **THEN** A Go parser instance is returned

### Sub-component: Extractpygraph

> Implements: `extractPyGraph`

Extracts a call graph from Python code.

#### Requirement: Extractpythongraph

The system SHALL the function shall extract a call graph from python code.

#### Scenario: Extractpygraph
- **GIVEN** Python code is provided
- **WHEN** The function is called with the Python code
- **THEN** A call graph is returned

### Sub-component: Extracttsgraph

> Implements: `extractTSGraph`

Extracts a call graph from TypeScript code.

#### Requirement: Extracttypescriptgraph

The system SHALL the function shall extract a call graph from typescript code.

#### Scenario: Extracttsgraph
- **GIVEN** TypeScript code is provided
- **WHEN** The function is called with the TypeScript code
- **THEN** A call graph is returned

### Sub-component: Extractgograph

> Implements: `extractGoGraph`

Extracts a call graph from Go code.

#### Requirement: Extractgograph

The system SHALL the function shall extract a call graph from go code.

#### Scenario: Extractgograph
- **GIVEN** Go code is provided
- **WHEN** The function is called with the Go code
- **THEN** A call graph is returned

### Sub-component: Extractrustgraph

> Implements: `extractRustGraph`

Extracts a call graph from Rust code.

#### Requirement: Extractrustgraph

The system SHALL the function shall extract a call graph from rust code.

#### Scenario: Extractrustgraph
- **GIVEN** Rust code is provided
- **WHEN** The function is called with the Rust code
- **THEN** A call graph is returned

### Sub-component: Extractrubygraph

> Implements: `extractRubyGraph`

Extracts a call graph from Ruby code.

#### Requirement: Extractrubygraph

The system SHALL the function shall extract a call graph from ruby code.

#### Scenario: Extractrubygraph
- **GIVEN** Ruby code is provided
- **WHEN** The function is called with the Ruby code
- **THEN** A call graph is returned

### Sub-component: Extractjavagraph

> Implements: `extractJavaGraph`

Extracts a call graph from Java code.

#### Requirement: Extractjavagraph

The system SHALL the function shall extract a call graph from java code.

#### Scenario: Extractjavagraph
- **GIVEN** Java code is provided
- **WHEN** The function is called with the Java code
- **THEN** A call graph is returned

### Sub-component: Extractcppgraph

> Implements: `extractCppGraph`

Extracts a call graph from C++ code.

#### Requirement: Extractcppgraph

The system SHALL the function shall extract a call graph from c++ code.

#### Scenario: Extractcppgraph
- **GIVEN** C++ code is provided
- **WHEN** The function is called with the C++ code
- **THEN** A call graph is returned

### Sub-component: Findbyqualifiedname

> Implements: `findByQualifiedName`

Finds function nodes by their qualified names.

#### Requirement: Findfunctionbyqualifiedname

The system SHALL the function shall find function nodes by their qualified names.

#### Scenario: Findbyqualifiedname
- **GIVEN** A class name and method name are provided
- **WHEN** The function is called with the class name and method name
- **THEN** An array of FunctionNode objects is returned

### Sub-component: Infertypesfromsource

> Implements: `inferTypesFromSource`

Infers types from source code for various programming languages.

#### Requirement: Infertypes

The system SHALL the function shall infer types from source code for various programming languages.

#### Scenario: Infertypesfromsource
- **GIVEN** Source code and a language are provided
- **WHEN** The function is called with the source code and language
- **THEN** An InferredTypes object is returned

### Sub-component: Resolveviatypeinference

> Implements: `resolveViaTypeInference`

Resolves a callee object via type inference.

#### Requirement: Resolvecalleeviatypeinference

The system SHALL the function shall resolve a callee object via type inference.

#### Scenario: Resolveviatypeinference
- **GIVEN** A callee object, callee name, inferred types, and trie are provided
- **WHEN** The function is called with the callee object, callee name, inferred types, and trie
- **THEN** A resolved callee object is returned

### Sub-component: Findbysimplename

> Implements: `findBySimpleName`

Finds function nodes by their simple names.

#### Requirement: Findfunctionbysimplename

The system SHALL the function shall find function nodes by their simple names.

#### Scenario: Findbysimplename
- **GIVEN** A function name is provided
- **WHEN** The function is called with the function name
- **THEN** An array of FunctionNode objects is returned

### Sub-component: Extractallhttpedges

> Implements: `extractAllHttpEdges`

Extracts HTTP edges from mixed JS+Python codebases.

#### Requirement: Extracthttpedges

The system SHALL the function shall extract http edges from mixed js+python codebases.

#### Scenario: Extractallhttpedges
- **GIVEN** An array of file paths is provided
- **WHEN** The function is called with the array of file paths
- **THEN** A promise that resolves to HTTP edges is returned

### Sub-component: Findenclosingfunction

> Implements: `findEnclosingFunction`

Finds the enclosing function for a given node.

#### Requirement: Findenclosingfunction

The system SHALL the function shall find the enclosing function for a given node.

#### Scenario: Findenclosingfunction
- **GIVEN** A node is provided
- **WHEN** The function is called with the node
- **THEN** The enclosing function is returned

### Sub-component: Extractclassrelationships

> Implements: `extractClassRelationships`

Extracts class relationships from code.

#### Requirement: Extractclassrelationships

The system SHALL the function shall extract class relationships from code.

#### Scenario: Extractclassrelationships
- **GIVEN** Code is provided
- **WHEN** The function is called with the code
- **THEN** Class relationships are returned

### Sub-component: Buildclassnodes

> Implements: `buildClassNodes`

Builds class nodes from class relationships.

#### Requirement: Buildclassnodes

The system SHALL the function shall build class nodes from class relationships.

#### Scenario: Buildclassnodes
- **GIVEN** Class relationships are provided
- **WHEN** The function is called with the class relationships
- **THEN** Class nodes are returned

### Requirement: Detectduplicates

The system SHALL analyzes a call graph to identify duplicate functions across the codebase.

#### Scenario: ExactDuplicateDetection
- **GIVEN** Two functions with identical normalized content
- **WHEN** detectDuplicates is called
- **THEN** The functions are grouped as 'exact' clones

#### Scenario: StructuralDuplicateDetection
- **GIVEN** Two functions with similar structure but different identifiers
- **WHEN** detectDuplicates is called
- **THEN** The functions are grouped as 'structural' clones

#### Scenario: NearDuplicateDetection
- **GIVEN** Two functions with high Jaccard similarity
- **WHEN** detectDuplicates is called
- **THEN** The functions are grouped as 'near' clones

### Requirement: Normalizecode

The system SHALL normalizes code content to facilitate duplicate detection.

#### Scenario: CommentStripping
- **GIVEN** Code with comments
- **WHEN** normalizeCode is called
- **THEN** Comments are removed from the code

#### Scenario: WhitespaceCollapsing
- **GIVEN** Code with multiple spaces or newlines
- **WHEN** normalizeCode is called
- **THEN** Whitespace is collapsed to single spaces

### Requirement: Normalizeidentifiers

The system SHALL normalizes identifiers in code to facilitate structural duplicate detection.

#### Scenario: KeywordPreservation
- **GIVEN** Code with keywords
- **WHEN** normalizeIdentifiers is called
- **THEN** Keywords remain unchanged

#### Scenario: IdentifierReplacement
- **GIVEN** Code with non-keyword identifiers
- **WHEN** normalizeIdentifiers is called
- **THEN** Identifiers are replaced with sequential placeholders

### Requirement: Computeshingles

The system SHALL computes shingles from tokenized code for near-duplicate detection.

#### Scenario: ShingleGeneration
- **GIVEN** A list of tokens and shingle size
- **WHEN** computeShingles is called
- **THEN** A set of shingles is generated

### Requirement: Computejaccardsimilarity

The system SHALL computes jaccard similarity between two sets of shingles.

#### Scenario: EmptySets
- **GIVEN** Two empty sets
- **WHEN** computeJaccardSimilarity is called
- **THEN** A similarity of 1 is returned

#### Scenario: NonEmptySets
- **GIVEN** Two non-empty sets with some common elements
- **WHEN** computeJaccardSimilarity is called
- **THEN** The Jaccard similarity is computed and returned

### Requirement: Convertbyteoffsettoline

The system SHALL converts a byte offset to a line number in source code.

#### Scenario: ByteOffsetConversion
- **GIVEN** Source code and a byte offset
- **WHEN** convertByteOffsetToLine is called
- **THEN** The corresponding line number is returned

### Requirement: Infertypesfromsource

The system SHALL infers variable types from source code based on the specified programming language.

#### Scenario: PythonTypeInference
- **GIVEN** Python source code with variable declarations
- **WHEN** inferTypesFromSource is called with language 'Python'
- **THEN** Returns a map of variable names to inferred class names

#### Scenario: C++TypeInference
- **GIVEN** C++ source code with variable declarations
- **WHEN** inferTypesFromSource is called with language 'C++'
- **THEN** Returns a map of variable names to inferred class names

#### Scenario: TypescriptTypeInference
- **GIVEN** TypeScript source code with variable declarations
- **WHEN** inferTypesFromSource is called with language 'TypeScript'
- **THEN** Returns a map of variable names to inferred class names

#### Scenario: GoTypeInference
- **GIVEN** Go source code with variable declarations
- **WHEN** inferTypesFromSource is called with language 'Go'
- **THEN** Returns a map of variable names to inferred class names

#### Scenario: RustTypeInference
- **GIVEN** Rust source code with variable declarations
- **WHEN** inferTypesFromSource is called with language 'Rust'
- **THEN** Returns a map of variable names to inferred class names

#### Scenario: JavaTypeInference
- **GIVEN** Java source code with variable declarations
- **WHEN** inferTypesFromSource is called with language 'Java'
- **THEN** Returns a map of variable names to inferred class names

#### Scenario: RubyTypeInference
- **GIVEN** Ruby source code with variable declarations
- **WHEN** inferTypesFromSource is called with language 'Ruby'
- **THEN** Returns a map of variable names to inferred class names

### Requirement: Resolveviatypeinference

The system SHALL resolves a method call to a function node using inferred types.

#### Scenario: SuccessfulResolution
- **GIVEN** A variable name and method name with inferred types
- **WHEN** resolveViaTypeInference is called with valid inputs
- **THEN** Returns the corresponding FunctionNode

#### Scenario: UnsuccessfulResolution
- **GIVEN** A variable name that has no inferred type
- **WHEN** resolveViaTypeInference is called with valid inputs
- **THEN** Returns undefined

### Requirement: Calculatenamescore

The system SHALL calculates a score for a file based on its name, considering high-value and negative name patterns.

#### Scenario: HighValueName
- **GIVEN** A file named 'auth-service.ts'
- **WHEN** calculateNameScore is called
- **THEN** The score should be 25

#### Scenario: NegativeName
- **GIVEN** A file named 'test-utils.ts'
- **WHEN** calculateNameScore is called
- **THEN** The score should be -10

### Requirement: Calculatepathscore

The system SHALL calculates a score for a file based on its path location, considering high-value paths and depth penalties.

#### Scenario: HighValuePath
- **GIVEN** A file located in 'src/core/auth-service.ts'
- **WHEN** calculatePathScore is called
- **THEN** The score should be 15

#### Scenario: DeeplyNestedFile
- **GIVEN** A file located in 'src/utils/deep/nested/file.ts' with depth 5
- **WHEN** calculatePathScore is called
- **THEN** The score should be reduced by 10

### Requirement: Calculatestructurescore

The system SHALL calculates a score for a file based on its structure, considering class definitions, interface definitions, export statements, decorators, and import statements.

#### Scenario: FileWithClassDefinitions
- **GIVEN** A file containing class definitions
- **WHEN** calculateStructureScore is called
- **THEN** The score should be increased by 10

#### Scenario: FileWithManyImports
- **GIVEN** A file with more than 10 import statements
- **WHEN** calculateStructureScore is called
- **THEN** The score should be increased by 10

### Requirement: Buildfilerelationships

The system SHALL builds a map of file relationships based on import analysis.

#### Scenario: BuildRelationships
- **GIVEN** An array of file metadata
- **WHEN** buildFileRelationships is called
- **THEN** A map of file relationships should be created

### Requirement: Calculateconnectivityscore

The system SHALL calculates a score for a file based on its connectivity, considering the number of files that import it and the number of files it imports.

#### Scenario: HighConnectivity
- **GIVEN** A file imported by 5 other files and importing 5 other files
- **WHEN** calculateConnectivityScore is called
- **THEN** The score should be 30

#### Scenario: OrphanFile
- **GIVEN** A file with no imports or importedBy relationships
- **WHEN** calculateConnectivityScore is called
- **THEN** The score should be reduced by 10

### Requirement: Normalizeurl

The system SHALL normalizes a url or path to a canonical form for comparison.

#### Scenario: NormalizeUrlWithProtocolAndHost
- **GIVEN** A URL with protocol and host (e.g., 'https://example.com/api/v1/users')
- **WHEN** normalizeUrl is called
- **THEN** The protocol and host are removed (e.g., '/api/v1/users')

#### Scenario: NormalizeUrlWithPathParameters
- **GIVEN** A URL with path parameters (e.g., '/users/{id}')
- **WHEN** normalizeUrl is called
- **THEN** Path parameters are replaced with ':param' (e.g., '/users/:param')

#### Scenario: NormalizeUrlWithQueryStringAndFragment
- **GIVEN** A URL with query string and fragment (e.g., '/users?id=123#section')
- **WHEN** normalizeUrl is called
- **THEN** Query string and fragment are removed (e.g., '/users')

#### Scenario: NormalizeUrlWithDuplicateSlashes
- **GIVEN** A URL with duplicate slashes (e.g., '//users//details')
- **WHEN** normalizeUrl is called
- **THEN** Duplicate slashes are collapsed (e.g., '/users/details')

#### Scenario: NormalizeUrlWithTrailingSlash
- **GIVEN** A URL with trailing slash (e.g., '/users/')
- **WHEN** normalizeUrl is called
- **THEN** Trailing slash is removed unless it is the root (e.g., '/users')

### Requirement: Candidatepaths

The system SHALL generates candidate normalized paths for a given url by stripping known api prefixes.

#### Scenario: GenerateCandidatePathsWithKnownPrefix
- **GIVEN** A normalized URL with a known prefix (e.g., '/api/v1/users')
- **WHEN** candidatePaths is called
- **THEN** The URL is included in candidates, and the prefix is stripped (e.g., ['/api/v1/users', '/users'])

#### Scenario: GenerateCandidatePathsWithoutKnownPrefix
- **GIVEN** A normalized URL without a known prefix (e.g., '/users')
- **WHEN** candidatePaths is called
- **THEN** The URL is included in candidates (e.g., ['/users'])

### Requirement: Extracthttpcalls

The system SHALL extracts http calls from frontend files using various http clients (fetch, axios, ky, got).

#### Scenario: ValidJavascriptFile
- **GIVEN** A valid JavaScript file with HTTP calls
- **WHEN** extractHttpCalls is called
- **THEN** An array of HttpCall objects is returned

#### Scenario: InvalidFilePath
- **GIVEN** An invalid file path
- **WHEN** extractHttpCalls is called
- **THEN** An empty array is returned

#### Scenario: FileWithNoHttpCalls
- **GIVEN** A file with no HTTP calls
- **WHEN** extractHttpCalls is called
- **THEN** An empty array is returned

### Requirement: Start

The system SHALL initializes the file watcher and begins monitoring for changes.

#### Scenario: SuccessfulStart
- **GIVEN** Valid project root path
- **WHEN** start is called
- **THEN** File watcher is initialized and ready to monitor changes

#### Scenario: ErrorDuringStart
- **GIVEN** Invalid project root path
- **WHEN** start is called
- **THEN** Error is caught and logged

### Requirement: Stop

The system SHALL stops the file watcher and cleans up resources.

#### Scenario: SuccessfulStop
- **GIVEN** File watcher is running
- **WHEN** stop is called
- **THEN** File watcher is stopped and resources are cleaned up

### Requirement: Handlechange

The system SHALL re-indexes a single changed file, updating signatures and optionally vector embeddings.

#### Scenario: SuccessfulReIndex
- **GIVEN** Valid file path and content
- **WHEN** handleChange is called
- **THEN** Signatures are updated and vector embeddings are re-embedded if applicable

#### Scenario: FileNotFound
- **GIVEN** Invalid file path
- **WHEN** handleChange is called
- **THEN** Error is caught and logged

#### Scenario: UnsupportedLanguage
- **GIVEN** File with unsupported language
- **WHEN** handleChange is called
- **THEN** File is skipped

### Requirement: Getparserforlanguage

The system SHALL retrieves or initializes a tree-sitter parser for a given programming language.

#### Scenario: SupportedLanguage
- **GIVEN** A supported programming language
- **WHEN** getParserForLanguage is called
- **THEN** Returns a configured tree-sitter parser for that language

#### Scenario: UnsupportedLanguage
- **GIVEN** An unsupported programming language
- **WHEN** getParserForLanguage is called
- **THEN** Returns null

### Requirement: Blanklinechunk

The system SHALL splits content into chunks based on blank lines as a fallback mechanism.

#### Scenario: ContentSmallerThanMaxchars
- **GIVEN** Content length is less than maxChars
- **WHEN** blankLineChunk is called
- **THEN** Returns an array containing the original content as a single chunk

#### Scenario: ContentLargerThanMaxchars
- **GIVEN** Content length is greater than maxChars
- **WHEN** blankLineChunk is called
- **THEN** Returns an array of chunks split by blank lines with specified overlap

### Requirement: Astchunkcontent

The system SHALL splits content into chunks based on ast declaration boundaries using tree-sitter, with fallback to blank-line chunking.

#### Scenario: ContentSmallerThanMaxchars
- **GIVEN** Content length is less than maxChars
- **WHEN** astChunkContent is called
- **THEN** Returns an array containing the original content as a single chunk

#### Scenario: SupportedLanguageWithValidAst
- **GIVEN** A supported programming language and valid AST
- **WHEN** astChunkContent is called
- **THEN** Returns an array of chunks split by AST declaration boundaries with header prepended to non-first
chunks

#### Scenario: UnsupportedLanguageOrParsingFailure
- **GIVEN** An unsupported programming language or parsing failure
- **WHEN** astChunkContent is called
- **THEN** Returns an array of chunks split by blank lines with specified overlap

### Requirement: Generatedigest

The system SHALL creates a comprehensive analysis of the codebase structure and dependencies.

#### Scenario: SuccessfulDigestGeneration
- **GIVEN** Valid project root path and output directory
- **WHEN** generateDigest is called
- **THEN** A codebase digest is generated and saved to the output directory

#### Scenario: InvalidProjectRootPath
- **GIVEN** Non-existent project root path
- **WHEN** generateDigest is called
- **THEN** An error is thrown indicating the invalid path

### Requirement: Rel

The system SHALL converts an absolute path to a relative path based on the project root.

#### Scenario: PathIsRelativeToRoot
- **GIVEN** Absolute path that starts with the root path
- **WHEN** rel is called
- **THEN** The relative path is returned without the root path prefix

#### Scenario: PathIsNotRelativeToRoot
- **GIVEN** Absolute path that does not start with the root path
- **WHEN** rel is called
- **THEN** The original absolute path is returned

### Requirement: Filecoupling

The system SHALL extracts file coupling metrics from a dependency graph.

#### Scenario: ValidDependencyGraph
- **GIVEN** A non-null dependency graph with nodes
- **WHEN** fileCoupling is called
- **THEN** An array of file coupling metrics is returned, sorted by importedBy in descending order

#### Scenario: NullDependencyGraph
- **GIVEN** A null dependency graph
- **WHEN** fileCoupling is called
- **THEN** An empty array is returned

### Requirement: Buildadjacency

The system SHALL constructs forward and backward adjacency maps from a serialized call graph.

#### Scenario: ValidCallGraph
- **GIVEN** A valid serialized call graph
- **WHEN** buildAdjacency is called
- **THEN** Forward and backward adjacency maps are constructed

### Requirement: Bfs

The system SHALL performs a breadth-first search on the adjacency map starting from seed nodes.

#### Scenario: ValidSeedsAndAdjacency
- **GIVEN** Valid seed nodes and adjacency map
- **WHEN** bfs is called
- **THEN** Visited nodes within max depth are returned

### Requirement: Computeriskscore

The system SHALL computes a risk score for a function node based on blast radius and hub status.

#### Scenario: ValidFunctionNode
- **GIVEN** A valid function node
- **WHEN** computeRiskScore is called
- **THEN** A risk score between 0 and 100 is returned

### Requirement: Recommendstrategy

The system SHALL recommends a refactoring strategy based on risk score, fan-in, and fan-out.

#### Scenario: ValidInputs
- **GIVEN** Valid risk score, fan-in, fan-out, and hub status
- **WHEN** recommendStrategy is called
- **THEN** A refactoring strategy is recommended

### Requirement: Nodetosummary

The system SHALL converts a function node to a summary object.

#### Scenario: ValidFunctionNode
- **GIVEN** A valid function node
- **WHEN** nodeToSummary is called
- **THEN** A summary object is returned

### Requirement: Handlegetcallgraph

The system SHALL retrieves the call graph summary from cached analysis for a given directory.

#### Scenario: ValidDirectory
- **GIVEN** A valid directory with cached analysis
- **WHEN** handleGetCallGraph is called
- **THEN** The call graph summary is returned

### Requirement: Handlegetsubgraph

The system SHALL extracts a depth-limited subgraph centered on a named function.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, function name, direction, max depth, and format
- **WHEN** handleGetSubgraph is called
- **THEN** A depth-limited subgraph is returned

### Requirement: Handleanalyzeimpact

The system SHALL performs a deep impact analysis for a single symbol in the call graph.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, symbol, and depth
- **WHEN** handleAnalyzeImpact is called
- **THEN** An impact analysis is returned

### Requirement: Handlegetlowriskrefactorcandidates

The system SHALL retrieves the safest functions to refactor based on risk score.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, limit, and file pattern
- **WHEN** handleGetLowRiskRefactorCandidates is called
- **THEN** Low-risk refactoring candidates are returned

### Requirement: Handlegetleaffunctions

The system SHALL retrieves leaf functions (functions with no outgoing calls).

#### Scenario: ValidInputs
- **GIVEN** Valid directory, limit, file pattern, and sort by
- **WHEN** handleGetLeafFunctions is called
- **THEN** Leaf functions are returned

### Requirement: Handlegetcriticalhubs

The system SHALL retrieves critical hub functions ranked by composite criticality.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, limit, and minimum fan-in
- **WHEN** handleGetCriticalHubs is called
- **THEN** Critical hub functions are returned

### Requirement: Handlegetgodfunctions

The system SHALL detects god functions (functions with high fan-out) and returns their call-graph neighborhood.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, file path, and fan-out threshold
- **WHEN** handleGetGodFunctions is called
- **THEN** God functions and their neighborhoods are returned

### Requirement: Handlegetfiledependencies

The system SHALL retrieves the file-level import dependencies for a given file.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, file path, and direction
- **WHEN** handleGetFileDependencies is called
- **THEN** File-level import dependencies are returned

### Requirement: Handletraceexecutionpath

The system SHALL finds all execution paths between two functions in the call graph.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, entry function, target function, max depth, and max paths
- **WHEN** handleTraceExecutionPath is called
- **THEN** Execution paths between the functions are returned


## Sub-components

> `GraphService` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Validatedirectory

> Implements: `validateDirectory`

Resolve and validate a user-supplied directory path.

#### Requirement: Validatepath

The system SHALL the function shall validate the provided directory path and return the resolved absolute path.

#### Scenario: Validdirectory
- **GIVEN** a valid directory path
- **WHEN** the function is called
- **THEN** it SHALL return the resolved absolute path

### Sub-component: Readcachedcontext

> Implements: `readCachedContext`

Read cached context data for a given directory.

#### Requirement: Readcontext

The system SHALL the function shall read and return the cached llm context for the specified directory, or null if no context exists.

#### Scenario: Contextexists
- **GIVEN** a directory with cached context
- **WHEN** the function is called
- **THEN** it SHALL return the cached LLMContext

### Sub-component: Buildadjacency

> Implements: `buildAdjacency`

Build forward and backward adjacency maps from a serialized call graph.

#### Requirement: Buildmaps

The system SHALL the function shall build and return forward (caller→callees) and backward (callee→callers) adjacency maps from the provided call graph.

#### Scenario: Validcallgraph
- **GIVEN** a valid serialized call graph
- **WHEN** the function is called
- **THEN** it SHALL return the forward and backward adjacency maps

### Sub-component: Readopenloreconfig

> Implements: `readOpenLoreConfig`

Read openlore configuration from .openlore/config.json.

#### Requirement: Readconfig

The system SHALL the function shall read and return the openlore configuration from the specified root path, or null if the configuration does not exist.

#### Scenario: Configexists
- **GIVEN** a root path with a valid .openlore/config.json
- **WHEN** the function is called
- **THEN** it SHALL return the OpenLoreConfig

### Sub-component: Bfs

> Implements: `bfs`

Perform a breadth-first search on the adjacency map starting from the given seeds.

#### Requirement: Performbfs

The system SHALL the function shall perform a breadth-first search on the adjacency map starting from the given seeds and return the visited nodes up to the specified maximum depth.

#### Scenario: Validinputs
- **GIVEN** valid seeds, adjacency map, and maxDepth
- **WHEN** the function is called
- **THEN** it SHALL return the visited nodes up to the specified depth

### Sub-component: Nodetosummary

> Implements: `nodeToSummary`

Convert a function node to a summary string.

#### Requirement: Convertnode

The system SHALL the function shall convert the provided function node to a summary string, or return an empty string if the node is undefined.

#### Scenario: Validnode
- **GIVEN** a valid FunctionNode
- **WHEN** the function is called
- **THEN** it SHALL return the summary string

### Sub-component: Computeriskscore

> Implements: `computeRiskScore`

Compute a risk score [0–100] for a node based on its properties.

#### Requirement: Calculaterisk

The system SHALL the function shall compute and return a risk score [0–100] for the provided node based on its blast radius, fan-in, fan-out, and hub status.

#### Scenario: Validnode
- **GIVEN** a valid FunctionNode with blastRadius and isHub
- **WHEN** the function is called
- **THEN** it SHALL return the computed risk score

### Sub-component: Recommendstrategy

> Implements: `recommendStrategy`

Recommend a refactoring strategy based on the node's risk score and properties.

#### Requirement: Recommend

The system SHALL the function shall recommend a refactoring strategy based on the provided risk score, fan-in, fan-out, and hub status.

#### Scenario: Validinputs
- **GIVEN** valid riskScore, fanIn, fanOut, and isHub
- **WHEN** the function is called
- **THEN** it SHALL return the recommended refactoring strategy
### Requirement: IacResourcesProjectOntoTheExistingCallgraphPrimitives

The system SHALL model infrastructure-as-code resources as nodes and edges in the existing call graph rather than a separate schema.

> Decision recorded: 8e1f10e6
> Date: 2026-06-01
### Requirement: EdgestoreUsesSchemaversionRebuildonbumpInsteadOfMigrations

The system SHALL invalidate and rebuild the EdgeStore cache when its schema version changes, rather than applying incremental migrations.

> Decision recorded: 81ba22ca
> Date: 2026-06-01
### Requirement: Bm25KeywordRetrievalIsTheZeronetworkFloorEmbeddingsAreOptional

The system SHALL provide BM25 keyword retrieval as the default search index, requiring no network access or API keys, with embedding-based retrieval as an optional enhancement.

> Decision recorded: 06a6e545
> Date: 2026-06-01
### Requirement: DecisionsProjectedAsFirstclassGraphNodesWithDedicatedSqliteTables

The system SHALL project active architectural decisions from the JSON decision store into the code graph as first-class nodes with `affects` edges, stored in dedicated SQLite tables that do not interfere with code-node statistics or call-edge traversal.

> Decision recorded: bf450b1c
> Date: 2026-06-02
### Requirement: CrossdomainCodeinfraEdgesViaLinecontainmentLinkage

The system SHALL emit a `references` edge from the narrowest enclosing code function to each embedded IaC resource declared within the same file, enabling end-to-end code↔infra graph traversal.

> Decision recorded: 708a8436
> Date: 2026-06-02
### Requirement: LocalGitgithubProvenanceStoredAsPerfileRowsInTheEdgestoreSqliteDb

The system SHALL store per-file git provenance (last author, recent authors, pull requests) in the edge-store SQLite database, extracted from local git history during analysis, and SHALL treat extraction failures as non-blocking.

> Decision recorded: f8778883
> Date: 2026-06-02
### Requirement: EdgestoreExposesWasresetFlagAfterSchemaMigrationWipe

The system SHALL expose whether the edge store was reset due to a schema version bump so that read-only callers can detect stale data and prompt for re-analysis.

> Decision recorded: 8365f6f8
> Date: 2026-06-02
### Requirement: ComputeCfgdefuseOverlayInsideLivetreeExtractorsExtendReturnContractToNodesRawedgesCfg

The system SHALL compute intraprocedural control-flow graphs and reaching-definition def-use edges inside language extractors while the parse tree is live, storing the overlay in the database only.

> Decision recorded: c8f2b9bf
> Date: 2026-06-12

### Requirement: ContentAddressedStableSymbolId

The system SHALL compute, for every named function and class symbol, a content-addressed stable identity (`stableId`) that is independent of the symbol's file path, derived deterministically from the symbol's qualified name (class.method or function) and a normalized parameter-list signature shape (empty parentheses when no signature was captured). The shape SHALL exclude the function body and the return type, so the `stableId` is invariant to body edits and to leading-modifier/return-type changes. The `stableId` SHALL be a pure function of the symbol's own static structure — no file path and no position-dependent discriminator — so it is identical across runs, machines, and full-vs-incremental builds, and is preserved when a symbol's file is renamed or moved. Symbols whose identity cannot be derived deterministically (anonymous or dynamically-generated) SHALL NOT receive a `stableId`. Two distinct symbols sharing a qualified name and parameter shape (homonyms) MAY share a `stableId`; the system SHALL resolve a `stableId` to a symbol only when it identifies a unique symbol, and SHALL fall back rather than guess when it is ambiguous. Full scenarios: `openspec/changes/add-content-addressed-stable-symbol-ids/specs/analyzer/spec.md`.

> Known container-qualification limitations. These are pre-existing analyzer container-qualification gaps that `stableId` inherits; none causes a *wrong* resolution (a `stableId` is never resolved to a symbol that is not the one bearing it), but they fall into two regimes with different completeness costs:
>
> 1. **Cross-file homonym (safe fallback).** Two distinct symbols in *different* files that share a qualified name + parameter shape (e.g. C++ cross-namespace same-name functions, or any two same-shape functions the language does not further qualify) each keep their own node; both carry the same `stableId`; resolution is ambiguous and `getNodeByStableId` returns `null` — the system falls back rather than guessing. No symbol is lost.
> 2. **Same-file path-id collapse (completeness gap, not a wrong resolution).** When two distinct symbols in the *same* file collide on the path-based node `id` `file::Class.name`, the analyzer keeps only the last one (last-write-wins at node aggregation) **before** `stableId` is computed, so the other symbol is absent from the entire graph — invisible to the path-based `id` and to `stableId` alike. Instances: TypeScript/C#/PHP `namespace`/`module` reuse and module-level functions (the namespace is not part of the qualified name), same-file nested-class name reuse in any language (only the innermost enclosing class is the container, so same-file `A.Inner.m` and `B.Inner.m` collapse to one `Inner.m`), and C++ overloaded definitions (one overload is dropped). The sole survivor resolves uniquely and correctly to itself; the dropped twin is simply not represented anywhere (and so a same-file deletion of the collapsed twin is not surfaced by `structural_diff`). This is a completeness limitation of the analyzer's container qualification, which `stableId` inherits — not one it introduces, and not a mis-resolution.
>
> Go method receivers and Rust `impl Trait for Struct` / generic `impl` blocks ARE correctly attributed to the implementing type. The `stableId` derivation itself is correct wherever the container is.
>
> Parameter group is located by the symbol's own name (`name(...)`), not the first `(` in the captured signature. This is required for genuine body-invariance in languages whose captured signature includes the body of a *paren-less* definition — Ruby (`def total; compute(5); end`), Scala (`def total = compute(5)`), and paren-less arrows (`const f = a => g(a)`): the body call `compute(5)` must NOT be mistaken for the parameter list, or a body edit would flip the `stableId`. A Go method receiver (`func (recv) Name(p)`, the receiver `(` is preceded by `func`, not the method name) and an arg-bearing decorator preceding the def are likewise skipped; an assigned lambda (`= (a) => …`) is recognized as the parameter list.
>
> Signature-shape sensitivity: the normalized shape is the source text of the parameter group with whitespace collapsed; it does not strip comments physically inside the parameter list nor canonicalize default-value expressions, so editing an in-parameter comment or a brace-delimited default value changes the `stableId`. This only weakens body/edit-invariance in those narrow cases and always fails *safe* — the symbol falls back to remove+add (diff) or `orphaned` (anchor), never to a wrong identity. A literal-aware comment stripper was deliberately not added (a naive strip would corrupt string-literal defaults such as URLs, *creating* collisions).

### Requirement: AdditiveStableIdentity

The system SHALL introduce `stableId` additively, without changing the existing path-based symbol `id`, which SHALL remain the canonical key for the call graph, the store primary keys, edge endpoints, the file-from-id derivation, the serialized graph, and all existing path-based consumers. `stableId` SHALL be persisted as a nullable serialized-node field and a nullable indexed store column, introduced by bumping the store schema version so existing caches drop-and-rebuild with no data-migration code. A graph or store that predates `stableId` SHALL remain valid, with an absent `stableId` treated as "no stable identity available".

### Requirement: RenameStableStructuralDiff

The system SHALL use `stableId` as the primary cross-version node matcher in structural diffing, matching two nodes as the same symbol when their `stableId`s are equal and falling back to the path-based `id` only for nodes that have no `stableId`. A purely moved or renamed-file symbol (same `stableId`) SHALL be reported as the same symbol (an exact move/rename candidate), not a removal plus an addition. The system SHALL retain the signature-shape rename-recovery heuristic for the leftover removed/added set (identifier renames and symbols with no `stableId`), and SHALL continue to surface both interpretations rather than silently merging them.

### Requirement: RenameStableMemoryAnchoring

The system SHALL record an optional `stableId` on a code-anchored memory alongside its path-based `nodeId`, and SHALL resolve a memory's anchor by first attempting the `nodeId` (current behavior) and, only on a miss, resolving by `stableId`. A memory anchored to a symbol later moved/renamed-file but otherwise unchanged SHALL resolve via `stableId` to the relocated symbol and report `fresh` (or `drifted` if its content changed) instead of `orphaned`. A memory recorded before this change (carrying only `nodeId`) SHALL behave exactly as today, with no migration.
### Requirement: AnchorPersistedMemoryToCallgraphSymbolsWithDeterministicFreshness

The system SHALL anchor persisted memories to call-graph symbols and compute deterministic fresh/drifted/orphaned verdicts on recall without LLM inference.

> Decision recorded: 34b178df
> Date: 2026-06-16
### Requirement: CodeanchoredMemoryToolsRememberrecallAreOptinViaADedicatedPreset

The system SHALL expose remember and recall tools only through the opt-in memory preset, not in the default or minimal tool surfaces.

> Decision recorded: b92dada4
> Date: 2026-06-16
### Requirement: SynthesizeDynamicdispatchCallEdgesWithExplicitProvenance

The system SHALL synthesize dynamic-dispatch call edges (event channels, route handlers) deterministically from the AST and tag each with explicit provenance so they are distinguishable from directly-resolved edges.

> Decision recorded: b7395127
> Date: 2026-06-16
### Requirement: ExposeValuelevelPrecisionAsOptinValuelevelvalueparamFlagsOnExistingImpactTools

The system SHALL expose value-level precision in analyze_impact and trace_execution_path via opt-in valueLevel and valueParam parameters, falling back to function granularity when no CFG overlay exists.

> Decision recorded: 4cf203d7
> Date: 2026-06-16
### Requirement: ModelTrycatchSwitchAndClosuresAsBranchingalternativepathStructuresInTheCfg

The system SHALL model try/catch, switch, and closure captures as branching/alternative-path CFG structures so that reaching-definitions never spuriously kills a definition visible along an alternative control-flow path.

> Decision recorded: 3db9424d
> Date: 2026-06-16
### Requirement: DowngradeEscapedClosuremutatedAddresstakenVariablesToMaySoExactIsNeverUnsound

The system SHALL downgrade reaching-definition precision to may for any variable that is assigned inside a nested closure or whose address is taken, ensuring the exact label is never applied unsoundly.

> Decision recorded: 8192f32f
> Date: 2026-06-16
### Requirement: AddLexicalScopeResolutionToTheDefuseOverlaySoShadowedVariablesNeverConflate

The system SHALL resolve lexical scopes so that identically-named variables in different scopes are never conflated in the reaching-definitions overlay.

> Decision recorded: 84cc2af4
> Date: 2026-06-16
### Requirement: CompareTreesitterAstNodesByPositionNotObjectIdentityFailsoftOnPathologicalCfgsFixMultilineValueflowChainingAndGlobalnonlocalEscape

The system SHALL compare tree-sitter AST nodes by source position (startIndex, endIndex), never by object identity, and SHALL fail soft with no overlay when a function exceeds 4000 CFG blocks or 128 reaching-defs sweeps.

> Decision recorded: a95c5c98
> Date: 2026-06-16
### Requirement: ExtendCfgOverlayToCCPhpDivergentcontrolflowLanguagesRemainFailsoft

The system SHALL build intraprocedural CFG overlays for C, C#, and PHP using their respective language specs, and SHALL fail soft with no overlay for languages lacking a CFG spec.

> Decision recorded: 04831b41
> Date: 2026-06-16
### Requirement: V1CfgOverlayCovers11FieldmappableLanguagesDivergentgrammarLanguagesStayFailsoft

The system SHALL limit the v1 CFG data-flow overlay to the 11 languages whose tree-sitter grammars expose field-named control-flow children, and SHALL fail soft for all other languages rather than risk unsound edges.

> Decision recorded: 27680bb9
> Date: 2026-06-16
### Requirement: ValuelevelImpacttraceFallsBackToFunctionGranularityOnIllposedQueriesInsteadOfReportingZero

The system SHALL fall back to function-granularity impact when a value-level query target cannot be resolved in the overlay, reporting applied:false with a reason rather than an empty narrowed result.

> Decision recorded: a37d851f
> Date: 2026-06-16
### Requirement: CfgOverlayBuildAndValuelevelConsumersAreExceptionisolatedSilentfailRouting

The system SHALL isolate CFG overlay build and read failures so they degrade to function-granularity results rather than dropping base call-graph data or failing MCP tools.

> Decision recorded: fda9fc24
> Date: 2026-06-16
### Requirement: InfiniteLoopsDropThePhantomSkipbodyEdgeInTheCfgNoUnsoundExact

The system SHALL model infinite loops (loop {}, for(;;), for {}) without a skip-body edge so that a pre-loop definition is never labeled exact when the loop body unconditionally reassigns it.

> Decision recorded: 7b1d823d
> Date: 2026-06-16
### Requirement: RustImplExtractionKeysMethodsOnImplementingTypeNotTrait

The system SHALL attribute Rust impl methods to the implementing type (not the trait) and strip generic parameters from the type name.

> Decision recorded: 2ec9dd45
> Date: 2026-06-16
### Requirement: PersonalizedPagerankAsQueryconditionedRetrievalRankingNotGlobalSalience

The system SHALL support an opt-in personalized-PageRank ranking mode for query-conditioned retrieval in orient and get_minimal_context, seeded by task-matched symbols rather than global salience.

> Decision recorded: 0bdd4319
> Date: 2026-06-16

### Requirement: EpistemicLeaseEmitsNeutralFreshnessFactsNotCoerciveImperatives

The system SHALL surface epistemic-lease freshness as neutral factual signals (elapsed time, cognitive load, index-behind-HEAD) rather than imperative commands directed at the consuming agent.

> Decision recorded: 8e95746d
> Date: 2026-06-16
### Requirement: UnifyDomainnameDerivationBehindASingleLeaffirstHelper

The system SHALL derive domain names using a leaf-first path walk with shared noise-directory filtering, ensuring consistent domain naming across dependency-graph clustering and repository mapping.

> Decision recorded: 201ace19
> Date: 2026-06-16
### Requirement: AddJpahibernateEntityParserToSchemaExtractor

The system SHALL extract schema models from Java files annotated with JPA @Entity or @MappedSuperclass, parsing table names, field names, types, and nullable status.

> Decision recorded: 6af4d045
> Date: 2026-06-16
### Requirement: GeneralizeFileextensionStrippingAndJavaMethodSignatureExtractionForMultilanguageSupport

The system SHALL extract Java method signatures including modifiers, generic type parameters, and throws clauses, while rejecting block-comment contents and body-level statements as false positives.

> Decision recorded: 8f94535c
> Date: 2026-06-16
### Requirement: InjectCallgraphEdgesIntoTheDependencyGraphForJvmLanguages

The system SHALL inject call-graph edges into the file-level dependency graph for JVM languages whose same-package references produce no import statements.

> Decision recorded: 67580817
> Date: 2026-06-17
### Requirement: JaxrsRouteDetectionRequiresJavaxjakartawsrsImportToAvoidFalsePositivesFromHttpClientLibraries

The system SHALL only classify a Java/Kotlin file as a JAX-RS server endpoint when it imports from the javax.ws.rs or jakarta.ws.rs package, to prevent false positives from HTTP client annotation libraries.

> Decision recorded: f9de2e30
> Date: 2026-06-17

### Requirement: SynthesizedDynamicDispatchEdges

The system SHALL augment the directly-resolved call graph with a deterministic synthesis pass that
adds call edges for dynamic-dispatch patterns that direct name resolution cannot recover, deriving
each edge from statically-paired sites in the AST. The pass SHALL NOT use an LLM, SHALL run after
direct resolution, and SHALL only add edges — it SHALL NOT modify or remove any directly-resolved
edge. At minimum the pass SHALL recover:

- **Event channels** — an edge from a dispatch site (`emit(k)` / `dispatch(k)`) to every handler
  registered on the same channel key `k` (`on(k, fn)` / `addEventListener(k, fn)`), when the key is a
  static key (string literal, substitution-free template, or constant member reference) shared by
  both sites. Event-channel recovery is **per-language**: it applies to each language whose
  registration and dispatch sites are both statically visible, starting with JavaScript/TypeScript
  and extending to further languages one at a time (see `MultiLanguageEventChannelSynthesis`).
- **Route → handler** — an edge from a route node already detected by route inventory to the handler
  function the route binds.

The pass SHALL be structured as independent per-pattern rules so that each rule is testable in
isolation and adding a rule does not alter the output of existing rules.

#### Scenario: Event handler is reachable through a synthesized edge

- **GIVEN** a handler function registered with `on('mount', handler)` and a separate site calling
  `emit('mount')`, with no direct call to `handler`
- **WHEN** the call graph is built
- **THEN** a synthesized edge exists from the `emit('mount')` site's enclosing function to `handler`

#### Scenario: Route is wired to its handler

- **GIVEN** a route that route inventory detects and the handler function it binds
- **WHEN** the call graph is built
- **THEN** a synthesized `calls`-kind edge exists from the route node to the handler function

#### Scenario: Mismatched channel keys produce no edge

- **GIVEN** a handler registered with `on('open', handler)` and a dispatch site calling `emit('close')`
- **WHEN** the call graph is built
- **THEN** no synthesized edge is created between the dispatch site and `handler`

#### Scenario: Direct edges are unchanged by synthesis

- **GIVEN** a call graph built with the synthesis pass enabled and the same graph built with it disabled
- **WHEN** the two graphs are compared
- **THEN** every directly-resolved edge is identical in both, and the synthesis-enabled graph differs
  only by added edges

### Requirement: MultiLanguageEventChannelSynthesis

The system SHALL recover event-channel edges across the languages it parses, not only
JavaScript/TypeScript, applying the same high-precision discipline per language: an edge is emitted
only when a registration site (`on`/`once`/`addListener`/`subscribe`/… with a static key and a
resolvable handler) and a dispatch site (`emit`/`dispatch`/`publish`/… on the same static key) are
both statically visible in that language. Each language's collector resolves the channel key and the
handler from that language's own AST; the pairing, fan-out cap, and provenance labeling are shared
and language-agnostic. Adding a language SHALL NOT change the edges synthesized for any other
language, and a language whose idioms are not statically pairable SHALL emit no edges rather than
guess.

Languages are added one at a time. The set in effect is JavaScript/TypeScript, Python, Ruby, PHP, and
Swift (NotificationCenter `addObserver(forName:)` ↔ `post(name:)`); the handler may be a function
reference, a member/attribute reference (`self.handler`), a bound reference, a Ruby block, a PHP
callable (`'fn'` / `[$this, 'm']`), or an inline function/lambda/closure (wired to the internal
functions its body calls). Keys are namespaced by kind (string / symbol / constant) so a key of one
kind never pairs with a same-text key of another.

#### Scenario: Python event handler is reachable through a synthesized edge

- **GIVEN** a Python handler registered with `emitter.on('mount', handler)` and a separate site
  calling `emitter.emit('mount')`, with no direct call to `handler`
- **WHEN** the call graph is built
- **THEN** a synthesized event-channel edge exists from the `emit('mount')` site's enclosing function
  to `handler`

#### Scenario: Python dispatch with a mismatched key produces no edge

- **GIVEN** a Python handler registered on `'open'` and a dispatch site on `'close'`
- **WHEN** the call graph is built
- **THEN** no synthesized edge is created between them

#### Scenario: Adding a language leaves other languages' edges unchanged

- **GIVEN** a project mixing JavaScript/TypeScript and Python event channels
- **WHEN** the call graph is built
- **THEN** the JavaScript/TypeScript synthesized edges are identical to those produced when no Python
  source is present, and the Python edges are added independently

### Requirement: TypeBasedEventSynthesis

The system SHALL recover **type-based** event edges in languages whose event systems key on an event
**type** rather than a string channel: a handler is registered by an annotation or a typed interface,
and a dispatch carries a constructed event instance. An edge SHALL be emitted from a dispatch site to
a handler when the handler's event type and the dispatched event's constructed type match, derived
statically from the AST with no LLM. The key is the event type name; pairing, the fan-out cap, and
provenance are shared with the string-key rule, but the producing rule is labeled distinctly
(`synthesizedBy: 'type-event'`). Type-based recovery is per-language and added one language at a time;
in effect it covers **Java** and **Kotlin** (Guava `@Subscribe` / Spring `@EventListener` handler methods paired with
`post(new T(...))` / `publishEvent(new T(...))`) and **C#** (a class implementing a handler interface
such as `INotificationHandler<T>` / `IRequestHandler<T>` paired with `Publish(new T(...))` /
`Send(new T(...))`). A dispatch whose argument is not a statically-typed construction SHALL emit no
edge rather than guess.

#### Scenario: Java annotated handler is reachable from its publisher

- **GIVEN** a method annotated `@Subscribe` (or `@EventListener`) whose first parameter type is `T`,
  and a separate site calling `post(new T(...))` (or `publishEvent(new T(...))`)
- **WHEN** the call graph is built
- **THEN** a synthesized `type-event` edge exists from the dispatch site's enclosing method to the
  annotated handler method

#### Scenario: C# typed handler is reachable from its publisher

- **GIVEN** a class implementing `INotificationHandler<T>` whose handler method takes `T`, and a
  separate site calling `Publish(new T(...))` (or `Send(new T(...))`)
- **WHEN** the call graph is built
- **THEN** a synthesized `type-event` edge exists from the dispatch site's enclosing method to the
  handler method

#### Scenario: Mismatched event types produce no edge

- **GIVEN** a handler for type `A` and a dispatch constructing type `B`
- **WHEN** the call graph is built
- **THEN** no synthesized edge is created between them

### Requirement: CallbackRegistrationSynthesis

The system SHALL recover edges for handlers that are **registered as callbacks** with a framework or
runtime that later invokes them, where there is no in-code dispatch site to pair against. When a
**named internal function** is passed as an argument to a **curated registrar** (an API known to
invoke its callback), the system SHALL add an edge from the registration site's enclosing function to
that handler, labeled `synthesizedBy: 'callback-registration'`. Only curated registrars SHALL match,
so a function passed to an unrelated call is never treated as a callback; and inline function/closure
arguments SHALL NOT be matched here (their bodies are already attributed to the enclosing function by
direct resolution, so an edge would be redundant). Recovery is per-language; in effect it covers
**Go** (`net/http` `HandleFunc`/`Handle` and router verbs `GET`/`POST`/… of gin/echo/chi),
**JavaScript/TypeScript** (scheduler registrars `setTimeout`/`setInterval`/`setImmediate`/
`queueMicrotask`/`requestAnimationFrame`/`requestIdleCallback`/`nextTick`), and **C++** (Qt
`connect(sender, &S::sig, receiver, &R::slot)` — the slot member function is wired; the signal, a Qt
declaration with no body, does not resolve and is left alone).

#### Scenario: Go HTTP handler registered by name is reachable

- **GIVEN** a function `handleX` passed to `mux.HandleFunc("/x", handleX)` (or `http.HandleFunc`, or a
  router `GET("/x", handleX)`), with no direct call to `handleX`
- **WHEN** the call graph is built
- **THEN** a synthesized `callback-registration` edge exists from the registration's enclosing
  function to `handleX`

#### Scenario: JS/TS scheduler callback registered by name is reachable

- **GIVEN** a named function `tick` passed to `setTimeout(tick, 1000)` (or `setInterval`), with no
  direct call to `tick`
- **WHEN** the call graph is built
- **THEN** a synthesized `callback-registration` edge exists from the enclosing function to `tick`

#### Scenario: C++ Qt slot is reachable from connect

- **GIVEN** `connect(button, &QPushButton::clicked, this, &MyWidget::onClicked)` where `onClicked` is a
  member function, with no direct call to `onClicked`
- **WHEN** the call graph is built
- **THEN** a synthesized `callback-registration` edge exists from the `connect` site's enclosing
  function to `onClicked` (and the external Qt signal `clicked`, having no internal definition, is
  not wired)

#### Scenario: A function passed to a non-registrar is not treated as a callback

- **GIVEN** a named function passed as an argument to a call whose method is not a curated registrar
- **WHEN** the call graph is built
- **THEN** no `callback-registration` edge is synthesized for it

### Requirement: ActorMessageSynthesis

The system SHALL recover edges for the actor/message-passing model where it is statically pairable to
a named handler: a message dispatch and a message handler keyed on the message tag. In Elixir, a
GenServer dispatch (`GenServer.cast`/`GenServer.call`, or `send` for `handle_info`) carrying a message
whose tag is a leading atom (including the tag of a `{:tag, …}` tuple) SHALL pair with the matching
`handle_cast`/`handle_call`/`handle_info` clause, emitting an edge from the dispatch site's enclosing
function to that clause, labeled `synthesizedBy: 'actor-message'`. The key SHALL be namespaced by
handler kind so a `cast` never pairs with a `handle_call` of the same tag. A dispatch whose message
tag is not a static atom SHALL emit no edge. Actor/channel mechanisms that expose no named handler to
pair — Go channels, and Akka/Scala `receive` blocks — SHALL NOT synthesize edges.

#### Scenario: Elixir GenServer cast reaches its handle_cast clause

- **GIVEN** `def handle_cast({:add, x}, state)` and a separate `GenServer.cast(pid, {:add, 1})`
- **WHEN** the call graph is built
- **THEN** a synthesized `actor-message` edge exists from the cast site's enclosing function to the
  `handle_cast` clause

#### Scenario: cast does not pair with a same-tag handle_call

- **GIVEN** `def handle_call(:fetch, _from, state)` and a `GenServer.cast(pid, :fetch)`
- **WHEN** the call graph is built
- **THEN** no synthesized edge is created between them (cast pairs only with `handle_cast`)

### Requirement: EdgeProvenanceLabeling

The system SHALL label every synthesized edge with a provenance distinct from directly-resolved
edges, by setting its `confidence` to `synthesized` and recording the rule that produced it in an
optional `synthesizedBy` property naming the pattern (for example `event-channel`, `route-handler`,
`callback-arg`). Directly-resolved edges SHALL retain their existing `confidence` value and SHALL NOT
carry `synthesizedBy`. A serialized call graph that predates this property SHALL load unchanged, with
absent `synthesizedBy` treated as a directly-resolved edge.

#### Scenario: Synthesized edge carries provenance

- **GIVEN** a synthesized event-channel edge
- **WHEN** the edge is inspected
- **THEN** its `confidence` is `synthesized` and its `synthesizedBy` names the producing rule

#### Scenario: Directly-resolved edge is distinguishable

- **GIVEN** a directly-resolved import edge
- **WHEN** the edge is inspected
- **THEN** its `confidence` is its resolution method (not `synthesized`) and it has no `synthesizedBy`

#### Scenario: Synthesized edges cost more in call distance

- **GIVEN** two paths from A to B, one entirely directly-resolved and one that traverses a synthesized edge
- **WHEN** call distance is computed for each path
- **THEN** the path traversing the synthesized edge has the greater total cost

### Requirement: HighPrecisionSynthesisBounds

The system SHALL bias edge synthesis toward false-negatives over false-positives: it SHALL emit an
edge only when a registration site and a dispatch site are statically paired on a shared key or
binding, and SHALL NOT fan a dispatch site out to functions it cannot pair. Per-channel handler
fan-out SHALL be capped by a fixed bound (default 8); a channel whose registered-handler count
exceeds the bound SHALL be dropped (no edges emitted for it) rather than partially or speculatively
wired, and the drop SHALL be logged with the channel key and count.

#### Scenario: Unpaired dispatch emits nothing

- **GIVEN** a dispatch site `emit('change')` with no statically-visible registration on `'change'`
- **WHEN** the synthesis pass runs
- **THEN** no synthesized edge is emitted for that dispatch site

#### Scenario: Over-cap channel is dropped, not guessed

- **GIVEN** a channel key with more registered handlers than the fan-out cap
- **WHEN** the synthesis pass runs
- **THEN** no synthesized edges are emitted for that channel and the drop is logged with the key and count

### Requirement: TypeHierarchyResolvedDispatch

The system SHALL augment the call graph with a deterministic Class Hierarchy Analysis (CHA) pass that
resolves polymorphic (virtual) method dispatch through inheritance and interface implementation,
deriving each edge from the class hierarchy already extracted into `ClassNode` / `InheritanceEdge` and
from the AST. The pass SHALL NOT use an LLM, SHALL run after the class hierarchy is built, and SHALL
only add edges — it SHALL NOT modify or remove any directly-resolved edge. For a method call
`recv.m(args)` that direct name resolution did not already pin to a callee, the pass SHALL add a
`calls`-kind edge from the call site's enclosing function to each implementation of a method named
`m` with compatible arity that is reachable in the type subtree of `recv`'s type, where:

- when `recv`'s declared type `T` is statically recoverable (an explicit type annotation, a
  `new T()` initializer, a parameter type, or a result from the existing type-inference engine), the
  set of targets SHALL be restricted to `T` and its subtypes in the hierarchy; and
- when `recv`'s declared type is not statically recoverable, the targets SHALL be every hierarchy
  class that declares a method named `m` with compatible arity.

The pass SHALL be structured as independent per-rule synthesis so each rule is testable in isolation
and adding a rule does not alter the output of existing rules.

#### Scenario: Virtual call resolves to all overrides in the receiver's subtree

- **GIVEN** an abstract type `Shape` with method `area`, subtypes `Circle` and `Square` that each
  override `area`, and a call `shape.area()` where `shape` has declared type `Shape`
- **WHEN** the call graph is built
- **THEN** a `calls`-kind edge exists from the call site's enclosing function to both `Circle.area`
  and `Square.area`

#### Scenario: Declared receiver type narrows the target set

- **GIVEN** the hierarchy above and a call `c.area()` where `c` has declared type `Circle`
- **WHEN** the call graph is built
- **THEN** an edge exists to `Circle.area` and no edge is created to `Square.area`

#### Scenario: Unrelated method names produce no edge

- **GIVEN** a call `shape.area()` and a subtype method `Circle.render` with no `area` declaration on
  `Circle`
- **WHEN** the call graph is built
- **THEN** no edge is created from the call site to `Circle.render`

#### Scenario: Calls on external types do not resolve

- **GIVEN** a call `arr.map(fn)` where `arr` is a built-in array type that is not present in the
  extracted class hierarchy
- **WHEN** the CHA pass runs
- **THEN** no virtual-dispatch edge is synthesized for that call

#### Scenario: Direct edges are unchanged by CHA synthesis

- **GIVEN** a call graph built with the CHA pass enabled and the same graph built with it disabled
- **WHEN** the two graphs are compared
- **THEN** every directly-resolved edge is identical in both, and the CHA-enabled graph differs only
  by added edges

### Requirement: MethodLevelOverrideEdges

The system SHALL materialize a method-level override edge for each base method `B.m` that is
overridden by a derived method `D.m`, where `D` is a subtype of `B` in the class hierarchy and both
`B` and `D` declare a method named `m` with compatible arity. The edge SHALL be directed from the
base method to the overriding method (`B.m → D.m`), SHALL use `kind: 'overrides'`, and SHALL be
provenance-labeled (see `EdgeProvenanceLabeling`). Override edges SHALL be derived from name and arity
matching only — the system SHALL NOT connect a base method to a derived method of a different name or
incompatible arity. This requirement replaces the prior class-level all-parent-methods-to-all-child-
methods adjacency expansion, which produced edges between unrelated methods, silently dropped large
class pairs, and was applied inconsistently across reachability paths.

#### Scenario: Override edge connects matching methods only

- **GIVEN** a base class `Animal` with methods `speak` and `feed`, and a subclass `Dog` that
  overrides `speak` but not `feed`
- **WHEN** the call graph is built
- **THEN** an `overrides`-kind edge exists from `Animal.speak` to `Dog.speak`, and no override edge
  exists from `Animal.feed` to `Dog.speak`

#### Scenario: Override propagation is consistent across reachability paths

- **GIVEN** a base method change analyzed once through the in-memory adjacency and once through the
  database-backed lazy reachability path
- **WHEN** the impacted set is computed by each path
- **THEN** both paths reach the overriding methods through the same materialized override edges and
  report the same overrides as impacted

#### Scenario: Override edges do not contribute to call distance

- **GIVEN** a base method `B.m`, an override `D.m`, and a `find_path` query whose only connection
  between two functions runs through the `B.m → D.m` override edge
- **WHEN** call distance is computed
- **THEN** the override edge is not treated as a call hop and does not appear as a step in a returned
  call path

#### Scenario: No silent drop on large class pairs

- **GIVEN** a base class and a subclass whose combined method count is large enough that the prior
  cross-product expansion would have been skipped
- **WHEN** the call graph is built
- **THEN** override edges are still emitted for every name-and-arity-matched override pair, and none
  are silently dropped

### Requirement: CHAProvenanceLabeling

The system SHALL label every CHA-synthesized edge with a provenance distinct from directly-resolved
edges by setting its `confidence` to `synthesized` and recording the producing rule in the
`synthesizedBy` property, using `cha-declared-type` for a virtual-dispatch edge whose target set was
narrowed by a statically-recovered receiver type, `cha-name-only` for a virtual-dispatch edge
resolved by method name over the whole hierarchy (the receiver type was not statically recovered and
the call site's argument count is not captured, so no arity narrowing is applied), and `override` for
a method-level override edge.
Directly-resolved edges SHALL retain their existing `confidence` value and SHALL NOT carry
`synthesizedBy`. A virtual-dispatch edge SHALL NOT introduce a new `EdgeConfidence` member or a new
call-distance cost — it SHALL reuse the existing `synthesized` cost, which is strictly greater than
any directly-resolved confidence. A serialized call graph that predates these edges SHALL load
unchanged.

#### Scenario: Precise and over-approximating dispatch are distinguishable

- **GIVEN** one virtual-dispatch edge resolved via a recovered declared type and one resolved only by
  method name
- **WHEN** the two edges are inspected
- **THEN** the first carries `synthesizedBy: 'cha-declared-type'` and the second carries
  `synthesizedBy: 'cha-name-only'`, and both carry `confidence: 'synthesized'`

#### Scenario: Override edge carries provenance

- **GIVEN** a materialized override edge
- **WHEN** the edge is inspected
- **THEN** its `confidence` is `synthesized`, its `kind` is `overrides`, and its `synthesizedBy` is
  `override`

#### Scenario: Virtual-dispatch edges cost more than a directly-resolved path

- **GIVEN** two call paths from A to B, one entirely directly-resolved and one traversing a
  virtual-dispatch edge
- **WHEN** call distance is computed for each path
- **THEN** the path traversing the virtual-dispatch edge has the greater total cost, and the
  `callDistance` confidence switch remains exhaustive without a new arm

### Requirement: HighPrecisionCHABounds

The system SHALL bias CHA edge synthesis toward false-negatives over false-positives. It SHALL emit a
virtual-dispatch edge only when the called method name resolves to at least one implementation in the
extracted hierarchy. Per-call-site target fan-out SHALL be capped by a fixed bound (default 8); a
call site whose name-and-arity candidate set exceeds the bound SHALL be dropped (no edges emitted for
it) rather than partially or speculatively wired, and the drop SHALL be logged with the method name
and candidate count. The system SHALL NOT maintain a hard-coded denylist of method names; bounding is
achieved by resolving only against user-defined hierarchy types and by the fan-out cap.

#### Scenario: Ubiquitous method name exceeding the cap is dropped, not guessed

- **GIVEN** a call `x.handle()` whose name-and-arity candidate set across the hierarchy exceeds the
  fan-out cap
- **WHEN** the CHA pass runs
- **THEN** no virtual-dispatch edges are emitted for that call site and the drop is logged with the
  method name and candidate count

#### Scenario: Unresolvable method emits nothing

- **GIVEN** a call `x.frobnicate()` where no hierarchy class declares a method named `frobnicate`
- **WHEN** the CHA pass runs
- **THEN** no virtual-dispatch edge is emitted for that call site

### Requirement: ProvenanceAwareReachability

The system SHALL prevent synthesized edges from manufacturing false dead-code positives while still
benefiting from them: a symbol reachable from a root only through one or more synthesized edges
(including dynamic-dispatch edges, CHA virtual-dispatch edges, and method-level override edges) SHALL
NOT be reported as `high`-confidence dead by dead-code analysis. Such a symbol SHALL be reclassified
as reachable, or at minimum reported at `low` confidence with a reason that names the synthesizing
rule. A symbol reachable through at least one fully directly-resolved path is unaffected.

The system SHALL include synthesized edges by default when computing reachability, impact, subgraphs,
paths, and execution traces, and SHALL provide an option to restrict traversal to directly-resolved
edges only, so a caller can trade completeness for certainty. This `directResolvedOnly` option SHALL
apply uniformly across the in-memory adjacency, the database-backed lazy reachability path, and
weighted (call-distance) traversal, excluding CHA virtual-dispatch and override edges identically to
dynamic-dispatch edges. The system SHALL NOT expand the class hierarchy into reachability through any
mechanism other than materialized, provenance-labeled override and virtual-dispatch edges (retiring
the prior unlabeled all-methods cross-product, which strict mode could not exclude and which differed
between reachability paths).

#### Scenario: Override-only-reachable symbol is not high-confidence dead

- **GIVEN** an overriding method reachable from an entry point only through a base method via a
  materialized override edge
- **WHEN** dead-code analysis runs
- **THEN** the override is not reported as `high`-confidence dead, and if reported at all it is `low`
  confidence with a reason naming the synthesizing rule

#### Scenario: Polymorphic-call-only-reachable symbol is not high-confidence dead

- **GIVEN** a method implementation reachable from an entry point only through a CHA virtual-dispatch
  edge
- **WHEN** dead-code analysis runs
- **THEN** the implementation is not reported as `high`-confidence dead

#### Scenario: Strict mode excludes CHA edges in every reachability path

- **GIVEN** a traversal requested in directly-resolved-only mode, run once through the in-memory
  adjacency and once through the database-backed lazy path
- **WHEN** reachability is computed by each
- **THEN** neither path traverses CHA virtual-dispatch or override edges, and a symbol reachable only
  through them is treated as unreached by both

#### Scenario: Test selection follows a base-method change to subtype tests

- **GIVEN** a base method `B.m`, an overriding `D.m`, and a test that exercises `D.m`
- **WHEN** `B.m` is changed and tests are selected with synthesized edges included
- **THEN** the test exercising `D.m` is selected, reached through the override edge

### Requirement: LengthpreservingMaskingOfPythonNoncodeRegionsBeforeRouteExtraction

The system SHALL mask Python triple-quoted strings and line comments length-preservingly before route-pattern matching so that character offsets remain byte-aligned with the original source.

> Decision recorded: 65d4ac12
> Date: 2026-06-17
### Requirement: HardenChaPrecisionFromRealooDogfoodingSamefileBaseResolutionVarnewtTypeRecoveryCHierarchyExtraction

The system SHALL resolve base-class references to same-file declarations before falling back to global name matching, and SHALL recover types from `var x = new T()` patterns in Java and C#.

> Decision recorded: 66e47bb4
> Date: 2026-06-17
### Requirement: ExtendChaHierarchyExtractionToKotlinphpswiftscalaWithAmbiguityskipAndQualifiedsupertypeskipGuards

The system SHALL extract class-hierarchy relationships for Kotlin, PHP, Swift, and Scala, skipping ambiguous cross-file base names and qualified supertypes to avoid false-positive dispatch edges.

> Decision recorded: 9d87726f
> Date: 2026-06-17
### Requirement: ExtendChaTypehierarchyExtractionToKotlinPhpSwiftAndScala

The system SHALL extract class-hierarchy relationships (extends/implements/mixin edges) from Kotlin, PHP, Swift, and Scala source files using tree-sitter grammars.

> Decision recorded: bf52c392
> Date: 2026-06-17
### Requirement: AmbiguousCrossfileParentResolutionPrefersFalsenegativeOverFalsepositive

The system SHALL skip cross-file parent resolution when the parent name is ambiguous (declared in more than one file), preferring false-negatives over false-positives in inheritance edges.

> Decision recorded: d5967a48
> Date: 2026-06-17
### Requirement: ResolveChaBaseClassesByLayeredEvidenceSamefileImportSamedirectoryGlobaluniqueInsteadOfBarenamethenskip

The system SHALL resolve CHA base-class references using layered evidence (same-file, import, same-directory, global-unique) before falling back to ambiguity-skip.

> Decision recorded: 320bf215
> Date: 2026-06-17
### Requirement: DetectGoStructEmbedsByAnonymousFieldNotFieldTypeAndUnwrapPointerEmbeds

The system SHALL detect Go struct embedding only via anonymous fields (with pointer unwrapping), never via named fields.

> Decision recorded: db3b354b
> Date: 2026-06-17
### Requirement: UseADeterministicFieldweightedRankerForRecallNoLearnedModel

The system SHALL rank recalled memories using a deterministic field-weighted scoring algorithm with identifier-aware normalization, without requiring LLM inference or embedding lookups.

> Decision recorded: 08005eb9
> Date: 2026-06-18
### Requirement: NameThePreflightBlastradiusGuardBlastradiusMcpBlastradiusCliDistinctFromTheExistingPreflightStalenessGate

The system SHALL provide a `blast_radius` tool (MCP) and `blast-radius` command (CLI) that computes the structural blast radius of a diff by orchestrating existing deterministic analyses into a single conclusion-shaped briefing.

> Decision recorded: 987286eb
> Date: 2026-06-18
### Requirement: BlastradiusHookUsesCapabilityDetectionInsteadOfFileexistenceCheck

The system SHALL The pre-commit hook SHALL verify that the openlore binary supports the blast-radius --hook flag before invoking it, falling back to exit 0 when it does not.

> Decision recorded: 51c2603d
> Date: 2026-06-19
### Requirement: BlastradiusPipelineTreatsAllInternalErrorsAsAdvisoryNeverblockGuarantee

The system SHALL The blast-radius pipeline SHALL catch exceptions from all composed handlers and degrade to advisory caveats, ensuring a zero exit code regardless of internal errors.

> Decision recorded: 215092bc
> Date: 2026-06-19

## Technical Notes

- **Implementation**: `src/core/analyzer/repository-mapper.ts, src/api/types.ts, src/core/analyzer/embedding-service.ts, src/core/analyzer/subgraph-extractor.ts, src/core/analyzer/architecture-writer.ts, src/core/analyzer/dependency-graph.ts, src/core/analyzer/spec-vector-index.ts, src/core/analyzer/file-walker.ts, src/core/analyzer/import-resolver-bridge.ts, src/core/analyzer/refactor-analyzer.ts, src/core/analyzer/vector-index.ts, src/core/analyzer/import-parser.ts, src/core/analyzer/signature-extractor.ts, src/core/analyzer/artifact-generator.ts, src/core/analyzer/cpp-header-resolver.ts, src/core/analyzer/call-graph.ts, src/core/analyzer/duplicate-detector.ts, src/core/analyzer/type-inference-engine.ts, src/core/analyzer/significance-scorer.ts, src/core/analyzer/http-route-parser.ts, src/core/analyzer/ast-chunker.ts, src/core/analyzer/codebase-digest.ts, src/utils/progress.ts, src/utils/prompts.ts, src/utils/logger.ts, src/utils/shutdown.ts`
- **Dependencies**: FileWalker, SignificanceScorer, OpenLoreConfig, SerializedCallGraph, FunctionNode, FileSignatureMap, DependencyGraphResult, LLMContext, ImportExportParser, HttpRouteParser, ScoredFile, resolveImport, extractAllHttpEdges, EmbeddingService, fileExists, node:fs/promises, node:path, ignore, FileAnalysis, CallGraph, DuplicateDetector, CodeShaper, ExtractedSignature, RepositoryMap, ProjectType, RepoStructure, ArchitectureLayer, DetectedDomain, EntryPointInfo, DataFlowInfo, KeyFiles, LLMContextPhase, CallGraphBuilder, MappingEntry, RefactorReport, TypeInferenceEngine, ImportMap, CallGraphResult, FunctionRegistryTrie, FileMetadata, ScoringConfig, FileRelationship, chokidar, SignatureExtractor, VectorIndex, ConfigManager, tree-sitter, tree-sitter-typescript, tree-sitter-python, tree-sitter-go, tree-sitter-rust, tree-sitter-ruby, tree-sitter-java, code-shaper

## Decisions

### IaC resources project onto the existing call-graph primitives

**Status:** Approved
**Date:** 2026-06-01
**ID:** 8e1f10e6

Infrastructure-as-code resources (Terraform, Kubernetes) are modeled as nodes and edges in the same EdgeStore call graph rather than a separate schema, so cross-domain code-to-infra impact analysis reuses existing traversal and query machinery instead of a parallel representation.

**Consequences:** IaC and code share one graph; a new resource type maps onto graph primitives, not a new store.

### EdgeStore uses SCHEMA_VERSION rebuild-on-bump instead of migrations

**Status:** Approved
**Date:** 2026-06-01
**ID:** 81ba22ca

The call-graph SQLite store is a derived cache, not a source of truth, so on a schema change we bump SCHEMA_VERSION and rebuild from source via analyze, avoiding fragile incremental migrations for a regenerable artifact.

**Consequences:** No migration code to maintain; a version bump triggers one re-analyze.

### BM25 keyword retrieval is the zero-network floor; embeddings are optional

**Status:** Approved
**Date:** 2026-06-01
**ID:** 06a6e545

Semantic retrieval must work with no API key and no network, so the default index is a deterministic BM25 corpus built from static analysis; embeddings layer on only when a provider is configured (Spec 06).

**Consequences:** orient and search_code always work offline; embeddings are an optional ranking boost, never a hard dependency.

### Decisions projected as first-class graph nodes with dedicated SQLite tables

**Status:** Approved
**Date:** 2026-06-02
**ID:** bf450b1c

Architectural decisions from the JSON store are projected into the code graph as `decision::` nodes with `affects` edges to governed files, enabling deterministic graph joins (e.g. orient can query governing decisions via SQL rather than runtime set-membership filtering). Dedicated tables keep decision data isolated so code-node statistics (hubs, entry points, countNodes) and call-edge BFS remain untouched.

**Consequences:** Schema version bumped to 3 (old DBs auto-rebuilt). EdgeStore gains decision-specific queries (insertDecisions, getAllDecisions, getDecisionsForFiles). The JSON decision store remains authoritative; SQLite projection is derived and best-effort — a malformed store never blocks the code-graph write. A new EdgeKind 'affects' is added to the call-graph type union.

### Cross-domain code↔infra edges via line-containment linkage

**Status:** Approved
**Date:** 2026-06-02
**ID:** 708a8436

Embedded IaC frameworks (Pulumi/CDK/CDKTF) declare infrastructure resources inside regular code files. To answer 'what infrastructure does this code provision?' deterministically, the call-graph builder now finds the narrowest enclosing code function by line range and emits a `references` edge from that function to the IaC resource node.

**Consequences:** analyze_impact and get_subgraph now traverse the code↔infra boundary end-to-end without any new edge kind — existing `references` traversal covers it. Standalone IaC files (.tf/.yaml) produce no cross-domain edges since there is no co-located code function. Module-top-level IaC declarations with no enclosing function are intentionally left unlinked.

### Local git/GitHub provenance stored as per-file rows in the edge-store SQLite DB

**Status:** Approved
**Date:** 2026-06-02
**ID:** f8778883

Spec-18 requires local provenance (last author, recent authors, associated PRs) to be available for orient and other MCP queries. Storing one row per file in a dedicated `provenance` table keeps the data bounded, avoids inflating the graph edges table, and allows idempotent full-replace on each analyze run.

**Consequences:** Schema version bumped to 4; existing DBs are dropped and rebuilt. Provenance extraction is best-effort and swallowed on failure, so non-git or shallow repos silently produce no provenance. All provenance is local-only — nothing is uploaded.

### EdgeStore exposes wasReset flag after schema migration wipe

**Status:** Approved
**Date:** 2026-06-02
**ID:** 8365f6f8

When the schema version bumps, EdgeStore drops all tables and rebuilds. Read-only callers (MCP handlers) need to distinguish an empty-but-valid store from a wiped store so they can prompt the user to re-run analyze rather than silently returning empty results.

**Consequences:** All read-path consumers of EdgeStore should check wasReset before serving results; the flag is set once at open time and is not resettable.

### Call-distance budget floors at max direct-edge cost

**Status:** Approved
**Date:** 2026-06-09
**ID:** 660e1edf

Verification on the repo's own graph showed the first cut of get_minimal_context's distance budgets (low=2/med=3/high=4) returned ZERO callers for 17% of low-risk functions that have callers, because their direct callers are name_only edges (cost 3 > budget 2). get_minimal_context exists to show direct callers for safe edits, so empty results are a regression. Floor the budget at the maximum direct-edge cost (name_only=3): low=3, med=4, high=6. Direct neighbours (cost ≤3) are therefore always within budget and never dropped for weak resolution; the tier now governs only how far past direct a tightly-coupled chain is pulled in, and the k-cap plus nearest-distance ranking decide what survives when neighbours are plentiful.

**Consequences:** The proposal's original 'weak direct caller is dropped under a tight budget' behaviour is intentionally removed — dropping a 1-hop neighbour for resolution uncertainty discards real adjacency. The analyzer spec scenario for MinimalContextScopedByNearestDistance is reworded to rank-and-retain (direct neighbours kept, ranked nearest-first, far-weak chains bounded) and a no-regression scenario is added. Refines the earlier 'Confidence-weighted call-distance metric' decision; callDistance costs themselves are unchanged.

### Structural landmark signals as labels, not a composite score

**Status:** Approved
**Date:** 2026-06-09
**ID:** bb57d41e

Surface 'which functions are structural anchors and why' as a set of labeled signals with raw evidence, not a blended salience score. A composite (hub*40+orchestrator*20+...) would be deterministic-but-arbitrary — a black box the agent must trust — violating the north-star principle (c6d1ad07). computeLandmarkSignals reuses existing classifiers with no new thresholds: hub (fanIn>=5), orchestrator (fanOut>=GOD_FUNCTION_FAN_OUT_THRESHOLD), chokepoint (hub ∧ ¬orchestrator), entrypoint, volatile (reuses volatilityLevel from change-coupling), dead (reuses dead-code roots+forward-BFS). Each signal carries raw evidence. No score, no rank field; ordering is the caller's responsibility. A get_landmarks tool is classified conclusion under the tool contract and ships in the opt-in navigation preset only.

**Consequences:** No LANDMARK_WEIGHTS constant or composite salience introduced (explicitly rejected). computeLandmarkSignals takes optional opts (volatilityByFile, deadIds) so pure structural labels need only the graph while volatile/dead are injected from git/reachability — keeping core unit-testable. A deadCodeIds helper added to reachability.ts shares the documented roots definition with handleFindDeadCode. Phase 1 (signals) + Phase 3 (get_landmarks tool) ship now; Phase 2 (orient landmarks[] proximity-ordered enrichment) deferred to follow-up.

### Compute CFG/def-use overlay inside live-tree extractors, extend return contract to {nodes, rawEdges, cfg}

**Status:** Approved
**Date:** 2026-06-12
**ID:** c8f2b9bf

Parse trees are freed per-extractor before later passes (WASM path calls tree.delete), so a CFG/def-use pass cannot run as a late pass over already-built FunctionNodes — the AST is gone. The overlay must be computed inside each extractor while the tree is live. A shared cfg.ts module builds per-function basic blocks and runs an intra-procedural reaching-definitions fixpoint to produce labeled (exact|may) def-use edges, all from AST shape with no LLM.

**Consequences:** Every in-scope extractor (TS/JS, Python, Go in v1) gains an optional cfg build call; CallGraphResult carries a transient cfgs map threaded to the DB writer. The overlay is DB-only (new tables, SCHEMA_VERSION bump 6→7) and is NOT added to SerializedCallGraph or the hot cache, so resident memory is unchanged. Unsupported languages return cfg undefined (fail-soft).

### Expose value-level precision as opt-in valueLevel/valueParam flags reusing the directResolvedOnly pattern

**Status:** Approved
**Date:** 2026-06-12
**ID:** b6f04199

The reaching-definitions overlay enables value/parameter-granularity impact, but the default function-granularity answer must stay byte-for-byte unchanged (spec ValueLevelImpactOptIn). Rather than new tools (mcp-quality minimize-surface), add two opt-in params — valueLevel (boolean) and valueParam (string) — to the existing analyze_impact and trace_execution_path, mirroring the established directResolvedOnly opt-in. With the flag absent the traversal is identical to before; with it set, the consumer loads the seed function's overlay (EdgeStore.getCfg), computes a forward data-flow slice (valueReachableLines) over the def-use edges, and restricts downstream/first-hop to calls whose argument lines are data-dependent on the targeted value. The cross-call hop is labeled may. When the function has no overlay (unsupported language / no CFG) the tool falls back to function granularity rather than erroring.

**Consequences:** analyze_impact gains params 4-5 (valueLevel, valueParam); trace_execution_path gains params 7-8. tools/list payload grows by two opt-in props per tool — navigation preset ceiling bumped 11800->12300 (conscious, per spec-28 precedent). No default output changes. valueReachableLines is a pure exported overlay function in cfg.ts.

### Model try/catch, switch, and closures in the CFG so alternative-path defs don't kill each other and captures are may

**Status:** Approved
**Date:** 2026-06-12
**ID:** 2c0d04e3

Dogfooding the overlay on real TS found three soundness/precision bugs: (1) try/catch and switch were treated as straight-line statements, so a catch-body or later-case definition spuriously KILLED the try-body / earlier-case definition in reaching-defs, omitting a real dependence (violates the spec's never-omit-a-real-dependence requirement); (2) closure captures of outer variables were labeled exact instead of may (violates DataFlowProvenanceLabeling); (3) nested-function params/locals leaked as outer uses. Fix: processTry models catch clauses as alternative paths from the same predecessor (both reach the join), with finally processed from the merge; processSwitch models each case as an alternative branch with language-specific fall-through (switchFallsThrough: TS/JS true, Go/Python false) so cases don't linearly kill each other; nested functions are no longer descended into as outer CFG — their free-variable reads become may closure-capture uses, their own params/locals excluded.

**Consequences:** CfgLangSpec gains tryTypes/switchTypes/nestedFnTypes/switchFallsThrough. Reaching-defs is now sound across exception and multi-way control flow for the supported languages; closure-captured dependences are conservatively may. Verified deterministic and branch-structured on the real corpus (233 branch / 241 merge blocks).

### Downgrade escaped (closure-mutated / address-taken) variables to may so exact is never unsound

**Status:** Approved
**Date:** 2026-06-12
**ID:** 8192f32f

The exact precision label is the load-bearing safety signal an agent trusts; a wrong exact is actively harmful context. Two unsound-exact holes were found by adversarial probing: (1) a local scalar reassigned inside a nested closure that is then invoked — the value at a later read is not necessarily the visible def; (2) a Go local whose address is taken (&x) and mutated through the pointer. Fix: a pre-pass (collectEscapedVars) collects names assigned inside any nested closure (excluding the closure's own params/locals) and names whose address is taken, and computeReachingDefs forces precision may for any edge whose variable escaped. The set is deliberately over-approximated because a false inclusion only weakens precision (more may), never produces an unsound exact. Read-only closure captures are NOT downgraded (verified), preserving precision.

**Consequences:** exact now means soundly exact for the supported languages: a local-scalar def-use with no aliasing, closure mutation, or address-of. This makes the overlay safe for agents to trust the exact label. collectEscapedVars adds one linear pre-pass per function.

### Add lexical scope resolution to the def-use overlay so shadowed variables never conflate

**Status:** Approved
**Date:** 2026-06-12
**ID:** 84cc2af4

Two parallel adversarial audits (real-repo stress + soundness review) found that name-based reaching-defs conflated shadowed variables, producing unsound exact edges: an inner block let/const/:= (TS/Go) or a comprehension loop var (Python) reached an outer same-named use as exact, and dropped the correct outer def. Fix: a resolveScopes pre-pass maps every identifier occurrence to a scope-qualified key (name#scopeId) — nearest enclosing scope that declares the name, else root (params/globals). Reaching-defs keys GEN/KILL/reach by the scoped key while emitting the bare name. Scopes open at: nested functions, comprehensions (Python), catch clauses, and (block-scoped langs only) every block and loop header. Also fixed: x++/x-- recorded as use+def (and as closure mutation), Go closure-escape descending expression_list LHS + inc/dec + address-of inside closures, Python try/else as the no-exception continuation, and labeled-statement unwrapping so labeled loops keep their back-edge.

**Consequences:** CfgLangSpec gains blockScoped, comprehensionTypes, updateTypes. Op/DefSite gain a scope key. exact now holds across shadowing for TS/JS/Python/Go. Verified deterministic on real repos with 0 invariant violations across 1596 overlays; ~90% exact / 10% may edge ratio.

### Compare AST nodes by position not identity; fail-soft on pathological CFGs; fix multi-line value-flow and global/nonlocal escape

**Status:** Approved
**Date:** 2026-06-12
**ID:** 045db3ae

A wider three-agent audit (value-level consumer, fuzz robustness, deep idioms) surfaced four issues. (1) DETERMINISM: callee-skip and case-value exclusion compared tree-sitter nodes by object identity (c === fn), but native tree-sitter returns fresh wrapper objects per access path, making the overlay non-deterministic (a flickering edge ~50% of builds). Fixed with a sameNode(a,b) position comparator (startIndex+endIndex); identity comparison of tree-sitter nodes is never safe. (2) FALSE NARROWING (value-level): valueReachableLines chained taint by exact line co-location, so a parameter feeding a multi-line initializer (const ctx = {\n x: p \n}) silently dropped every downstream the object reached — telling an agent a change is safe when it isn't. Fixed by tagging RHS uses with the start line of the def they feed (useInDefLine) and chaining on that. (3) UNSOUND-EXACT: Python global/same-level nonlocal vars were exact despite being mutable through hidden calls; now added to the escaped set (may). (4) ROBUSTNESS: the reaching-defs fixpoint was quadratic on deeply nested loops (47s at 700-deep); now capped at 128 sweeps + 4000 blocks, failing soft to no overlay. Also added Python with...as binding defs.

**Consequences:** Overlay is deterministic (verified 25-30 identical builds of call-heavy code + 3 identical full-analyze hashes). Value-level no longer false-narrows on the ubiquitous build-options-object-from-param pattern. global/nonlocal are may. Pathological/machine-generated functions fail soft in <2s. DefUseEdge gains optional useInDefLine. New caps: REACHING_MAX_SWEEPS=128, MAX_CFG_BLOCKS=4000.

### Fix Java arrow-switch, C++ alias/pointer escape + param extraction, Ruby statement modifiers in the overlay

**Status:** Approved
**Date:** 2026-06-12
**ID:** f5810afa

Two real-repo adversarial agents validating the Java/C++/Rust/Ruby overlay extension (gson, nlohmann/json, ripgrep, sinatra) found Rust fully sound but surfaced four unsound-exact issues and two coverage gaps. Fixed: (1) Java 14+ arrow switch (switch_rule) was unrecognized, dropping the whole switch and leaking the pre-switch def as exact — added switch_rule to caseParts/isCaseNode and made arrow/match/when cases non-fall-through. (2) C++ reference binding (int& r = x) aliases x; writes through r were invisible, leaving x exact — collectEscapedVars now marks a reference_declarator's referent as escaped (may). (3) C++ address-of &x parses as pointer_expression (not unary_expression), so escape analysis missed it — now handled. (4) Ruby statement modifiers (x = 2 if c) were treated as unconditional strong defs, dropping the prior reaching def — added processModifier modeling them as a one-armed branch/loop. Coverage: C++ parameters live under function_declarator (extractParamNames now descends, was [] for all C++); Java enhanced-for loop var is in the name field (recordLoopHeader now reads it).

**Consequences:** exact is now sound across Java/C++/Ruby switch and aliasing idioms. Verified on real repos: nlohmann/json 2172 overlays 0 violations with params now populated (795 fns), may correctly higher. Full suite 3603, deterministic. Remaining safe gaps: Ruby begin/rescue fails soft (no overlay) on some shapes.

### Extend overlay to C, C#, PHP via the spec-08 query path; keep divergent languages fail-soft

**Status:** Approved
**Date:** 2026-06-12
**ID:** 07c32832

User asked to cover all OpenLore-supported languages. A regression+fail-soft safety agent first proved the overlay breaks nothing for any of the 18 languages (analyze exits 0, no-overlay languages keep full call-graph nodes/edges with 0 overlay rows, 0 invariant violations). Then extended overlay support to three more native-grammar languages that map cleanly to the existing spec-driven builder: C (a syntactic subset of C++, reuses CPP_SPEC — zero risk), C# (Java-like; switch_section handled like Java groups), and PHP (C-family; case_statement reuses the default branch). Wired buildCfgFor into extractByQueries (the spec-08 path) inside withTree so it is WASM-tree-lifetime safe; made CfgNode.children optional since the soft-loaded node interface exposes only namedChildren. Generalized extractParamNames to spec.identTypes so PHP variable_name params are extracted. Divergent-control-flow languages (Kotlin when, Scala match, Swift switch-patterns) and the deferred set (Lua, Bash, Elixir, Dart) remain fail-soft (no spec -> buildFunctionCfg returns undefined) rather than risk an unsound overlay.

**Consequences:** Overlay now covers 11 languages: TS/JS, Python, Go, Java, C++, Rust, Ruby, C, C#, PHP. Remaining supported languages fail soft cleanly (verified). buildCfgFor accepts the CfgNode interface; extractByQueries returns cfg. Deterministic; full suite 3606.

### v1 overlay language scope: 11 field-mappable languages; divergent-grammar languages stay fail-soft

**Status:** Approved
**Date:** 2026-06-12
**ID:** 997c1da9

The CfgLangSpec builder is field-centric (childForFieldName for condition/consequence/body/left/right). The 11 languages added (TS/JS, Python, Go, Java, C++, Rust, Ruby, C, C#, PHP) all expose these fields and map cleanly, validated by adversarial agents on real repos. Kotlin, Scala, and Swift have field-less/positional grammars — e.g. Kotlin if_expression exposes no condition/consequence/alternative fields (positional children), Swift if/switch bodies are bare `statements` nodes. Adapting the builder to positional parsing for these is bug-prone and would risk the unsound-exact edges the whole effort guards against. Per the never-feed-an-agent-wrong-data principle and the explicit deferral in the original proposal, these plus Lua/Bash/Elixir/Dart remain fail-soft (no CfgLangSpec -> buildFunctionCfg returns undefined -> no overlay), which a regression agent proved is clean: they still graph fully (nodes/edges) with 0 overlay rows and no crash. They are candidates for dedicated future passes with positional-grammar handling.

**Consequences:** v1 overlay scope is the 11 field-mappable languages. The remaining 7 supported code languages are covered safely by fail-soft, not by a (potentially unsound) overlay. Future extension requires generalizing processIf/processLoop to positional fallbacks before adding a divergent-grammar spec.

### Extend CFG escape analysis to Rust &mut borrows, JS shorthand-destructure reassigns, and variadic params

**Status:** Approved
**Date:** 2026-06-14
**ID:** 3bade573

Adversarial real-repo dogfooding (serde, clap, ky/zod/got) found unsound `exact` def-use labels. (1) Rust `&mut x` is a `reference_expression` with a `mutable_specifier` child, a node type the escape walk never inspected (it only handled `unary_expression`/`pointer_expression` with `&`), so every mutably-borrowed local stayed `exact` despite out-of-band mutation (243 confirmed bad edges in serde+clap). A shared borrow `&x` (no `mutable_specifier`) correctly stays `exact`. (2) JS/TS object-shorthand destructuring reassignment in a closure (`({a,b} = …)`) left a/b `exact` because `addLeftIdents` did not descend `shorthand_property_identifier_pattern`. (3) Variadic params (TS `...rest` → `rest_pattern`, Python `*args`/`**kwargs` → `list_splat_pattern`/`dictionary_splat_pattern`) extracted as no param, so value-level impact under-reported a function's blast radius.

**Consequences:** `exact` is now soundly exact for Rust mutable borrows and JS shorthand-destructure closure captures; both are correctly downgraded to `may`. Variadic params are extracted, so the value-level overlay sees data flow from them. Overlay content changes (more `may` edges, more param defs) on repos using these idioms — a correctness improvement, still deterministic.

### Value-level impact/trace falls back to function granularity on an ill-posed query

**Status:** Approved
**Date:** 2026-06-14
**ID:** 313b897e

Dogfooding showed the value-level opt-in could silently report zero blast radius when valueReachableLines() returned an empty set — e.g. a mistyped valueParam that matches no parameter/local, or an "all parameters" request on a function the overlay extracted no params for. Zero downstream reads to an agent as "this change is safe," the exact failure value-level must avoid. The handlers now treat a query as well-posed only when its target resolves in the overlay (a named valueParam is a parameter or a tracked def-use variable; an unnamed request needs at least one parameter) and otherwise fall back to the full function-granularity result with an explicit reason, instead of an empty narrowed slice.

**Consequences:** analyze_impact and trace_execution_path return applied:false with a clear reason (and the full blast radius / unrestricted first hop) when the value-level target can't be resolved, rather than a misleading zero. A genuine zero — a real parameter that flows to no callee — is still reported as a sound applied:true narrowing. Regression-tested in graph.test.ts.

### Downgrade PHP by-ref captures/foreach and C# ref locals in CFG escape analysis

**Status:** Approved
**Date:** 2026-06-14
**ID:** 3ffcd5e5

Adversarial dogfooding of the less-validated languages (Java/C#/PHP/C/Ruby on gson, AutoMapper, MediatR, FastRoute, Guzzle, sds, sinatra) found three unsound `exact` def-use classes — Java, C, and Ruby were sound. (1) PHP `use (&$x)` by-reference closure capture: a subscript/member write inside the closure (`$called[] = …`, a real Guzzle test pattern) mutates the outer var out of band, but addLeftIdents deliberately skips subscript writes and the analyzer never inspected the anonymous_function_use_clause/by_ref node, so it could not tell by-ref from by-value capture. (2) PHP `foreach ($a as &$v)`: the by-ref loop var aliases each element and stays aliased to the last after the loop. (3) C# ref local `ref T r = ref e`: r aliases e, so writes through r change e — the variable_declarator/ref_expression form had no case (ref/out arguments were already handled).

**Consequences:** A PHP by-ref capture escapes unconditionally; PHP foreach by-ref loop vars and C# ref-local referents are downgraded to `may`. Verified on real Guzzle: the previously-unsound `$called` def-use edges are now `may`; 0 structural violations, determinism bit-identical. Noted v1 limitations (not fixed, require non-syntactic analysis): PHP variable-variables (`$$name`) and by-ref builtins (`sort($arr)`). Regression-tested per case in cfg.test.ts.

### CFG overlay build and value-level consumers are exception-isolated (silent-fail routing)

**Status:** Approved
**Date:** 2026-06-14
**ID:** fda9fc24

PR #147 moved tree-sitter and grammars to optionalDependencies with lazy/null-safe loading. Merging it into the CFG-overlay branch raised the chance that the overlay builder meets an unexpected or partially-loaded grammar shape. The overlay is strictly additive, so a builder throw must never break the base call graph. buildCfgFor previously called buildFunctionCfg without a guard — a throw would propagate into the extractor and be caught only by the per-file dispatch try/catch, which skips the ENTIRE file, silently dropping its base nodes/edges (and in watch mode rolling back the per-file swap). Now buildCfgFor wraps the build in try/catch and fails soft to no overlay. Symmetrically, the value-level opt-in consumers (analyze_impact, trace_execution_path) wrap their overlay use in try/catch and fall back to the full function-granularity result on any error, so a corrupt overlay blob or builder surprise can never fail the tool.

**Consequences:** An overlay-build or overlay-read failure degrades to "no overlay / function-granularity" with a DEBUG log, never an exception that drops base call-graph data or fails an MCP tool. The merge resolution keeps each language extractor's null-safe early return (grammar absent → empty nodes/edges/cfg). Regression-tested: value-level falls back (not throws) when the store errors.

### Infinite loops drop the phantom skip-body edge in the CFG (no unsound exact)

**Status:** Approved
**Date:** 2026-06-14
**ID:** 7b1d823d

A final pre-merge audit found an unsound `exact` in processLoop: it unconditionally added a condition→merge `false` edge (the "body skipped" path), which is correct for a pre-test or foreach/range loop (zero iterations possible) but WRONG for an infinite loop (Rust `loop {}`, Go `for {}`, C-family `for(;;)`), whose body always runs and whose only exits are `break`s. The phantom edge let a pre-loop def bypass the body and reach a post-loop use as `exact` even when the body unconditionally reassigned it — confirmed across Rust, Go, C, C++, Java, C#. Fixed with an isInfiniteLoop() guard that drops the false edge only for truly-infinite loops, while foreach/range and conditional loops keep it (so a pre-loop def still reaches when the iterable is empty). Go exposes `for cond {}` as a bare expression child with no condition field, so the loop's condition is now recovered structurally (also fixing a latent gap where Go for-condition reads were never recorded). The reaching-defs edge sort comparator gained useInDefLine as a final tiebreaker so output ordering never relies on sort stability.

**Consequences:** `exact` is now sound for code following an infinite loop (the pre-loop def is correctly killed). Foreach/range/conditional loops are unchanged (pre-loop def still reaches via the empty-iteration path). Go `for cond {}` now records its condition's variable reads. Verified deterministic + 0 structural violations at scale on clap/fmt/gin; per-language regression tests added. Staged as v2.0.18.

### Anchor persisted memory to call-graph symbols with deterministic freshness

**Status:** Approved
**Date:** 2026-06-16
**ID:** 34b178df

Every persisted memory (architectural decisions and remember-notes) carries StructuralAnchors resolved against the call graph, and recall computes a fresh/drifted/orphaned verdict from booleans only (symbol existence + content-hash equality) — no LLM, no threshold, no weighted score. This is what code-anchored memory can do that probabilistic vector memory cannot: self-invalidate when the code it describes changes or dies, so recall never serves stale context silently.

**Consequences:** New StructuralAnchor/MemoryFreshness/AnchoredMemory types and a pure anchor engine (decisions/anchor.ts) plus a disk adapter. record_decision now captures anchors. Two new opt-in MCP tools (remember/recall) in a 'memory' preset, kept out of the default/minimal surface. recall enforces a no-silent-stale guarantee (orphaned memories are never authoritative). Notes stored in .openlore/memory, isolated from the decisions gate. Wiring memory-staleness into check_spec_drift and orient is deferred.

### Code-anchored memory tools (remember/recall) are opt-in via a dedicated preset

**Status:** Approved
**Date:** 2026-06-16
**ID:** b92dada4

Following the mcp-quality minimize-tool-surface principle, memory tools are excluded from default and minimal presets and shipped in a separate 'memory' preset so agents only see them when explicitly opted in.

**Consequences:** Users must enable the memory preset to access remember/recall; the default tool surface stays lean; memory tools are classified as conclusion-class in the tool contract.

### Synthesize dynamic-dispatch call edges with explicit provenance

**Status:** Approved
**Date:** 2026-06-16
**ID:** b7395127

The call graph is built purely by direct name resolution, so it is blind to dynamic dispatch (event emit/on, route→handler, callback registration), causing find_dead_code false positives, under-counted analyze_impact, and missed select_tests. A post-resolution synthesis pass recovers these edges deterministically from the AST using pattern-matching over tree-sitter trees (no LLM). Each synthesized edge carries its own provenance (confidence='synthesized' + synthesizedBy naming the rule) so it is never silently mixed with directly-resolved edges.

**Consequences:** EdgeConfidence gains 'synthesized'; CallEdge gains optional synthesizedBy; CALL_DISTANCE_COSTS assigns higher cost to synthesized edges so directly-resolved paths are preferred. Synthesis pass runs after resolution with per-rule fan-out caps. Reachability/dead-code must not report synthesized-only-reachable symbols as high-confidence dead; traversal tools gain a directly-resolved-only strict mode. Serialized graphs without synthesizedBy load unchanged (backward compatible).

### Expose value-level precision as opt-in valueLevel/valueParam flags on existing impact tools

**Status:** Approved
**Date:** 2026-06-16
**ID:** 4cf203d7

The reaching-definitions overlay enables value/parameter-granularity impact analysis, but the default function-granularity answer must stay byte-for-byte unchanged (spec ValueLevelImpactOptIn). Rather than adding new tools (violating mcp-quality minimize-surface), two opt-in params — valueLevel (boolean) and valueParam (string) — are added to analyze_impact and trace_execution_path, mirroring the established directResolvedOnly opt-in pattern. With the flags absent the traversal is identical to before; with them set, the consumer loads the seed function's overlay via EdgeStore.getCfg, computes a forward data-flow slice (valueReachableLines) over def-use edges, and restricts downstream/first-hop to calls whose argument lines are data-dependent on the targeted value. Cross-call hops are labeled 'may'. When a function has no overlay (unsupported language / no CFG) the tool falls back to function granularity rather than erroring.

**Consequences:** analyze_impact gains two params (valueLevel, valueParam); trace_execution_path gains two params. tools/list payload grows by two opt-in props per tool — navigation preset token ceiling bumped 11800→12300 (conscious, per spec-28 precedent). No default output changes. valueReachableLines is a pure exported overlay function in cfg.ts.

### Model try/catch, switch, and closures as branching/alternative-path structures in the CFG

**Status:** Approved
**Date:** 2026-06-16
**ID:** 3db9424d

Dogfooding the overlay on real TS revealed three soundness/precision bugs: (1) try/catch and switch were treated as straight-line statements, so catch-body or later-case definitions spuriously killed try-body / earlier-case definitions in reaching-defs, omitting real dependences (violates the spec's never-omit-a-real-dependence requirement); (2) closure captures of outer variables were labeled exact instead of may (violates DataFlowProvenanceLabeling); (3) nested-function params/locals leaked as outer uses. Fix: processTry models catch clauses as alternative paths from the same predecessor (both reach the join), with finally processed from the merge; processSwitch models each case as an alternative branch with language-specific fall-through (switchFallsThrough: TS/JS true, Go/Python false) so cases don't linearly kill each other; nested functions are not descended into as outer CFG — their free-variable reads become may closure-capture uses, their own params/locals excluded.

**Consequences:** CfgLangSpec gains tryTypes/switchTypes/nestedFnTypes/switchFallsThrough. Reaching-defs is now sound across exception and multi-way control flow for supported languages; closure-captured dependences are conservatively may.

### Compare tree-sitter AST nodes by position, not object identity; fail-soft on pathological CFGs; fix multi-line value-flow chaining and global/nonlocal escape

**Status:** Approved
**Date:** 2026-06-16
**ID:** a95c5c98

A wide audit surfaced four correctness/robustness issues in the CFG overlay. (1) DETERMINISM: tree-sitter returns fresh wrapper objects per access path, so comparing nodes by === is non-deterministic (flickering edges ~50% of builds). A sameNode(a,b) helper comparing startIndex+endIndex replaces all identity checks; identity comparison of tree-sitter nodes is never safe. (2) VALUE-LEVEL FALSE NARROWING: valueReachableLines chained taint by exact line co-location, so a parameter feeding a multi-line initializer silently dropped every downstream use the object reached — telling an agent a change is safe when it isn't. Fixed by tagging RHS uses with useInDefLine (the start line of the def they feed) and chaining on that. (3) UNSOUND-EXACT: Python global and same-level nonlocal variables were labeled exact despite being mutable through hidden calls; now added to the escaped set (may), extending the pattern established in 8192f32f. (4) ROBUSTNESS: the reaching-defs fixpoint was quadratic on deeply nested loops; now capped at REACHING_MAX_SWEEPS=128 and MAX_CFG_BLOCKS=4000, failing soft to no overlay rather than hanging.

**Consequences:** Overlay is deterministic (verified across 25-30 identical builds). Value-level analysis no longer false-narrows on the ubiquitous build-options-object-from-param pattern. global/nonlocal are may. Pathological/machine-generated functions fail soft in <2s. DefUseEdge gains optional useInDefLine field.

### Fix Java arrow-switch, C++ alias/pointer escape, C++ param extraction, and Ruby statement modifiers in the CFG overlay

**Status:** Approved
**Date:** 2026-06-16
**ID:** 2480603b

Adversarial validation on real repos (gson, nlohmann/json, ripgrep, sinatra) surfaced four unsound-exact issues and two coverage gaps in the multi-language overlay extension. (1) Java 14+ arrow switch (switch_rule) was unrecognized, dropping the whole switch and leaking the pre-switch def as exact — added switch_rule to caseParts/isCaseNode and made arrow/match/when cases non-fall-through. (2) C++ reference binding (int& r = x) aliases x; writes through r were invisible, leaving x exact — collectEscapedVars now marks a reference_declarator's referent as escaped (may). (3) C++ address-of &x parses as pointer_expression (not unary_expression), so escape analysis missed it — now handled. (4) Ruby statement modifiers (x = 2 if c) were treated as unconditional strong defs, dropping the prior reaching def — added processModifier modeling them as a one-armed branch/loop. Coverage: C++ parameters live under function_declarator (extractParamNames now descends); Java enhanced-for loop var is in the name field (recordLoopHeader now reads it).

**Consequences:** exact is now sound across Java/C++/Ruby switch and aliasing idioms. Verified on real repos: nlohmann/json 2172 overlays 0 violations with params now populated (795 fns). Full suite 3603, deterministic. Remaining safe gap: Ruby begin/rescue fails soft (no overlay) on some shapes.

### Extend CFG overlay to C, C#, PHP; divergent-control-flow languages remain fail-soft

**Status:** Approved
**Date:** 2026-06-16
**ID:** 04831b41

C reuses CPP_SPEC as a syntactic subset (zero risk). C# maps to the Java-like spec path (switch_section handled like Java case groups). PHP is C-family with case_statement reusing the default branch logic. buildCfgFor is wired into extractByQueries (the spec-08 path) inside withTree for WASM-tree-lifetime safety. CfgNode.children made optional since soft-loaded nodes expose only namedChildren. extractParamNames generalized to spec.identTypes so PHP variable_name params are extracted. Languages with divergent control flow (Kotlin when, Scala match, Swift switch-patterns) and the deferred set (Lua, Bash, Elixir, Dart) fail soft (no spec → buildFunctionCfg returns undefined) rather than risk an unsound overlay.

**Consequences:** Overlay covers 11 languages: TS/JS, Python, Go, Java, C++, Rust, Ruby, C, C#, PHP. Remaining supported languages fail soft cleanly (verified by regression agent). buildCfgFor accepts the CfgNode interface; extractByQueries returns cfg.

### v1 CFG overlay covers 11 field-mappable languages; divergent-grammar languages stay fail-soft

**Status:** Approved
**Date:** 2026-06-16
**ID:** 27680bb9

The CfgLangSpec builder is field-centric (childForFieldName for condition/consequence/body/left/right). The 11 languages (TS/JS, Python, Go, Java, C++, Rust, Ruby, C, C#, PHP) all expose these fields and map cleanly, validated by adversarial agents on real repos. Kotlin, Scala, and Swift have field-less/positional grammars (e.g. Kotlin if_expression exposes no condition/consequence/alternative fields), and Lua/Bash/Elixir/Dart are deferred — adapting the builder to positional parsing is bug-prone and risks the unsound-exact edges the overlay guards against. Per the never-feed-an-agent-wrong-data principle, these 7 remain fail-soft (no CfgLangSpec → buildFunctionCfg returns undefined → no overlay rows, no crash), proven clean by a regression agent.

**Consequences:** v1 overlay scope is the 11 field-mappable languages. The remaining 7 supported code languages are covered safely by fail-soft, not by a potentially unsound overlay. Future extension requires generalizing processIf/processLoop to positional fallbacks before adding a divergent-grammar spec.

### Value-level impact/trace falls back to function granularity on ill-posed queries instead of reporting zero

**Status:** Approved
**Date:** 2026-06-16
**ID:** a37d851f

Dogfooding revealed that valueReachableLines() could return an empty set on ill-posed queries (mistyped valueParam, or 'all parameters' on a function with no overlay params), which an agent interprets as 'this change is safe' — the exact failure value-level must avoid. The handlers now validate that the target resolves in the overlay (a named valueParam is a known parameter or tracked def-use variable; an unnamed request needs at least one parameter) and fall back to full function-granularity with an explicit reason when it does not, rather than returning a misleading zero-impact narrowing.

**Consequences:** analyze_impact and trace_execution_path return applied:false with a clear reason (plus the full blast radius / unrestricted first hop) when the value-level target can't be resolved. A genuine zero — a real parameter that flows to no callee — is still reported as applied:true. Regression-tested in graph.test.ts.

### Rust impl extraction keys methods on implementing type, not trait

**Status:** Approved
**Date:** 2026-06-16
**ID:** 2ec9dd45

For `impl Trait for Struct`, the previous heuristic (first `type_identifier` child) picked the trait name, wrongly attributing methods to the trait and causing collisions across all impls of that trait. Using the tree-sitter `type` field targets the implementing type, and stripping generic args normalizes `Box<T>` to `Box`.

**Consequences:** Rust method stable IDs and call-graph edges change for any method inside `impl Trait for X` or generic impl blocks; downstream caches keyed on className may need rebuilding.

### Same-file path-id collapse is an inherited completeness limit, not a stableId defect

**Status:** Approved
**Date:** 2026-06-16
**ID:** 38356f7e

Two distinct same-file symbols that collide on the path-based node id (file::Class.name) are dropped at analyzer node aggregation (last-write-wins) before stableId runs. The survivor resolves uniquely; the dropped twin is invisible everywhere. This is a pre-existing container-qualification gap that stableId inherits, not a wrong-identity bug. A literal-aware comment stripper was rejected because naive // removal corrupts string-literal defaults (e.g. URLs), CREATING collisions. Namespace-qualifying className was rejected as a cross-cutting 13-language analyzer change out of scope. Signature-shape sensitivity to in-parameter comments and brace defaults always fails safe (orphan or remove+add, never wrong identity).

**Consequences:** analyzer/spec.md now distinguishes two regimes: (1) cross-file homonym → ambiguous→null safe fallback, (2) same-file path-id collapse → completeness gap (dropped symbol invisible to structural_diff etc.). Signature-shape comment/default sensitivity documented as a deliberate safe-failing limit. Two pinning tests added. No runtime behavior change. Future work if needed: namespace/full-nested-path qualification of className across languages.

### Personalized PageRank as query-conditioned retrieval ranking (not global salience)

**Status:** Approved
**Date:** 2026-06-16
**ID:** 0bdd4319

Shortest-path distance ranks a candidate by its single cheapest path to the task seeds; it cannot capture multi-path / connectivity-weighted relevance. Personalized PageRank (random-walk-with-restart seeded on the task's matched symbols) ranks a candidate by how many ways and how densely it is connected to the task, which is a better objective for pulling the most task-relevant functions into a fixed token budget. This is exposed strictly as an opt-in retrieval ranking mode on existing handlers (orient, get_minimal_context), seeded by the task-symbol set orient already computes — it is query-conditioned, never a global task-independent importance number. It refines the scope of the add-structural-landmark-salience decision (c6d1ad07 lineage) to global salience only; it does not overturn it. It introduces no new tuning constant — damping (0.85) and convergence tolerance (1e-6) are extracted to shared named constants with the existing PageRank in dependency-graph.ts. It must demonstrate lift over the distance ranker on >=2 real repos or be closed.

**Consequences:** Adds an opt-in rankBy: "pagerank" mode to orient and get_minimal_context; default behavior of every handler stays byte-identical and the distance ranker is retained. A new deterministic personalized-PageRank primitive is added over the in-memory call graph (sorted-id iteration, id tie-break, distance-bounded neighborhood). No new MCP tool and no change to default/minimal/preset tool surfaces. If the acceptance comparison shows no lift, the change is closed and the landmark decision is left intact.

### Epistemic lease emits neutral freshness facts, not coercive imperatives

**Status:** Approved
**Date:** 2026-06-16
**ID:** 8e95746d

The epistemic-lease feature injected escalating imperative language into every MCP tool response (STOP, "Repository model: EXPIRED", "do NOT…"). This is structurally a prompt-injection pattern — it trains agents to obey authoritative imperatives in tool output, the exact behavior agents must resist — and contradicts the north-star decision (c6d1ad07: deterministic structural facts, not guessing) and the landmark-salience principle (hand the agent facts, let it rank). Wall-clock age alone escalated to CRITICAL (false positive), and the agent's own commits flipped the lease to stale via git-hash divergence even though committing is the most-informed action in a session. Fix: emit a single neutral, factual freshness note (minutes since orient, cognitive load since orient, whether the analysis index is behind HEAD) phrased as information the agent can act on, not a command. Drive severity from accumulated cognitive load, not wall clock.

**Consequences:** staleBlock/degradedSignal reworded to neutral facts (no STOP/EXPIRED/do-NOT, no system-banner box art); git-hash divergence no longer forces stale — it sets a factual index-behind-HEAD flag and at most contributes to degraded; computeStaleDepth driven by cognitive load, not wall-clock age; decay tracking, cross-module density/oscillation model, and telemetry retained; epistemic-lease gains a spec requirement (mcp-handlers) and ADR where it previously had neither.

### Unify domain-name derivation behind a single leaf-first helper

**Status:** Approved
**Date:** 2026-06-16
**ID:** 201ace19

Domain naming was split across two diverging functions: `suggestDomainName` (dependency-graph clusters) correctly walks the path leaf-first and skips build-layout/reverse-DNS noise, but `inferDomains` (repository-mapper, which drives SUMMARY's 'Detected Domains' and the generated OpenSpec spec files) walked root-first and picked the first non-skip segment. On Java/Kotlin projects this grabbed the reverse-DNS org root (e.g. `org/springframework/...`) so every business file collapsed into one nonsense `springframework` domain — the exact bug reported in #138 and still present after #139, which only fixed the cluster path. Extracting one shared `deriveDomainFromPath` helper (leaf-first walk + shared DOMAIN_NOISE_DIRS + canonical pattern map) makes both code paths agree and prevents future drift.

**Consequences:** New module src/core/analyzer/domain-naming.ts owns DOMAIN_NOISE_DIRS, the canonical domain pattern map, and deriveDomainFromPath. dependency-graph.ts and repository-mapper.ts both consume it. inferDomains now yields leaf-package business domains (vet/owner/system) instead of the org root; SUMMARY and generated specs change accordingly for Java/Kotlin/Go projects.

### Add JPA/Hibernate entity parser to schema extractor

**Status:** Approved
**Date:** 2026-06-16
**ID:** 6af4d045

Java codebases using JPA/Hibernate annotations (@Entity, @MappedSuperclass) had no schema extraction support, leaving persistent models invisible to the schema inventory. The parser reads raw Java source (not skeletonized) because the TS/Py skeletonizer would mangle annotations and field declarations.

**Consequences:** OrmType union gains 'jpa' variant; Java files with @Entity/@MappedSuperclass are parsed from raw source while all other languages continue through the skeletonizer. Future ORM dialects for Java (e.g. Micronaut Data, jOOQ codegen) would need separate parsers.

### Generalize file-extension stripping and Java method signature extraction for multi-language support

**Status:** Approved
**Date:** 2026-06-16
**ID:** 8f94535c

The analyzer hardcoded JS/TS/Python extensions in several places, causing Java/Kotlin files to leak extensions into entity names (e.g. VetControllerJava), miss handler methods with inline annotations (@ResponseBody), mis-parse license-header block comments as declarations, and drop methods with complex generic type parameters. Fixes generalize extension stripping, add block-comment tracking, widen the method regex for annotations in return-type position, and reject body-statement false positives.

**Consequences:** Extension stripping is now generic (any single dot-extension), which is broader but could theoretically strip meaningful suffixes from unconventional filenames. Java signature extraction is richer (modifiers, type params, throws clauses preserved) but the regex is more complex to maintain.

### Inject call-graph edges into the dependency graph for JVM languages

**Status:** Approved
**Date:** 2026-06-17
**ID:** 67580817

Java/Kotlin require imports only for cross-package references; same-package classes are used with no import. The dependency graph was built purely from import edges, so a Java project's file-level graph was nearly empty (spring-petclinic: 10 edges) while its call graph held 1261 — same-package relationships were invisible, hurting structural comprehension and leaving cluster views empty. Fix: (1) run injection for Java/Kotlin regardless of import-edge count, (2) seed the dedup set with existing edges so injected call edges never duplicate an import edge, (3) resolve call-graph file paths to absolute so the two id spaces align.

**Consequences:** New exported SAME_PACKAGE_IMPLICIT_LANGS set (Java, Kotlin). injectCallGraphEdges now dedupes against pre-existing edges, making it safe to run alongside import edges. The absolute-path resolution also repairs the previously-silent no-op injection for Swift/C/C++. Java/Kotlin dependency graphs are now populated (petclinic 10→70 edges, gson 318→1517) with structural clusters; injected edges carry isCallEdge:true.

### Require a ws.rs import for JAX-RS route detection

**Status:** Approved
**Date:** 2026-06-17
**ID:** 954eac79

JAX-RS route detection fired on any Java file containing both @Path and an HTTP-method annotation (@GET/@POST/...). Retrofit — an HTTP CLIENT library — uses identically-named @GET/@POST/@Path from retrofit2.http on interface methods (client request templates), so OpenLore hallucinated 28 phantom server routes for it (adversarial-audit finding). The defining signal for a real JAX-RS server resource is the javax.ws.rs / jakarta.ws.rs import, which Retrofit never has and Spring does not need. Gating JAX-RS detection on that import removes the false positives without affecting Spring (separate detection path) or genuine JAX-RS resources (which always import ws.rs).

**Consequences:** extractJavaRouteDefinitions now requires an `import javax|jakarta.ws.rs` before classifying a file as JAX-RS. Retrofit/OkHttp client interfaces yield 0 routes; petclinic Spring routes (17) and JAX-RS resources (which import ws.rs) are unaffected.

### JPA field parser handles inline annotations on the same line as the field declaration

**Status:** Approved
**Date:** 2026-06-17
**ID:** 8605684f

Common JPA patterns place annotations inline with the field (e.g. `@Id private Long id;`). The previous parser only recognized annotations on their own line, causing inline-annotated fields — including primary keys — to be silently dropped from schema extraction.

**Consequences:** The parser now iteratively strips leading annotations before testing for a field match, correctly capturing inline-annotated fields. Pure-annotation lines still accumulate in pendingAnn for multi-line annotation stacks.

### JAX-RS route detection requires javax/jakarta.ws.rs import to avoid false positives from HTTP client libraries

**Status:** Approved
**Date:** 2026-06-17
**ID:** f9de2e30

Retrofit interfaces use identically-named @GET/@POST/@Path annotations from retrofit2.http, which are client request templates, not server endpoints. Without checking the import package, the parser would emit phantom server routes for HTTP client definitions.

**Consequences:** JAX-RS routes are only detected when the file imports from javax.ws.rs or jakarta.ws.rs; projects using non-standard JAX-RS re-exports would not be recognized. Retrofit and similar HTTP client interfaces are correctly excluded.

### Length-preserving masking of Python non-code regions before route extraction

**Status:** Approved
**Date:** 2026-06-17
**ID:** 65d4ac12

Route-matching regexes use character offsets (m.index) that are resolved to line numbers via getLine(), which measures against original line lengths. Stripping comments changed string length and could drift line numbers. Additionally, triple-quoted docstrings can contain route-like patterns (e.g. Flask's sansio/scaffold.py code-block examples) that produced false-positive route matches. Length-preserving masking keeps byte alignment while eliminating both failure modes.

**Consequences:** Over-masking (e.g. unmatched triple-quotes) can only drop potential matches, never invent them, consistent with the false-negatives-over-false-positives bias. Any future non-code region handling in route extraction must preserve string length or getLine() will produce wrong line numbers.

### Harden CHA precision from real-OO dogfooding: same-file base resolution, var-new-T type recovery, C# hierarchy extraction

**Status:** Approved
**Date:** 2026-06-17
**ID:** 66e47bb4

Adversarial dogfooding CHA on real OO corpora (java-design-patterns, python-patterns, dotnet/samples) surfaced three defects: (1) global first-match base-class resolution linked unrelated same-named classes across files — fixed by preferring same-file ClassNode resolution before global fallback; (2) type-inference required uppercase declared types, so Java 10+/C# `var x = new T()` locals recovered no type and virtual calls fell to broad cha-name-arity — fixed by adding var-new-T matchers for Java and C#; (3) no C# branch in extractClassRelationships left CHA inert for C# — fixed by adding a C# branch over base_list, splitting base-class vs interface by I<Upper> naming convention.

**Consequences:** Same-file base resolution eliminates cross-file name collisions (empty same-file base still falls back to global — rare, documented). var-new-T recovery converts many heuristic cha-name-arity edges into precise type_inference edges (Java cha-name-arity 113→65 on design-patterns slice). C# CHA now functional (0→81 inheritance / 59 override edges on slice). Residual cha-name-arity over-approximation remains for non-locally-recoverable receivers; field-type tracking / RTA-VTA pruning stay out-of-scope per HighPrecisionCHABounds.

### Extend CHA hierarchy extraction to Kotlin/PHP/Swift/Scala with ambiguity-skip and qualified-supertype-skip guards

**Status:** Approved
**Date:** 2026-06-17
**ID:** 9d87726f

CHA was inert for Kotlin, PHP, Swift, and Scala because extractClassRelationships had no branch for them, producing zero inheritance and dispatch edges. Added hierarchy-extraction branches for each language (Kotlin/Swift via delegation/inheritance specifiers; PHP distinguishing extends from implements; Scala via extends_clause with a new getScalaParser). Real-repo dogfooding then surfaced two false-positive bugs: (1) cross-file/cross-namespace same-name class collisions wired by the global first-match fallback — fixed by skipping resolution when a base name is non-same-file AND ambiguous; (2) Kotlin/Swift qualified supertypes (Outer.Inner) mis-captured the outer segment as the base — fixed by taking the leaf name and skipping dotted types.

**Consequences:** CHA now has hierarchy support for 11 languages (TS/JS, Python, Java, C++, C#, Ruby, Go, Kotlin, PHP, Swift, Scala). The ambiguity-skip is language-agnostic and eliminates cross-file collision false positives everywhere, at the cost of skipping legitimate cross-file override edges whose base name is reused (FQCN/namespace-aware resolution is the future enhancement). Swift/Scala protocol/trait abstract methods without bodies are not extracted as nodes, so only concrete-base hierarchies get edges.

### Extend CHA type-hierarchy extraction to Kotlin, PHP, Swift, and Scala

**Status:** Approved
**Date:** 2026-06-17
**ID:** bf52c392

The polymorphic-dispatch (CHA) feature requires class-hierarchy edges for each supported language; adding tree-sitter-based extraction for these four languages broadens the cross-language coverage of resolved virtual-call edges.

**Consequences:** New runtime dependency on tree-sitter-scala; each language follows the same pattern (lazy parser init + query-based extraction) so maintenance cost is linear; qualified/nested type names are deliberately skipped to avoid phantom edges.

### Ambiguous cross-file parent resolution prefers false-negative over false-positive

**Status:** Approved
**Date:** 2026-06-17
**ID:** d5967a48

When multiple classes share a bare name across different files (e.g. two unrelated `Logger` interfaces in different PHP namespaces), a global first-match would fabricate a false override edge and steal the real one from the correct twin; skipping the resolution entirely is safer for downstream dispatch accuracy.

**Consequences:** Some legitimate cross-file inheritance edges will be missed when name collisions exist; this is an acceptable precision-over-recall tradeoff that avoids polluting the call graph with phantom dispatch targets.

### Resolve CHA base classes by layered evidence (same-file → import → same-directory → global-unique) instead of bare-name-then-skip

**Status:** Approved
**Date:** 2026-06-17
**ID:** 320bf215

The prior ambiguity-skip in buildClassNodes dropped cross-file override edges whenever the base class name was reused anywhere in the codebase — measured at ~37% of all base-references on Laravel (1034/2810). Replaced with layered, most-specific-evidence-first resolution: (1) class in child's own file; (2) file the child imports the name from via importMap; (3) class unique within child's directory (same package); (4) globally-unique class; (5) skip. Each layer carries real evidence, so only genuinely-ambiguous cross-directory bases with no import are skipped, preserving the false-negatives-over-false-positives bias while recovering legitimate edges.

**Consequences:** Threads the existing importMap into buildClassNodes (new optional param). Recall recovered substantially with no precision loss: DesignPatternsPHP 71→87 override edges, Laravel 1758 override edges / 512 cross-package at ~100% precision. Residual: globally-duplicated base names without import or same-dir co-location are still skipped. True FQCN/namespace extraction for non-importMap languages (PHP/Kotlin/Swift/Scala/C#) remains a future enhancement.

### Detect Go struct embeds by anonymous field (not field type), and unwrap pointer embeds

**Status:** Approved
**Date:** 2026-06-17
**ID:** db3b354b

Real-repo dogfood (cobra) showed the Go hierarchy extractor misread NAMED struct fields as embeds: the query captured the type of every field_declaration, so `CompletionOptions CompletionOptions` (a named field) produced phantom `embeds` inheritance edges and polluted ClassNode.parentClasses with field types. Go embedding is an ANONYMOUS field (a type with no field name); a named field is composition, not embedding. Changed the Go branch in extractClassRelationships to treat a field_declaration as an embed only when it has no `name:` child, and to unwrap a leading pointer (`*Base`) which the prior query had also missed (false negative).

**Consequences:** Go embedding edges are now precise: anonymous Base and *Mixin embeds wire; named fields do not. parentClasses is no longer polluted with field types. Verified on cobra and a synthetic struct. Go structural interface satisfaction remains unsupported (future work).

### Use a deterministic field-weighted ranker for recall (no learned model)

**Status:** Approved
**Date:** 2026-06-18
**ID:** 08005eb9

recall previously ranked memories by binary substring token-overlap, which silently dropped relevant memories on a phrasing mismatch (e.g. a camelCase identifier vs a plain word). Replaced it with a deterministic field-weighted, graded ranker: identifier-aware normalization (camelCase/PascalCase/snake_case/kebab-case split before lower-casing, fixed stopword set), fixed field weights (anchorSymbol 4 > tag 3 > anchorFile 2 > content 1), occurrence-capped grading, and an exact-anchor boost (8) when the query names every subtoken of an anchored symbol. This keeps the memory path LLM-free and embedding-free per the north star (decision c6d1ad07) while fixing the worst retrieval failure mode. A substring fallback (weight 0.1, applied only when the token score is zero) guarantees the candidate set is a superset of the old behavior.

**Consequences:** Weights and the stopword set are fixed, documented, exported constants — changing them is a code+test change, not a runtime knob. recall items gain an optional match {fields, anchorBoost} field for transparent ranking reasons. Embedding-backed recall is deliberately deferred to a future proposal with its own decision. The authoritative/orphaned freshness split is unchanged and still runs after ranking.

### Grounding certificates + verified-current + grounding-density budget tiering on recall

**Status:** Approved
**Date:** 2026-06-18
**ID:** 61c2ea7d

Implements add-trust-calibrated-context-economy on the recall path (the only memory surface; orient has none yet). A fresh recalled fact now carries a GroundingCertificate {symbol?, filePath, lineSpan?, contentHash} per anchor and a verifiedCurrent marker, so the agent can cite the proven-unchanged span instead of re-reading. The certificate reuses the same span the freshness check hashes; lineSpan is computed from the node's byte offsets against the live file (the edge store persists offsets, not line numbers), so no schema change and no new extraction. recall gains an optional tokenBudget that returns the highest grounding-density facts first (verified-current core) and reports the withheld count — never a silent cap. Deliberate deviation from the proposal: budget ordering uses grounding density (verified-current first) rather than pulling the hub/chokepoint/volatile salience classifiers into the memory path, keeping the change deterministic and self-contained; salience-label ordering is noted as a future refinement.

**Consequences:** New GroundingCertificate type. recall response gains optional verifiedCurrent/certificates per item and a budget {tokenBudget, returned, withheld} block with a no-silent-cap note; all additive (callers ignoring them are unaffected). Certificates only attach to fresh, anchored facts when the graph is available; drifted/orphaned never carry them, preserving the authoritative-recall invariant. The full MCP tool manifest is near its spec-28 char budget, so the recall tool description was kept minimal (the certificate is self-documenting via response fields) — reinforces add-lean-default-tool-surface.

### Name the pre-flight blast-radius guard `blast_radius` (MCP) / `blast-radius` (CLI), distinct from the existing `preflight` staleness gate

**Status:** Approved
**Date:** 2026-06-18
**ID:** 987286eb

The add-preflight-blast-radius-guard proposal is titled "pre-flight blast-radius guard," but `openlore preflight` already exists as an unrelated CI graph-staleness gate (src/cli/preflight/). Reusing the word "preflight" across both surfaces would conflate two different concerns. The new capability is named `blast_radius` everywhere to be collision-free and self-describing ("compute my diff's structural blast radius"). It is implemented as pure orchestration of existing deterministic analyses (analyze_impact, select_tests, check_spec_drift which already folds in anchored-memory + ADR drift, and getChangedFiles) composed into a single conclusion-shaped briefing — no new structural computation, no LLM. The MCP tool is classified `conclusion` and kept out of the `minimal` preset. The git hook is advisory-by-default (exit 0); opt-in blocking for named high-risk patterns reads `.openlore/config.json` `blastRadius.block`. The multi-repo-federation cross-repo-consumers input is scoped out (federation not yet shipped) and documented as a no-op with a note.

**Consequences:** A new MCP tool `blast_radius` and CLI `openlore blast-radius` (with --install-hook, --hook, --json) ship; OpenLoreConfig gains an optional `blastRadius?: { block?: string[] }` field; a new advisory pre-commit hook block (marker `# openlore-blast-radius-hook`) installs alongside the decisions hook. Federation cross-repo consumers remain a documented gap until add-multi-repo-federation lands.

### Blast-radius hook uses capability detection instead of file-existence check

**Status:** Approved
**Date:** 2026-06-19
**ID:** 51c2603d

A stale or incompatible openlore binary that lacks the --hook flag would fail at runtime and block the commit. Probing --help output ensures the hook only fires when the installed version actually supports it.

**Consequences:** The hook is self-disabling against older openlore versions; users upgrading incrementally will silently skip blast-radius until the new binary is in place.

### Blast-radius pipeline treats all internal errors as advisory (never-block guarantee)

**Status:** Approved
**Date:** 2026-06-19
**ID:** 215092bc

The blast-radius briefing composes multiple handlers (impact analysis, spec drift); any throw from a composed handler must degrade gracefully rather than blocking a commit, preserving the advisory-by-default contract.

**Consequences:** Failures in individual symbols or drift checks are surfaced as caveats in the briefing but never produce a non-zero exit code; bugs in composed handlers become harder to notice without inspecting briefing output.
