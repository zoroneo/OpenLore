# API Specification

> Generated by openlore v1.0.0 on 2026-04-05 10:50

## Purpose

This document specifies the programmatic (embeddable) API exposed by `src/api/` — the in-process
functions (`init`, `analyze`, `generate`, `drift`, `run`, …) a host process calls to drive OpenLore.
OpenLore is a local-first CLI/library; it exposes no networked HTTP API and performs no user
authentication (see `overview` and `mcp-security`). Credentials appear only as LLM-provider API
keys read from the environment.

## Requirements

### Requirement: Functionconfiguration

The API SHALL support `Function init(options: InitApiOptions): Promise<InitResult>` to [partial spec — file too large to fully analyze (2 parts)] initialize the configuration for the openlore tool.

**Request:**

```json
{
  "force": "boolean",
  "openspecPath": "string",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "configPath": "string",
  "openspecPath": "string",
  "projectType": "string",
  "created": "boolean"
}
```

#### Scenario: InitializeConfiguration
- **GIVEN** No existing configuration
- **WHEN** init() is called
- **THEN** Configuration file is created and paths are returned

#### Scenario: OverwriteExistingConfiguration
- **GIVEN** Existing configuration with force=true
- **WHEN** init() is called
- **THEN** Existing configuration is overwritten

### Requirement: Functionanalysis

The API SHALL support `Function analyze(options: AnalyzeApiOptions): Promise<AnalyzeResult>` to [partial spec — file too large to fully analyze (2 parts)] analyze the codebase to generate repository and dependency maps.

**Request:**

```json
{
  "maxFiles": "number",
  "includePatterns": "string[]",
  "excludePatterns": "string[]",
  "force": "boolean",
  "outputPath": "string",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "repoMap": "CoreRepositoryMap",
  "depGraph": "DependencyGraphResult",
  "artifacts": "AnalysisArtifacts",
  "duration": "number"
}
```

#### Scenario: AnalyzeCodebase
- **GIVEN** Valid codebase
- **WHEN** analyze() is called
- **THEN** Repository and dependency maps are generated

#### Scenario: ForceReAnalysis
- **GIVEN** Existing analysis with force=true
- **WHEN** analyze() is called
- **THEN** Fresh analysis is performed

### Requirement: Runanalysisanalysis

The API SHALL support `runAnalysis runAnalysis(rootPath: string, outputPath: string, options: { maxFiles: number; include: string[]; exclude: string[] }): Promise<AnalysisResult>` to [partial spec — file too large to fully analyze (3 parts)] run the complete analysis pipeline including repository mapping, dependency graph building, and artifact generation.

**Request:**

```json
{
  "rootPath": "string",
  "outputPath": "string",
  "options": {
    "maxFiles": "number",
    "include": "string[]",
    "exclude": "string[]"
  }
}
```

**Response:**

```json
{
  "repoMap": "RepositoryMap",
  "depGraph": "DependencyGraphResult",
  "artifacts": "AnalysisArtifacts",
  "duration": "number"
}
```

#### Scenario: RunCompleteAnalysis
- **GIVEN** A valid rootPath and outputPath, and options including maxFiles, include, and exclude patterns
- **WHEN** runAnalysis is called
- **THEN** Returns an AnalysisResult object containing repoMap, depGraph, artifacts, and duration
- **AND** Logs the number of files found, analyzed, and skipped
- **AND** Logs the number of nodes, edges, and clusters in the dependency graph
- **AND** Logs any circular dependencies found
- **AND** Generates and saves analysis artifacts

### Requirement: Functiongeneration

The API SHALL support `Function generate(options: GenerateApiOptions): Promise<GenerateResult>` to [partial spec — file too large to fully analyze (2 parts)] generate openapi specifications from the analyzed codebase.

**Request:**

```json
{
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "sslVerify": "boolean",
  "openaiCompatBaseUrl": "string",
  "domains": "string[]",
  "writeMode": "'replace' | 'merge' | 'skip'",
  "adr": "boolean",
  "adrOnly": "boolean",
  "mapping": "boolean",
  "dryRun": "boolean",
  "analysisPath": "string",
  "force": "boolean",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "report": "GenerationReport",
  "pipelineResult": "PipelineResult",
  "duration": "number"
}
```

#### Scenario: GenerateSpecifications
- **GIVEN** Analyzed codebase
- **WHEN** generate() is called
- **THEN** OpenAPI specifications are generated

#### Scenario: DryRun
- **GIVEN** Analyzed codebase with dryRun=true
- **WHEN** generate() is called
- **THEN** Specifications are previewed without writing

### Requirement: Functionverification

The API SHALL support `Function verify(options: VerifyApiOptions): Promise<VerifyResult>` to [partial spec — file too large to fully analyze (2 parts)] verify the generated specifications against the codebase.

**Request:**

```json
{
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "openaiCompatBaseUrl": "string",
  "sslVerify": "boolean",
  "samples": "number",
  "threshold": "number",
  "domains": "string[]",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "report": "VerificationReport",
  "duration": "number"
}
```

#### Scenario: VerifySpecifications
- **GIVEN** Generated specifications
- **WHEN** verify() is called
- **THEN** Specifications are verified against the codebase

#### Scenario: SampleVerification
- **GIVEN** Generated specifications with samples=5
- **WHEN** verify() is called
- **THEN** Only 5 files are sampled for verification

### Requirement: Functiondrift

The API SHALL support `Function drift(options: DriftApiOptions): Promise<DriftResult>` to [partial spec — file too large to fully analyze (2 parts)] detect drift between the codebase and specifications.

**Request:**

```json
{
  "baseRef": "string",
  "files": "string[]",
  "domains": "string[]",
  "llmEnhanced": "boolean",
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "openaiCompatBaseUrl": "string",
  "sslVerify": "boolean",
  "failOn": "DriftSeverity",
  "maxFiles": "number",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "driftItems": "DriftItem[]",
  "summary": "DriftSummary"
}
```

#### Scenario: DetectDrift
- **GIVEN** Codebase and specifications
- **WHEN** drift() is called
- **THEN** Drift between code and specs is detected

#### Scenario: LlmEnhancedDriftDetection
- **GIVEN** Codebase and specifications with llmEnhanced=true
- **WHEN** drift() is called
- **THEN** LLM is used for deeper semantic comparison

### Requirement: Functionpipeline

The API SHALL support `Function run(options: RunApiOptions): Promise<{ init: InitResult, analyze: AnalyzeResult, generate: GenerateResult, verify: VerifyResult }>` to [partial spec — file too large to fully analyze (2 parts)] run the full pipeline from initialization to verification.

**Request:**

```json
{
  "force": "boolean",
  "reanalyze": "boolean",
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "sslVerify": "boolean",
  "openaiCompatBaseUrl": "string",
  "maxFiles": "number",
  "adr": "boolean",
  "dryRun": "boolean",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "init": "InitResult",
  "analyze": "AnalyzeResult",
  "generate": "GenerateResult",
  "verify": "VerifyResult"
}
```

#### Scenario: RunFullPipeline
- **GIVEN** Codebase
- **WHEN** run() is called
- **THEN** Full pipeline from initialization to verification is executed

#### Scenario: DryRun
- **GIVEN** Codebase with dryRun=true
- **WHEN** run() is called
- **THEN** Pipeline is previewed without changes

### Requirement: Getrunresult

The API SHALL support `GET /api/run-result` to [partial spec — file too large to fully analyze (2 parts)] retrieve the result of a openlore run.

**Response:**

```json
{
  "init": {
    "status": "string",
    "message": "string"
  },
  "analysis": {
    "status": "string",
    "message": "string",
    "artifacts": "AnalysisArtifacts"
  },
  "generation": {
    "status": "string",
    "message": "string",
    "report": "GenerationReport"
  },
  "duration": "number"
}
```

#### Scenario: SuccessfulRunResultRetrieval
- **GIVEN** A completed openlore run
- **WHEN** GET /api/run-result is called
- **THEN** 200 OK with the RunResult in the response body

### Requirement: Functioninsertionrole

The API SHALL support `function classifyRole(fanIn: number, fanOut: number, isHub: boolean, isEntryPoint: boolean): InsertionRole` to classifies the role of a node in the codebase based on its fan-in, fan-out, hub status, and entry point status.

**Request:**

```json
{
  "fanIn": "number",
  "fanOut": "number",
  "isHub": "boolean",
  "isEntryPoint": "boolean"
}
```

**Response:**

```json
{
  "role": "InsertionRole"
}
```

#### Scenario: ClassifyANodeAsAHub
- **GIVEN** A node with high fan-in and fan-out, and marked as a hub
- **WHEN** classifyRole is called
- **THEN** Returns the role as a hub

#### Scenario: ClassifyANodeAsAnEntryPoint
- **GIVEN** A node marked as an entry point
- **WHEN** classifyRole is called
- **THEN** Returns the role as an entry point

### Requirement: Functioninsertionstrategy

The API SHALL support `function deriveStrategy(role: InsertionRole): InsertionStrategy` to derives the insertion strategy based on the classified role of a node.

**Request:**

```json
{
  "role": "InsertionRole"
}
```

**Response:**

```json
{
  "strategy": "InsertionStrategy"
}
```

#### Scenario: DeriveStrategyForAHub
- **GIVEN** A node classified as a hub
- **WHEN** deriveStrategy is called
- **THEN** Returns the strategy for a hub

#### Scenario: DeriveStrategyForAnEntryPoint
- **GIVEN** A node classified as an entry point
- **WHEN** deriveStrategy is called
- **THEN** Returns the strategy for an entry point

### Requirement: Functioninsertionreason

The API SHALL support `function buildReason(name: string, role: InsertionRole, strategy: InsertionStrategy, fanIn: number, fanOut: number): unknown` to builds a reason for the insertion strategy based on the node's name, role, strategy, fan-in, and fan-out.

**Request:**

```json
{
  "name": "string",
  "role": "InsertionRole",
  "strategy": "InsertionStrategy",
  "fanIn": "number",
  "fanOut": "number"
}
```

**Response:**

```json
{
  "reason": "unknown"
}
```

#### Scenario: BuildReasonForAHub
- **GIVEN** A node classified as a hub with high fan-in and fan-out
- **WHEN** buildReason is called
- **THEN** Returns the reason for the hub strategy

#### Scenario: BuildReasonForAnEntryPoint
- **GIVEN** A node classified as an entry point
- **WHEN** buildReason is called
- **THEN** Returns the reason for the entry point strategy

### Requirement: Functioninsertionscore

The API SHALL support `function compositeScore(semanticRelevance: number, role: InsertionRole): number` to calculates a composite score based on semantic relevance and the role of the node.

**Request:**

```json
{
  "semanticRelevance": "number",
  "role": "InsertionRole"
}
```

**Response:**

```json
{
  "score": "number"
}
```

#### Scenario: CalculateCompositeScoreForAHub
- **GIVEN** A node with high semantic relevance and classified as a hub
- **WHEN** compositeScore is called
- **THEN** Returns a high composite score

#### Scenario: CalculateCompositeScoreForAnEntryPoint
- **GIVEN** A node with moderate semantic relevance and classified as an entry point
- **WHEN** compositeScore is called
- **THEN** Returns a moderate composite score

### Requirement: Functioncodesearch

The API SHALL support `function handleSearchCode(directory: string, query: string, limit: number = 10, language?: string, minFanIn?: number): unknown` to performs a semantic search for code in the specified directory based on the query.

**Request:**

```json
{
  "directory": "string",
  "query": "string",
  "limit": "number",
  "language": "string",
  "minFanIn": "number"
}
```

**Response:**

```json
{
  "results": "unknown"
}
```

#### Scenario: SearchForCodeInADirectory
- **GIVEN** A directory and a query
- **WHEN** handleSearchCode is called
- **THEN** Returns a list of code results matching the query

#### Scenario: SearchForCodeWithASpecificLanguage
- **GIVEN** A directory, a query, and a specific language
- **WHEN** handleSearchCode is called
- **THEN** Returns a list of code results matching the query and language

### Requirement: Functioninsertionpoint

The API SHALL support `function handleSuggestInsertionPoints(directory: string, description: string, limit: number = 5, language?: string): unknown` to suggests the best places in the codebase to implement a new feature based on the description.

**Request:**

```json
{
  "directory": "string",
  "description": "string",
  "limit": "number",
  "language": "string"
}
```

**Response:**

```json
{
  "insertionPoints": "unknown"
}
```

#### Scenario: SuggestInsertionPointsForANewFeature
- **GIVEN** A directory and a description of the new feature
- **WHEN** handleSuggestInsertionPoints is called
- **THEN** Returns a list of suggested insertion points

#### Scenario: SuggestInsertionPointsWithASpecificLanguage
- **GIVEN** A directory, a description of the new feature, and a specific language
- **WHEN** handleSuggestInsertionPoints is called
- **THEN** Returns a list of suggested insertion points in the specified language

### Requirement: Functionspeccontent

The API SHALL support `function handleGetSpec(directory: string, domain: string): unknown` to returns the full content of a spec domain's spec.md plus its mapping entries.

**Request:**

```json
{
  "directory": "string",
  "domain": "string"
}
```

**Response:**

```json
{
  "specContent": "unknown"
}
```

#### Scenario: GetSpecContentForADomain
- **GIVEN** A directory and a domain name
- **WHEN** handleGetSpec is called
- **THEN** Returns the full content of the spec.md and its mapping entries

### Requirement: Functionspecdomain

The API SHALL support `function handleListSpecDomains(directory: string): Promise<unknown>` to lists all spec domains available in the project.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "specDomains": "unknown"
}
```

#### Scenario: ListAllSpecDomainsInAProject
- **GIVEN** A directory
- **WHEN** handleListSpecDomains is called
- **THEN** Returns a list of all spec domains available in the project

### Requirement: Functionspecsearch

The API SHALL support `function handleSearchSpecs(directory: string, query: string, limit: number = 10, domain?: string, section?: string): unknown` to performs a semantic search over the spec index built by 'openlore analyze --embed'.

**Request:**

```json
{
  "directory": "string",
  "query": "string",
  "limit": "number",
  "domain": "string",
  "section": "string"
}
```

**Response:**

```json
{
  "searchResults": "unknown"
}
```

#### Scenario: SearchSpecsInADirectory
- **GIVEN** A directory and a query
- **WHEN** handleSearchSpecs is called
- **THEN** Returns a list of search results matching the query

#### Scenario: SearchSpecsInASpecificDomainAndSection
- **GIVEN** A directory, a query, a specific domain, and a specific section
- **WHEN** handleSearchSpecs is called
- **THEN** Returns a list of search results matching the query, domain, and section

### Requirement: Detectgitrepositoryproject

The API SHALL support `detectGitRepository detectGitRepository(rootPath: string): Promise<boolean>` to detect if the directory is a git repository

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "result": "boolean"
}
```

#### Scenario: DetectGitRepository
- **GIVEN** A directory path
- **WHEN** The function is called
- **THEN** Returns true if .git directory exists, false otherwise

### Requirement: Detectprojecttypeproject

The API SHALL support `detectProjectType detectProjectType(rootPath: string): Promise<ProjectDetectionResult>` to detect the project type based on manifest files

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "projectType": "string",
  "manifestFile": "string | null",
  "hasGit": "boolean",
  "confidence": "'high' | 'medium' | 'low'"
}
```

#### Scenario: DetectProjectType
- **GIVEN** A directory path
- **WHEN** The function is called
- **THEN** Returns the detected project type, manifest file, git status, and confidence level

### Requirement: Getprojecttypenameproject

The API SHALL support `getProjectTypeName getProjectTypeName(type: ProjectType): string` to get a human-readable project type name

**Request:**

```json
{
  "type": "ProjectType"
}
```

**Response:**

```json
{
  "name": "string"
}
```

#### Scenario: GetProjectTypeName
- **GIVEN** A project type
- **WHEN** The function is called
- **THEN** Returns the human-readable name of the project type

### Requirement: Readpackagejsonproject

The API SHALL support `readPackageJson readPackageJson(rootPath: string): Promise<Record<string, unknown> | null>` to read and parse package.json if it exists

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "packageJson": "Record<string, unknown> | null"
}
```

#### Scenario: ReadPackage.json
- **GIVEN** A directory path
- **WHEN** The function is called
- **THEN** Returns the parsed package.json content if it exists, null otherwise

### Requirement: Classmcpwatcher

The API SHALL support `Class McpWatcher` to watches source files for changes and incrementally updates signatures in llm-context.json and vector index.

#### Scenario: StartWatching
- **GIVEN** A valid project root path
- **WHEN** McpWatcher.start() is called
- **THEN** The watcher starts monitoring the project directory for changes

#### Scenario: StopWatching
- **GIVEN** An active McpWatcher instance
- **WHEN** McpWatcher.stop() is called
- **THEN** The watcher stops monitoring the project directory

#### Scenario: HandleFileChange
- **GIVEN** A source file is modified
- **WHEN** The watcher detects the change
- **THEN** The watcher re-indexes the file and updates llm-context.json
- **AND** If embedding is enabled and an embedding server is reachable, the vector index is updated

### Requirement: Functionproviderconfig

The API SHALL support `Function resolveProviderConfig(directory: string)` to [partial spec — file too large to fully analyze (4 parts)] resolve the active llm provider by checking environment keys in priority order: gemini api key → anthropic api key → openai-compatible base url → config file → openai key.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "kind": "string",
  "baseUrl": "string",
  "apiKey": "string",
  "model": "string"
}
```

#### Scenario: ResolveGeminiProvider
- **GIVEN** GEMINI_API_KEY is set in environment variables
- **WHEN** resolveProviderConfig is called
- **THEN** Returns a ProviderConfig with kind 'gemini', baseUrl, apiKey, and model

#### Scenario: ResolveAnthropicProvider
- **GIVEN** ANTHROPIC_API_KEY is set in environment variables
- **WHEN** resolveProviderConfig is called
- **THEN** Returns a ProviderConfig with kind 'anthropic', baseUrl, apiKey, and model

#### Scenario: ResolveOpenaiCompatibleProvider
- **GIVEN** OPENAI_COMPAT_BASE_URL and OPENAI_COMPAT_API_KEY are set in environment variables
- **WHEN** resolveProviderConfig is called
- **THEN** Returns a ProviderConfig with kind 'openai-compat', baseUrl, apiKey, and model

### Requirement: PostChatCompletions

The API SHALL support `POST /chat/completions` to [partial spec — file too large to fully analyze (4 parts)] process chat messages and execute tools based on the conversation.

**Request:**

```json
{
  "model": "string",
  "messages": "array",
  "tools": "array"
}
```

**Response:**

```json
{
  "choices": "array",
  "filePaths": "array"
}
```

#### Scenario: SuccessfulToolExecution
- **GIVEN** Valid API key and tool definitions
- **WHEN** POST /chat/completions is called with valid messages and tools
- **THEN** 200 OK with assistant's response and file paths
- **AND** Tool execution results are included in the response
- **AND** File paths are deduplicated

#### Scenario: ToolExecutionError
- **GIVEN** Valid API key and tool definitions
- **WHEN** POST /chat/completions is called with a tool that throws an error
- **THEN** 200 OK with error message in the response
- **AND** Error message is included in the response
- **AND** File paths are empty

#### Scenario: ApiError
- **GIVEN** Invalid API key
- **WHEN** POST /chat/completions is called
- **THEN** Error response with status code and message
- **AND** Error message includes status code
- **AND** Error message includes preview of the response

### Requirement: PostGenerateContent

The API SHALL support `POST /generateContent` to [partial spec — file too large to fully analyze (4 parts)] generate content using the gemini model with tool use capabilities.

**Request:**

```json
{
  "systemInstruction": {
    "parts": [
      {
        "text": "string"
      }
    ]
  },
  "contents": [
    {
      "role": "string",
      "parts": [
        {
          "text": "string"
        }
      ]
    }
  ],
  "tools": [
    {
      "function_declarations": [
        {
          "name": "string",
          "description": "string",
          "parameters": "object"
        }
      ]
    }
  ],
  "tool_config": {
    "function_calling_config": {
      "mode": "string"
    }
  }
}
```

**Response:**

```json
{
  "candidates": [
    {
      "content": {
        "parts": [
          {
            "text": "string",
            "functionCall": {
              "name": "string",
              "args": "object"
            },
            "functionResponse": {
              "name": "string",
              "response": "object"
            }
          }
        ]
      }
    }
  ]
}
```

#### Scenario: GenerateContentWithToolUse
- **GIVEN** Valid API key and request body with system instruction, contents, tools, and tool config
- **WHEN** POST /generateContent is called
- **THEN** 200 OK with response containing generated content and tool use results

### Requirement: PostMessages

The API SHALL support `POST /messages` to [partial spec — file too large to fully analyze (4 parts)] generate messages using the anthropic model with tool use capabilities.

**Request:**

```json
{
  "model": "string",
  "max_tokens": "number",
  "system": "string",
  "tools": [
    {
      "name": "string",
      "description": "string",
      "input_schema": "object"
    }
  ],
  "messages": [
    {
      "role": "string",
      "content": "string"
    }
  ]
}
```

**Response:**

```json
{
  "content": [
    {
      "type": "string",
      "text": "string",
      "tool_use_id": "string",
      "name": "string",
      "input": "object"
    }
  ],
  "stop_reason": "string"
}
```

#### Scenario: GenerateMessagesWithToolUse
- **GIVEN** Valid API key and request body with model, max tokens, system prompt, tools, and messages
- **WHEN** POST /messages is called
- **THEN** 200 OK with response containing generated messages and tool use results

### Requirement: PostApiChatAgent

The API SHALL support `POST /api/chat-agent` to [partial spec — file too large to fully analyze (4 parts)] run the chat agent to process messages and handle tool use.

**Request:**

```json
{
  "directory": "string",
  "messages": "array",
  "modelOverride": "string",
  "signal": "AbortSignal",
  "onToolStart": "function",
  "onToolEnd": "function"
}
```

**Response:**

```json
{
  "result": "object",
  "error": "string"
}
```

#### Scenario: RunChatAgentWithGeminiProvider
- **GIVEN** Valid Gemini API key and messages
- **WHEN** POST /api/chat-agent is called with Gemini provider
- **THEN** 200 OK with chat agent result
- **AND** Processes messages using Gemini provider
- **AND** Handles tool use callbacks

#### Scenario: RunChatAgentWithAnthropicProvider
- **GIVEN** Valid Anthropic API key and messages
- **WHEN** POST /api/chat-agent is called with Anthropic provider
- **THEN** 200 OK with chat agent result
- **AND** Processes messages using Anthropic provider
- **AND** Handles tool use callbacks

#### Scenario: RunChatAgentWithOpenaiProvider
- **GIVEN** Valid OpenAI API key and messages
- **WHEN** POST /api/chat-agent is called with OpenAI provider
- **THEN** 200 OK with chat agent result
- **AND** Processes messages using OpenAI provider
- **AND** Handles tool use callbacks

### Requirement: Postspecification

The API SHALL support `POST /api/generate` to generate openspec specification files from analysis results using llm.

**Request:**

```json
{
  "options": {
    "type": "object",
    "properties": {
      "configPath": {
        "type": "string",
        "description": "Path to the configuration file"
      },
      "outputPath": {
        "type": "string",
        "description": "Path to the output directory"
      }
    }
  }
}
```

**Response:**

```json
{
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "description": "Status of the generation process"
    },
    "specs": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Generated specification files"
      }
    },
    "logs": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Log messages from the generation process"
      }
    }
  }
}
```

#### Scenario: GenerateOpenspecFiles
- **GIVEN** Authenticated user with valid configuration
- **WHEN** POST /api/generate is called with options
- **THEN** 200 OK with generated specification files and logs
- **AND** Progress updates are sent during the process
- **AND** Generated ADRs are included in the response

#### Scenario: GenerateOpenspecFilesWithDefaultOptions
- **GIVEN** Authenticated user without options
- **WHEN** POST /api/generate is called without options
- **THEN** 200 OK with generated specification files and logs
- **AND** Default configuration is used
- **AND** Progress updates are sent during the process

### Requirement: CliCommandspecification

The API SHALL support `CLI Command openlore verify` to [partial spec — file too large to fully analyze (2 parts)] verify generated specs against actual source code to ensure accuracy.

**Request:**

```json
{
  "samples": "number",
  "threshold": "number",
  "files": "string[]",
  "domains": "string[]",
  "verbose": "boolean",
  "json": "boolean"
}
```

**Response:**

```json
{
  "results": "VerificationResult[]",
  "recommendation": "string",
  "confidenceScore": "number"
}
```

#### Scenario: BasicVerification
- **GIVEN** Generated specs exist and LLM API is configured
- **WHEN** openlore verify is run with default options
- **THEN** Verification report is generated with confidence score and recommendations

#### Scenario: DomainSpecificVerification
- **GIVEN** Multiple domains are defined in the codebase
- **WHEN** openlore verify --domains user,order is run
- **THEN** Only files from user and order domains are verified

#### Scenario: JsonOutput
- **GIVEN** Generated specs exist
- **WHEN** openlore verify --json is run
- **THEN** Verification results are output as JSON

### Requirement: Getspecrequirement

The API SHALL support `GET /api/specs/requirements` to retrieve spec requirements referenced in mapping.json.

**Request:**

```json
{
  "rootPath": "string (optional)"
}
```

**Response:**

```json
{
  "generatedAt": "string (optional)",
  "requirements": {
    "type": "object",
    "properties": {
      "requirementKey": {
        "type": "object",
        "properties": {
          "title": "string",
          "body": "string",
          "specFile": "string (optional)",
          "domain": "string (optional)",
          "service": "string (optional)"
        }
      }
    }
  }
}
```

#### Scenario: RetrieveSpecRequirements
- **GIVEN** A valid mapping.json file exists
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with the requirements object in the response body

#### Scenario: HandleMissingMapping.json
- **GIVEN** No mapping.json file exists
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with an empty requirements object in the response body

### Requirement: Postopenlorepipeline

The API SHALL support `POST /api/run` to run the full openlore pipeline: init → analyze → generate.

**Request:**

```json
{
  "options": {
    "type": "object",
    "properties": {
      "configPath": {
        "type": "string",
        "description": "Path to the configuration file"
      },
      "outputDir": {
        "type": "string",
        "description": "Directory to output the generated specifications"
      },
      "verbose": {
        "type": "boolean",
        "description": "Enable verbose logging"
      }
    }
  }
}
```

**Response:**

```json
{
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "description": "Status of the operation"
    },
    "message": {
      "type": "string",
      "description": "Additional message about the operation"
    },
    "data": {
      "type": "object",
      "description": "Generated specifications and related data"
    }
  }
}
```

#### Scenario: RunOpenLorePipeline
- **GIVEN** Authenticated user with valid configuration
- **WHEN** POST /api/run is called with valid options
- **THEN** 200 OK with status and generated specifications in response body
- **AND** Configuration file exists
- **AND** Output directory is writable

#### Scenario: RunOpenLorePipelineWithDefaultConfig
- **GIVEN** Authenticated user without configuration
- **WHEN** POST /api/run is called without options
- **THEN** 200 OK with status and generated specifications in response body
- **AND** Default configuration is used
- **AND** Output directory is writable

#### Scenario: RunOpenLorePipelineWithInvalidConfig
- **GIVEN** Authenticated user with invalid configuration
- **WHEN** POST /api/run is called with invalid options
- **THEN** 400 Bad Request with error message in response body
- **AND** Configuration file does not exist
- **AND** Output directory is not writable

### Requirement: FunctioncodebaseAnalysis

The API SHALL support `Function openloreAnalyze(options: AnalyzeApiOptions = {})` to run static analysis on the codebase. scans the repository, builds a dependency graph, and generates analysis artifacts. no llm involvement.

**Request:**

```json
{
  "options": {
    "rootPath": "string",
    "maxFiles": "number",
    "excludePatterns": "string[]",
    "includePatterns": "string[]",
    "force": "boolean",
    "outputPath": "string",
    "onProgress": "ProgressCallback"
  }
}
```

**Response:**

```json
{
  "repoMap": "object",
  "depGraph": "DependencyGraphResult",
  "artifacts": {
    "repoStructure": "RepoStructure",
    "summaryMarkdown": "string",
    "dependencyDiagram": "string",
    "llmContext": "LLMContext"
  },
  "duration": "number"
}
```

#### Scenario: AnalyzeWithExistingRecentAnalysis
- **GIVEN** Existing recent analysis artifacts
- **WHEN** openloreAnalyze is called without force option
- **THEN** Returns cached analysis results
- **AND** Does not perform new analysis
- **AND** Returns duration as time taken to load cached results

#### Scenario: AnalyzeWithForceOption
- **GIVEN** Existing analysis artifacts
- **WHEN** openloreAnalyze is called with force option
- **THEN** Performs new analysis
- **AND** Scans directory structure
- **AND** Builds dependency graph
- **AND** Generates analysis artifacts
- **AND** Returns new analysis results

#### Scenario: AnalyzeWithoutConfiguration
- **GIVEN** No openlore configuration
- **WHEN** openloreAnalyze is called
- **THEN** Throws error
- **AND** Error message indicates missing configuration

### Requirement: CliCommandcodebaseAnalysis

The API SHALL support `CLI Command openlore analyze` to [partial spec — file too large to fully analyze (3 parts)] run static analysis on the codebase without llm involvement to output repository map, dependency graph, and file significance scores.

**Request:**

```json
{
  "output": "string",
  "maxFiles": "number",
  "include": "string[]",
  "exclude": "string[]",
  "force": "boolean",
  "embed": "boolean",
  "reindexSpecs": "boolean"
}
```

**Response:**

```json
{
  "repoStructure": "object",
  "dependencyGraph": "object",
  "llmContext": "object",
  "refactorPriorities": "object",
  "duplicates": "object",
  "architectureMd": "string",
  "codebaseMd": "string"
}
```

#### Scenario: BasicAnalysis
- **GIVEN** A codebase with a openlore configuration
- **WHEN** The command 'openlore analyze' is executed
- **THEN** The analysis results are written to the default output directory
- **AND** Repository structure is analyzed
- **AND** Dependency graph is generated
- **AND** File significance scores are calculated

#### Scenario: CustomOutputDirectory
- **GIVEN** A codebase with a openlore configuration
- **WHEN** The command 'openlore analyze --output ./custom-analysis' is executed
- **THEN** The analysis results are written to the specified custom directory
- **AND** Repository structure is analyzed
- **AND** Dependency graph is generated
- **AND** File significance scores are calculated

#### Scenario: ReindexSpecs
- **GIVEN** A codebase with existing analysis and openlore configuration
- **WHEN** The command 'openlore analyze --reindex-specs' is executed
- **THEN** Only the OpenSpec specs are re-indexed into the vector index
- **AND** Full codebase analysis is skipped
- **AND** Vector index is updated with the latest specs

### Requirement: Functionopenlore

The API SHALL support `Function openloreInit(options: InitApiOptions = {})` to initialize openlore in a project directory.

**Request:**

```json
{
  "options": {
    "type": "object",
    "properties": {
      "InitApiOptions": {
        "type": "object"
      }
    }
  }
}
```

**Response:**

```json
{
  "InitResult": {
    "type": "object"
  }
}
```

#### Scenario: InitializeOpenLore
- **GIVEN** A project directory
- **WHEN** openloreInit is called with options
- **THEN** Returns a promise that resolves to InitResult
- **AND** Creates necessary directories and files
- **AND** Updates .gitignore if needed
- **AND** Generates default configuration if not present

### Requirement: Postspecverification

The API SHALL support `POST /api/verify` to verify generated specs against actual source code.

**Request:**

```json
{
  "rootPath": "string",
  "samples": "number",
  "threshold": "number",
  "provider": "string",
  "model": "string",
  "apiBase": "string",
  "sslVerify": "boolean",
  "openaiCompatBaseUrl": "string",
  "onProgress": "function"
}
```

**Response:**

```json
{
  "report": "object",
  "duration": "number"
}
```

#### Scenario: VerifySpecs
- **GIVEN** Valid openlore configuration, existing specs, and LLM API key
- **WHEN** POST /api/verify is called with valid parameters
- **THEN** 200 OK with verification report and duration
- **AND** Progress updates are sent via onProgress callback
- **AND** LLM logs are saved

#### Scenario: MissingConfiguration
- **GIVEN** No openlore configuration exists
- **WHEN** POST /api/verify is called
- **THEN** 400 Bad Request with error message

#### Scenario: MissingSpecs
- **GIVEN** No specs exist in the expected location
- **WHEN** POST /api/verify is called
- **THEN** 400 Bad Request with error message

#### Scenario: MissingLlmApiKey
- **GIVEN** No LLM API key is set in environment variables
- **WHEN** POST /api/verify is called
- **THEN** 400 Bad Request with error message

### Requirement: FunctionspecificationDriftDetection

The API SHALL support `Function openloreDrift` to detect specification drift in a project by analyzing changes between different versions of specifications and adrs (architecture decision records).

**Request:**

```json
{
  "options": {
    "type": "object",
    "properties": {
      "baseRef": {
        "type": "string",
        "description": "The base Git reference to compare against"
      },
      "headRef": {
        "type": "string",
        "description": "The head Git reference to compare"
      },
      "configPath": {
        "type": "string",
        "description": "Path to the configuration file"
      },
      "specDir": {
        "type": "string",
        "description": "Directory containing specifications"
      },
      "adrDir": {
        "type": "string",
        "description": "Directory containing ADRs"
      },
      "llmService": {
        "type": "object",
        "description": "LLM service configuration"
      }
    }
  }
}
```

**Response:**

```json
{
  "type": "object",
  "properties": {
    "gaps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "Path to the file with drift"
          },
          "domains": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Domains affected by the drift"
          },
          "severity": {
            "type": "string",
            "description": "Severity level of the drift"
          },
          "reason": {
            "type": "string",
            "description": "Reason for the drift"
          }
        }
      },
      "description": "List of detected specification gaps"
    },
    "staleSpecs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of stale specifications"
    },
    "uncoveredFiles": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "Path to the uncovered file"
          },
          "domains": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Domains affected by the uncovered file"
          },
          "severity": {
            "type": "string",
            "description": "Severity level of the uncovered file"
          }
        }
      },
      "description": "List of uncovered files"
    },
    "orphanedSpecs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of orphaned specifications"
    },
    "adrGaps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "adrId": {
            "type": "string",
            "description": "ID of the ADR with drift"
          },
          "domains": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Domains affected by the drift"
          },
          "severity": {
            "type": "string",
            "description": "Severity level of the drift"
          },
          "reason": {
            "type": "string",
            "description": "Reason for the drift"
          }
        }
      },
      "description": "List of detected ADR gaps"
    },
    "adrOrphaned": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of orphaned ADRs"
    }
  }
}
```

#### Scenario: DetectSpecificationDrift
- **GIVEN** A Git repository with specifications and ADRs
- **WHEN** openloreDrift is called with valid options
- **THEN** Returns a DriftResult object with detected gaps, stale specs, uncovered files, orphaned specs, ADR
gaps, and orphaned ADRs
- **AND** The function checks if the directory is a Git repository
- **AND** Reads the configuration file
- **AND** Gets the changed files between the base and head references
- **AND** Builds a map of specifications and ADRs
- **AND** Detects gaps, stale specs, uncovered files, orphaned specs, ADR gaps, and orphaned ADRs
- **AND** Enhances gaps with LLM classification if configured

#### Scenario: HandleInvalidGitReference
- **GIVEN** An invalid Git reference is provided
- **WHEN** openloreDrift is called with an invalid base or head reference
- **THEN** Throws an error indicating the invalid Git reference
- **AND** The function validates the Git references before proceeding

#### Scenario: HandleMissingConfigurationFile
- **GIVEN** The configuration file is missing
- **WHEN** openloreDrift is called with a missing configuration file
- **THEN** Throws an error indicating the missing configuration file
- **AND** The function checks for the existence of the configuration file

### Requirement: Functionvectorindex

The API SHALL support `Function runEmbedStep` to [partial spec — file too large to fully analyze (3 parts)] builds a semantic vector index for the codebase without involving llm.

**Request:**

```json
{
  "rootPath": "string",
  "outputPath": "string",
  "openloreConfig": "OpenLoreConfig | null",
  "force": "boolean",
  "llmContext": "LLMContext | null"
}
```

**Response:**

```json
{
  "status": "string",
  "message": "string",
  "vectorIndexPath": "string"
}
```

#### Scenario: BuildVectorIndexWithExistingLlmContext
- **GIVEN** Valid rootPath, outputPath, and llmContext
- **WHEN** runEmbedStep is called
- **THEN** Vector index is built successfully
- **AND** Function index is built with the provided LLM context
- **AND** Spec indexing is attempted

#### Scenario: BuildVectorIndexWithoutLlmContext
- **GIVEN** Valid rootPath and outputPath, but no llmContext
- **WHEN** runEmbedStep is called
- **THEN** Vector index is built using cached LLM context
- **AND** If cached LLM context is not available, the function exits early
- **AND** Spec indexing is attempted

#### Scenario: BuildVectorIndexWithEmbeddingServiceFromEnvironment
- **GIVEN** Valid rootPath, outputPath, and llmContext, with embedding service configured in environment
- **WHEN** runEmbedStep is called
- **THEN** Vector index is built using the embedding service from environment
- **AND** Function index is built with the provided LLM context
- **AND** Spec indexing is attempted

#### Scenario: BuildVectorIndexWithEmbeddingServiceFromConfig
- **GIVEN** Valid rootPath, outputPath, and llmContext, with embedding service configured in openloreConfig
- **WHEN** runEmbedStep is called
- **THEN** Vector index is built using the embedding service from config
- **AND** Function index is built with the provided LLM context
- **AND** Spec indexing is attempted

#### Scenario: BuildVectorIndexWithNoEmbeddingService
- **GIVEN** Valid rootPath, outputPath, and llmContext, with no embedding service configured
- **WHEN** runEmbedStep is called
- **THEN** Vector index build fails with an error message
- **AND** Function index is not built
- **AND** Spec indexing is not attempted

### Requirement: Functionspecvectorindex

The API SHALL support `Function runSpecIndexing` to [partial spec — file too large to fully analyze (3 parts)] indexes openspec specs into the vector index.

**Request:**

```json
{
  "rootPath": "string",
  "outputPath": "string",
  "openloreConfig": "OpenLoreConfig | null"
}
```

**Response:**

```json
{
  "status": "string",
  "message": "string",
  "specIndexPath": "string"
}
```

#### Scenario: IndexSpecsWithEmbeddingServiceFromEnvironment
- **GIVEN** Valid rootPath, outputPath, and specs directory exists, with embedding service configured in
environment
- **WHEN** runSpecIndexing is called
- **THEN** Specs are indexed successfully
- **AND** Spec index is built using the embedding service from environment

#### Scenario: IndexSpecsWithEmbeddingServiceFromConfig
- **GIVEN** Valid rootPath, outputPath, and specs directory exists, with embedding service configured in
openloreConfig
- **WHEN** runSpecIndexing is called
- **THEN** Specs are indexed successfully
- **AND** Spec index is built using the embedding service from config

#### Scenario: IndexSpecsWithNoEmbeddingService
- **GIVEN** Valid rootPath, outputPath, and specs directory exists, with no embedding service configured
- **WHEN** runSpecIndexing is called
- **THEN** Spec indexing is skipped silently

#### Scenario: IndexSpecsWithNoSpecsDirectory
- **GIVEN** Valid rootPath and outputPath, but no specs directory exists
- **WHEN** runSpecIndexing is called
- **THEN** Spec indexing is skipped with a warning message

#### Scenario: IndexSpecsWithErrorDuringIndexing
- **GIVEN** Valid rootPath, outputPath, and specs directory exists, but an error occurs during indexing
- **WHEN** runSpecIndexing is called
- **THEN** Spec indexing is skipped with an error message

### Requirement: Functionnode.jsVersion

The API SHALL support `Function checkNodeVersion` to [partial spec — file too large to fully analyze (2 parts)] check the node.js version against the minimum required version.

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: NodeVersionCheck
- **GIVEN** Node.js version is at least the minimum required version
- **WHEN** checkNodeVersion is called
- **THEN** Returns status 'ok' with the current Node.js version

#### Scenario: NodeVersionCheck
- **GIVEN** Node.js version is below the minimum required version
- **WHEN** checkNodeVersion is called
- **THEN** Returns status 'fail' with the current Node.js version and a fix suggestion

### Requirement: FunctiongitRepository

The API SHALL support `Function checkGit` to [partial spec — file too large to fully analyze (2 parts)] check if the project is a git repository and if git is available.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: GitCheck
- **GIVEN** The project is a Git repository and Git is available
- **WHEN** checkGit is called
- **THEN** Returns status 'ok' with a message indicating Git repository detected

#### Scenario: GitCheck
- **GIVEN** The project is not a Git repository
- **WHEN** checkGit is called
- **THEN** Returns status 'warn' with a message indicating no .git directory found and a fix suggestion

#### Scenario: GitCheck
- **GIVEN** The project is a Git repository but Git is not available
- **WHEN** checkGit is called
- **THEN** Returns status 'warn' with a message indicating .git found but git binary not on PATH and a fix
suggestion

### Requirement: FunctionopenloreConfiguration

The API SHALL support `Function checkConfig` to [partial spec — file too large to fully analyze (2 parts)] check if the openlore configuration file exists and is valid.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: ConfigCheck
- **GIVEN** The openlore configuration file exists and is valid
- **WHEN** checkConfig is called
- **THEN** Returns status 'ok' with a message indicating the configuration file exists and the project type

#### Scenario: ConfigCheck
- **GIVEN** The openlore configuration file exists but is invalid
- **WHEN** checkConfig is called
- **THEN** Returns status 'fail' with a message indicating the configuration file could not be parsed and a fix
suggestion

#### Scenario: ConfigCheck
- **GIVEN** The openlore configuration file does not exist
- **WHEN** checkConfig is called
- **THEN** Returns status 'warn' with a message indicating the configuration file not found and a fix
suggestion

### Requirement: FunctionanalysisArtifacts

The API SHALL support `Function checkAnalysis` to [partial spec — file too large to fully analyze (2 parts)] check if the analysis artifacts exist and are not stale.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: AnalysisCheck
- **GIVEN** The analysis artifacts exist and are not stale
- **WHEN** checkAnalysis is called
- **THEN** Returns status 'ok' with a message indicating the analysis artifacts exist and are fresh

#### Scenario: AnalysisCheck
- **GIVEN** The analysis artifacts exist but are stale
- **WHEN** checkAnalysis is called
- **THEN** Returns status 'warn' with a message indicating the analysis artifacts are stale and a fix
suggestion

#### Scenario: AnalysisCheck
- **GIVEN** The analysis artifacts do not exist
- **WHEN** checkAnalysis is called
- **THEN** Returns status 'warn' with a message indicating no analysis found and a fix suggestion

### Requirement: FunctionopenspecDirectory

The API SHALL support `Function checkOpenSpecDir` to [partial spec — file too large to fully analyze (2 parts)] check if the openspec directory exists.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: OpenspecDirectoryCheck
- **GIVEN** The OpenSpec directory exists
- **WHEN** checkOpenSpecDir is called
- **THEN** Returns status 'ok' with a message indicating the OpenSpec directory exists

#### Scenario: OpenspecDirectoryCheck
- **GIVEN** The OpenSpec directory does not exist
- **WHEN** checkOpenSpecDir is called
- **THEN** Returns status 'warn' with a message indicating the OpenSpec directory not found and a fix
suggestion

### Requirement: FunctionllmProvider

The API SHALL support `Function checkLLMProvider` to [partial spec — file too large to fully analyze (2 parts)] check if an llm provider is configured.

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: LlmProviderCheck
- **GIVEN** Anthropic API key is set
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'ok' with a message indicating ANTHROPIC_API_KEY is set

#### Scenario: LlmProviderCheck
- **GIVEN** OpenAI API key is set
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'ok' with a message indicating OPENAI_API_KEY is set

#### Scenario: LlmProviderCheck
- **GIVEN** Gemini API key is set
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'ok' with a message indicating GEMINI_API_KEY is set

#### Scenario: LlmProviderCheck
- **GIVEN** Claude Code CLI is available
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'ok' with a message indicating claude CLI detected

#### Scenario: LlmProviderCheck
- **GIVEN** OPENLORE_API_BASE is set but no API key
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'warn' with a message indicating OPENLORE_API_BASE is set but no API key

#### Scenario: LlmProviderCheck
- **GIVEN** No LLM provider is configured
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'fail' with a message indicating no LLM provider configured and a fix suggestion

### Requirement: FunctiondiskSpace

The API SHALL support `Function checkDiskSpace` to [partial spec — file too large to fully analyze (2 parts)] check the available disk space.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: DiskSpaceCheck
- **GIVEN** Available disk space is sufficient
- **WHEN** checkDiskSpace is called
- **THEN** Returns status 'ok' with a message indicating the available disk space

#### Scenario: DiskSpaceCheck
- **GIVEN** Available disk space is low
- **WHEN** checkDiskSpace is called
- **THEN** Returns status 'warn' with a message indicating the available disk space is low and a fix suggestion

#### Scenario: DiskSpaceCheck
- **GIVEN** Available disk space is critically low
- **WHEN** checkDiskSpace is called
- **THEN** Returns status 'fail' with a message indicating the available disk space is critically low and a fix
suggestion

#### Scenario: DiskSpaceCheck
- **GIVEN** df command is not available
- **WHEN** checkDiskSpace is called
- **THEN** Returns status 'ok' with a message indicating the check was skipped

### Requirement: CliCommandenvironment

The API SHALL support `CLI Command openlore doctor` to [partial spec — file too large to fully analyze (2 parts)] check your environment and configuration for common issues

#### Scenario: RunAllChecks
- **GIVEN** User has openlore installed
- **WHEN** User runs 'openlore doctor'
- **THEN** All checks are performed and results are displayed
- **AND** Node.js version is checked
- **AND** Git repository is detected
- **AND** openlore configuration is checked
- **AND** Analysis artifacts freshness is checked
- **AND** OpenSpec directory presence is checked
- **AND** LLM provider configuration is checked
- **AND** Available disk space is checked

#### Scenario: OutputResultsAsJson
- **GIVEN** User has openlore installed
- **WHEN** User runs 'openlore doctor --json'
- **THEN** All checks are performed and results are displayed as JSON

### Requirement: Climcp

The API SHALL support `CLI mcp` to main command for the model-centric programming (mcp) tool

#### Scenario: StartMcpServer
- **GIVEN** MCP tool is installed
- **WHEN** User runs 'mcp' command
- **THEN** MCP server starts and provides various subcommands

### Requirement: CliMcpOrient

The API SHALL support `CLI mcp orient` to orient the codebase by analyzing and suggesting improvements

**Request:**

```json
{
  "directory": "string",
  "file": "string",
  "domain": "string"
}
```

**Response:**

```json
{
  "recommendations": "array",
  "score": "number",
  "reason": "string"
}
```

#### Scenario: OrientCodebase
- **GIVEN** Valid directory and configuration
- **WHEN** User runs 'mcp orient'
- **THEN** Returns recommendations for improving the codebase

### Requirement: CliMcpAnalyzeCodebase

The API SHALL support `CLI mcp analyze-codebase` to analyze the codebase for refactoring opportunities

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "refactoringOpportunities": "array",
  "analysisResults": "object"
}
```

#### Scenario: AnalyzeCodebase
- **GIVEN** Valid directory
- **WHEN** User runs 'mcp analyze-codebase'
- **THEN** Returns refactoring opportunities and analysis results

### Requirement: Cliarchitecture

The API SHALL support `CLI mcp get-architecture-overview` to get an overview of the architecture of the codebase

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "architectureOverview": "object"
}
```

#### Scenario: GetArchitectureOverview
- **GIVEN** Valid directory
- **WHEN** User runs 'mcp get-architecture-overview'
- **THEN** Returns an overview of the architecture

### Requirement: Clirefactoring

The API SHALL support `CLI mcp get-refactor-report` to get a report on refactoring opportunities

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "refactorReport": "object"
}
```

#### Scenario: GetRefactorReport
- **GIVEN** Valid directory
- **WHEN** User runs 'mcp get-refactor-report'
- **THEN** Returns a report on refactoring opportunities

### Requirement: Clicallgraph

The API SHALL support `CLI mcp get-call-graph` to get the call graph of the codebase

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "callGraph": "object"
}
```

#### Scenario: GetCallGraph
- **GIVEN** Valid directory
- **WHEN** User runs 'mcp get-call-graph'
- **THEN** Returns the call graph of the codebase

### Requirement: Clifunction

The API SHALL support `CLI mcp get-signatures` to get the signatures of functions in the codebase

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "signatures": "array"
}
```

#### Scenario: GetSignatures
- **GIVEN** Valid directory
- **WHEN** User runs 'mcp get-signatures'
- **THEN** Returns the signatures of functions in the codebase

### Requirement: Clisubgraph

The API SHALL support `CLI mcp get-subgraph` to get a subgraph of the codebase

**Request:**

```json
{
  "directory": "string",
  "root": "string"
}
```

**Response:**

```json
{
  "subgraph": "object"
}
```

#### Scenario: GetSubgraph
- **GIVEN** Valid directory and root
- **WHEN** User runs 'mcp get-subgraph'
- **THEN** Returns a subgraph of the codebase

### Requirement: Cliexecutionpath

The API SHALL support `CLI mcp trace-execution-path` to trace the execution path of a function

**Request:**

```json
{
  "directory": "string",
  "function": "string"
}
```

**Response:**

```json
{
  "executionPath": "array"
}
```

#### Scenario: TraceExecutionPath
- **GIVEN** Valid directory and function
- **WHEN** User runs 'mcp trace-execution-path'
- **THEN** Returns the execution path of the function

### Requirement: Climapping

The API SHALL support `CLI mcp get-mapping` to get the mapping of the codebase

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "mapping": "object"
}
```

#### Scenario: GetMapping
- **GIVEN** Valid directory
- **WHEN** User runs 'mcp get-mapping'
- **THEN** Returns the mapping of the codebase

### Requirement: Cliimpactanalysis

The API SHALL support `CLI mcp analyze-impact` to analyze the impact of changes in the codebase

**Request:**

```json
{
  "directory": "string",
  "changes": "array"
}
```

**Response:**

```json
{
  "impactAnalysis": "object"
}
```

#### Scenario: AnalyzeImpact
- **GIVEN** Valid directory and changes
- **WHEN** User runs 'mcp analyze-impact'
- **THEN** Returns the impact analysis of the changes

### Requirement: Functionanalysisdata

The API SHALL support `Function loadAnalysis(analysisPath: string)` to [partial spec — file too large to fully analyze (2 parts)] load analysis data from disk

**Request:**

```json
{
  "analysisPath": "string"
}
```

**Response:**

```json
{
  "repoStructure": "RepoStructure",
  "llmContext": "LLMContext",
  "depGraph": "DependencyGraphResult",
  "age": "number",
  "timestamp": "string"
}
```

#### Scenario: LoadAnalysisData
- **GIVEN** Valid analysis path
- **WHEN** Function is called
- **THEN** Returns analysis data including repo structure, LLM context, dependency graph, age, and timestamp
- **AND** If analysis path is invalid, returns null

### Requirement: Functionuserconfirmation

The API SHALL support `Function promptConfirmation(message: string, autoYes: boolean)` to [partial spec — file too large to fully analyze (2 parts)] prompt user for confirmation

**Request:**

```json
{
  "message": "string",
  "autoYes": "boolean"
}
```

**Response:**

```json
{
  "confirmation": "boolean"
}
```

#### Scenario: PromptUserForConfirmation
- **GIVEN** Message and autoYes flag
- **WHEN** Function is called
- **THEN** Returns true if autoYes is true or user confirms
- **AND** Returns false if autoYes is false and user does not confirm

### Requirement: Functionllmservice

The API SHALL support `Function verifyApiConnectivity(llm: LLMService)` to [partial spec — file too large to fully analyze (2 parts)] verify llm api connectivity

**Request:**

```json
{
  "llm": "LLMService"
}
```

**Response:**

```json
{
  "success": "boolean"
}
```

#### Scenario: VerifyLlmApiConnectivity
- **GIVEN** LLM service instance
- **WHEN** Function is called
- **THEN** Returns true if API connectivity is verified
- **AND** Returns false if API connectivity verification fails

### Requirement: CliCommandopenspec

The API SHALL support `CLI Command openlore generate` to [partial spec — file too large to fully analyze (2 parts)] generate openspec specification files from analysis results using llm. outputs to openspec/specs/ directory in standard openspec format.

**Request:**

```json
{
  "analysis": "string (path to existing analysis)",
  "model": "string (LLM model to use for generation)",
  "dryRun": "boolean (show what would be generated without writing files)",
  "domains": "string[] (only generate specific domains)",
  "reanalyze": "boolean (force fresh analysis even if recent exists)",
  "merge": "boolean (use merge strategy for existing specs)",
  "noOverwrite": "boolean (skip any existing spec files)",
  "yes": "boolean (skip confirmation prompts)",
  "outputDir": "string (override openspec output location)",
  "adr": "boolean (generate Architecture Decision Records alongside specs)",
  "adrOnly": "boolean (only generate ADRs)",
  "force": "boolean (force regeneration from scratch, ignoring any cached stage results)"
}
```

**Response:**

```json
{
  "filesWritten": "string[] (list of files written)",
  "filesMerged": "string[] (list of files merged)",
  "filesSkipped": "string[] (list of files skipped)",
  "filesBackedUp": "string[] (list of files backed up)",
  "configUpdated": "boolean (whether config.yaml was updated)",
  "warnings": "string[] (list of warnings)",
  "validationErrors": "string[] (list of validation errors)",
  "nextSteps": "string[] (list of next steps)",
  "totalTime": "string (total time taken)",
  "reportPath": "string (path to the generation report)"
}
```

#### Scenario: GenerateSpecsWithDefaultSettings
- **GIVEN** A project with existing analysis and LLM API key configured
- **WHEN** openlore generate is executed
- **THEN** OpenSpec files are generated in the openspec/specs/ directory
- **AND** The command shows a summary of the generated files
- **AND** The command suggests next steps

#### Scenario: DryRunGeneration
- **GIVEN** A project with existing analysis and LLM API key configured
- **WHEN** openlore generate --dry-run is executed
- **THEN** The command shows what would be generated without writing files
- **AND** The command lists the domains to be generated
- **AND** The command shows the output paths for the specs

#### Scenario: GenerateSpecsForSpecificDomains
- **GIVEN** A project with existing analysis and LLM API key configured
- **WHEN** openlore generate --domains auth,api,database is executed
- **THEN** OpenSpec files are generated only for the specified domains
- **AND** The command shows a summary of the generated files
- **AND** The command suggests next steps

#### Scenario: MergeWithExistingSpecs
- **GIVEN** A project with existing analysis, LLM API key configured, and existing OpenSpec files
- **WHEN** openlore generate --merge is executed
- **THEN** The command merges the generated specs with the existing ones
- **AND** The command shows a summary of the merged files
- **AND** The command suggests next steps

#### Scenario: SkipExistingSpecFiles
- **GIVEN** A project with existing analysis, LLM API key configured, and existing OpenSpec files
- **WHEN** openlore generate --no-overwrite is executed
- **THEN** The command skips any existing spec files and only generates new ones
- **AND** The command shows a summary of the generated files
- **AND** The command suggests next steps

#### Scenario: GenerateAdrsAlongsideSpecs
- **GIVEN** A project with existing analysis and LLM API key configured
- **WHEN** openlore generate --adr is executed
- **THEN** The command generates OpenSpec files and Architecture Decision Records
- **AND** The command shows a summary of the generated files and ADRs
- **AND** The command suggests next steps

#### Scenario: OnlyGenerateAdrs
- **GIVEN** A project with existing analysis and LLM API key configured
- **WHEN** openlore generate --adr-only is executed
- **THEN** The command only generates Architecture Decision Records
- **AND** The command shows a summary of the generated ADRs
- **AND** The command suggests next steps

#### Scenario: ForceRegenerationFromScratch
- **GIVEN** A project with existing analysis and LLM API key configured
- **WHEN** openlore generate --force is executed
- **THEN** The command forces full regeneration from scratch, ignoring any cached stage results
- **AND** The command shows a summary of the generated files
- **AND** The command suggests next steps

### Requirement: Functionrunmetadata

The API SHALL support `Function saveRunMetadata(rootPath: string, metadata: RunMetadata)` to [partial spec — file too large to fully analyze (2 parts)] save run metadata to disk

**Request:**

```json
{
  "rootPath": "string",
  "metadata": "RunMetadata"
}
```

#### Scenario: SaveRunMetadata
- **GIVEN** Valid root path and metadata
- **WHEN** Function is called
- **THEN** Metadata is saved to disk

### Requirement: FunctionDisplayBanner

The API SHALL support `Function displayBanner(projectName: string, projectType: string, rootPath: string)` to [partial spec — file too large to fully analyze (2 parts)] display the pipeline banner

**Request:**

```json
{
  "projectName": "string",
  "projectType": "string",
  "rootPath": "string"
}
```

#### Scenario: DisplayBanner
- **GIVEN** Project details
- **WHEN** Function is called
- **THEN** Banner is displayed in console

### Requirement: FunctionDisplayCompletionBanner

The API SHALL support `Function displayCompletionBanner()` to [partial spec — file too large to fully analyze (2 parts)] display the completion banner

#### Scenario: DisplayCompletionBanner
- **GIVEN** Successful completion
- **WHEN** Function is called
- **THEN** Completion banner is displayed in console

### Requirement: CliCommandspecificationGenerationPipeline

The API SHALL support `CLI Command openlore run` to [partial spec — file too large to fully analyze (2 parts)] run the full openlore pipeline (init → analyze → generate)

**Request:**

```json
{
  "force": "boolean",
  "reanalyze": "boolean",
  "model": "string",
  "dryRun": "boolean",
  "yes": "boolean",
  "maxFiles": "number",
  "adr": "boolean"
}
```

**Response:**

```json
{
  "status": "string",
  "message": "string",
  "metadata": {
    "version": "string",
    "timestamp": "string",
    "duration": "number",
    "steps": {
      "init": {
        "status": "string",
        "reason": "string"
      },
      "analyze": {
        "status": "string",
        "reason": "string",
        "filesAnalyzed": "number"
      },
      "generate": {
        "status": "string",
        "specsGenerated": "number"
      }
    },
    "result": "string",
    "error": "string"
  }
}
```

#### Scenario: FullPipelineExecution
- **GIVEN** A project directory with code to analyze
- **WHEN** openlore run is executed
- **THEN** The pipeline completes successfully with generated specifications
- **AND** Initialization step creates configuration if needed
- **AND** Analysis step processes codebase and builds artifacts
- **AND** Generation step produces OpenSpec specifications

#### Scenario: DryRun
- **GIVEN** A project directory with code to analyze
- **WHEN** openlore run --dry-run is executed
- **THEN** The pipeline shows what would be done without making changes
- **AND** No files are modified
- **AND** Estimated cost is displayed
- **AND** Detailed preview of generation steps is shown

#### Scenario: ForceReinitialization
- **GIVEN** An existing openlore configuration
- **WHEN** openlore run --force is executed
- **THEN** The pipeline reinitializes the configuration
- **AND** Existing configuration is overwritten
- **AND** New analysis is performed
- **AND** Fresh specifications are generated

### Requirement: FunctionerrorHandling

The API SHALL support `Function sanitizeErrorMessage(msg: string): string` to sanitizes error messages to remove sensitive information.

#### Scenario: SanitizeErrorMessage
- **GIVEN** An error message containing sensitive information
- **WHEN** The sanitizeErrorMessage function is called
- **THEN** The error message is sanitized and returned

### Requirement: FunctionfileSystem

The API SHALL support `Function safePath(rootPath: string, userPath: string): string | null` to ensures a user-provided path is safe and within the root path.

#### Scenario: ValidateSafePath
- **GIVEN** A root path and a user-provided path
- **WHEN** The safePath function is called
- **THEN** The function returns the resolved path if it is safe, otherwise null

### Requirement: CommandcliCommand

The API SHALL support `Command viewCommand` to configures and runs the view command for the cli application.

#### Scenario: RunViewCommand
- **GIVEN** A CLI command line with the 'view' command
- **WHEN** The viewCommand is executed
- **THEN** The server is configured and the view command is processed

### Requirement: FunctionserverConfiguration

The API SHALL support `Function configureServer(devServer)` to configures the development server with various settings and handlers.

#### Scenario: ConfigureServer
- **GIVEN** A development server instance
- **WHEN** The configureServer function is called
- **THEN** The server is configured with necessary settings and handlers

### Requirement: CliCommandprojectConfiguration

The API SHALL support `CLI Command openlore init` to initialize openlore in the current project by detecting project type, creating configuration, and preparing for analysis.

**Request:**

```json
{
  "options": {
    "force": "boolean",
    "openspec-path": "string"
  }
}
```

**Response:**

```json
{
  "status": "string",
  "configPath": "string",
  "openspecPath": "string",
  "projectType": "string"
}
```

#### Scenario: InitializeWithDefaults
- **GIVEN** Current directory is a valid project root
- **WHEN** openlore init is executed
- **THEN** Creates .openlore/config.json and openspec/ directory structure
- **AND** Detects project type
- **AND** Updates .gitignore if needed
- **AND** Outputs initialization summary

#### Scenario: OverwriteExistingConfig
- **GIVEN** Existing .openlore/config.json exists
- **WHEN** openlore init --force is executed
- **THEN** Overwrites existing configuration
- **AND** Preserves existing OpenSpec setup if present
- **AND** Updates .gitignore if needed

#### Scenario: CustomOpenspecPath
- **GIVEN** Current directory is a valid project root
- **WHEN** openlore init --openspec-path ./docs/specs is executed
- **THEN** Creates configuration with custom OpenSpec path
- **AND** Creates directory structure at specified path
- **AND** Updates configuration accordingly

### Requirement: Commandspecificationverification

The API SHALL support `Command verify` to [partial spec — file too large to fully analyze (2 parts)] verify the accuracy of generated specifications against the actual codebase.

**Request:**

```json
{
  "files": "string[]",
  "domains": "string[]",
  "json": "boolean"
}
```

**Response:**

```json
{
  "report": "VerificationReport",
  "results": "VerificationResult[]"
}
```

#### Scenario: VerifySpecifications
- **GIVEN** A set of generated specifications and corresponding code files
- **WHEN** The verify command is executed with file paths or domains
- **THEN** A verification report is generated and displayed
- **AND** Individual file verification results are shown with scores and feedback
- **AND** A summary report is displayed with overall confidence and recommendations

### Requirement: FunctionSeverityLabel

The API SHALL support `Function severityLabel(severity: DriftSeverity): string` to [partial spec — file too large to fully analyze (2 parts)] converts a driftseverity enum value to a human-readable label.

**Request:**

```json
{
  "severity": "DriftSeverity"
}
```

**Response:**

```json
{
  "label": "string"
}
```

#### Scenario: ConvertErrorSeverity
- **GIVEN** A DriftSeverity value of 'error'
- **WHEN** The function is called with 'error'
- **THEN** Returns the string 'ERROR'

#### Scenario: ConvertWarningSeverity
- **GIVEN** A DriftSeverity value of 'warning'
- **WHEN** The function is called with 'warning'
- **THEN** Returns the string 'WARNING'

#### Scenario: ConvertInfoSeverity
- **GIVEN** A DriftSeverity value of 'info'
- **WHEN** The function is called with 'info'
- **THEN** Returns the string 'INFO'

### Requirement: FunctionSeverityIcon

The API SHALL support `Function severityIcon(severity: DriftSeverity): string` to [partial spec — file too large to fully analyze (2 parts)] converts a driftseverity enum value to a corresponding icon.

**Request:**

```json
{
  "severity": "DriftSeverity"
}
```

**Response:**

```json
{
  "icon": "string"
}
```

#### Scenario: GetErrorIcon
- **GIVEN** A DriftSeverity value of 'error'
- **WHEN** The function is called with 'error'
- **THEN** Returns the string '✗'

#### Scenario: GetWarningIcon
- **GIVEN** A DriftSeverity value of 'warning'
- **WHEN** The function is called with 'warning'
- **THEN** Returns the string '⚠'

#### Scenario: GetInfoIcon
- **GIVEN** A DriftSeverity value of 'info'
- **WHEN** The function is called with 'info'
- **THEN** Returns the string '→'

### Requirement: FunctionKindLabel

The API SHALL support `Function kindLabel(kind: string): string` to [partial spec — file too large to fully analyze (2 parts)] converts a drift issue kind to a human-readable label.

**Request:**

```json
{
  "kind": "string"
}
```

**Response:**

```json
{
  "label": "string"
}
```

#### Scenario: ConvertGapKind
- **GIVEN** A kind value of 'gap'
- **WHEN** The function is called with 'gap'
- **THEN** Returns the string 'gap'

#### Scenario: ConvertStaleKind
- **GIVEN** A kind value of 'stale'
- **WHEN** The function is called with 'stale'
- **THEN** Returns the string 'stale'

#### Scenario: ConvertUncoveredKind
- **GIVEN** A kind value of 'uncovered'
- **WHEN** The function is called with 'uncovered'
- **THEN** Returns the string 'uncovered'

#### Scenario: ConvertOrphanedSpecKind
- **GIVEN** A kind value of 'orphaned-spec'
- **WHEN** The function is called with 'orphaned-spec'
- **THEN** Returns the string 'orphaned'

#### Scenario: ConvertAdrGapKind
- **GIVEN** A kind value of 'adr-gap'
- **WHEN** The function is called with 'adr-gap'
- **THEN** Returns the string 'adr-gap'

#### Scenario: ConvertAdrOrphanedKind
- **GIVEN** A kind value of 'adr-orphaned'
- **WHEN** The function is called with 'adr-orphaned'
- **THEN** Returns the string 'adr-orphaned'

#### Scenario: ConvertUnknownKind
- **GIVEN** An unknown kind value
- **WHEN** The function is called with an unknown kind
- **THEN** Returns the kind value as-is

### Requirement: FunctionDisplayIssue

The API SHALL support `Function displayIssue(issue: DriftIssue, verbose: boolean): void` to [partial spec — file too large to fully analyze (2 parts)] displays a drift issue in the console.

**Request:**

```json
{
  "issue": "DriftIssue",
  "verbose": "boolean"
}
```

#### Scenario: DisplayErrorIssue
- **GIVEN** A DriftIssue with severity 'error'
- **WHEN** The function is called with the issue and verbose=false
- **THEN** Displays the issue with error icon and label, including the message

#### Scenario: DisplayWarningIssueVerbosely
- **GIVEN** A DriftIssue with severity 'warning'
- **WHEN** The function is called with the issue and verbose=true
- **THEN** Displays the issue with warning icon and label, including the message

#### Scenario: DisplayInfoIssue
- **GIVEN** A DriftIssue with severity 'info'
- **WHEN** The function is called with the issue and verbose=false
- **THEN** Displays the issue with info icon and label, excluding the message

### Requirement: Functiondriftresult

The API SHALL support `Function displaySummary(result: DriftResult): void` to [partial spec — file too large to fully analyze (2 parts)] displays a summary of drift issues in the console.

**Request:**

```json
{
  "result": "DriftResult"
}
```

#### Scenario: DisplaySummaryWithGaps
- **GIVEN** A DriftResult with gaps in the summary
- **WHEN** The function is called with the result
- **THEN** Displays the summary with the number of gaps

#### Scenario: DisplaySummaryWithStaleIssues
- **GIVEN** A DriftResult with stale issues in the summary
- **WHEN** The function is called with the result
- **THEN** Displays the summary with the number of stale issues

#### Scenario: DisplaySummaryWithNoIssues
- **GIVEN** A DriftResult with no issues in the summary
- **WHEN** The function is called with the result
- **THEN** Displays 'No issues found' in the summary

### Requirement: FunctionInstallPreCommitHook

The API SHALL support `Function installPreCommitHook(rootPath: string): Promise<void>` to [partial spec — file too large to fully analyze (2 parts)] installs a pre-commit hook to check for spec drift.

**Request:**

```json
{
  "rootPath": "string"
}
```

#### Scenario: InstallHookInAGitRepository
- **GIVEN** A rootPath pointing to a git repository
- **WHEN** The function is called with the rootPath
- **THEN** Installs the pre-commit hook and displays a success message

#### Scenario: InstallHookInANonGitRepository
- **GIVEN** A rootPath not pointing to a git repository
- **WHEN** The function is called with the rootPath
- **THEN** Displays an error message and exits with code 1

#### Scenario: AppendToExistingHook
- **GIVEN** A rootPath with an existing pre-commit hook
- **WHEN** The function is called with the rootPath
- **THEN** Appends the openlore drift check to the existing hook

### Requirement: FunctionUninstallPreCommitHook

The API SHALL support `Function uninstallPreCommitHook(rootPath: string): Promise<void>` to [partial spec — file too large to fully analyze (2 parts)] uninstalls the openlore drift check from the pre-commit hook.

**Request:**

```json
{
  "rootPath": "string"
}
```

#### Scenario: UninstallFromExistingHook
- **GIVEN** A rootPath with a pre-commit hook containing openlore drift check
- **WHEN** The function is called with the rootPath
- **THEN** Removes the openlore drift check from the hook and displays a success message

#### Scenario: UninstallFromNonExistentHook
- **GIVEN** A rootPath without a pre-commit hook
- **WHEN** The function is called with the rootPath
- **THEN** Displays a warning message

#### Scenario: DeleteEmptyHookFile
- **GIVEN** A rootPath with a pre-commit hook that only contains openlore drift check
- **WHEN** The function is called with the rootPath
- **THEN** Deletes the hook file and displays a success message

### Requirement: CliCommandspecdrift

The API SHALL support `CLI Command openlore drift` to [partial spec — file too large to fully analyze (2 parts)] detect spec drift: find code changes not reflected in specs

**Response:**

```json
{
  "timestamp": "string",
  "baseRef": "string",
  "totalChangedFiles": "number",
  "specRelevantFiles": "number",
  "issues": "DriftIssue[]",
  "summary": {
    "gaps": "number",
    "stale": "number",
    "uncovered": "number",
    "orphanedSpecs": "number",
    "adrGaps": "number",
    "adrOrphaned": "number",
    "total": "number"
  },
  "hasDrift": "boolean",
  "duration": "number",
  "mode": "string"
}
```

#### Scenario: DetectDriftAgainstMainBranch
- **GIVEN** A git repository with code changes
- **WHEN** openlore drift is executed
- **THEN** A report of spec drift issues is generated

#### Scenario: CompareAgainstDevelopBranch
- **GIVEN** A git repository with code changes
- **WHEN** openlore drift --base develop is executed
- **THEN** A report of spec drift issues is generated

#### Scenario: OutputJsonForCiIntegration
- **GIVEN** A git repository with code changes
- **WHEN** openlore drift --json is executed
- **THEN** A JSON report of spec drift issues is output

#### Scenario: OnlyFailOnErrorLevelDrift
- **GIVEN** A git repository with code changes
- **WHEN** openlore drift --fail-on error is executed
- **THEN** The command exits with a non-zero status only if error-level drift is detected

#### Scenario: UseLlmForSemanticAnalysis
- **GIVEN** A git repository with code changes and LLM API key configured
- **WHEN** openlore drift --use-llm is executed
- **THEN** A report of spec drift issues with semantic analysis is generated

#### Scenario: InstallAsPreCommitHook
- **GIVEN** A git repository
- **WHEN** openlore drift --install-hook is executed
- **THEN** A pre-commit hook is installed for drift detection

#### Scenario: RemovePreCommitHook
- **GIVEN** A git repository with a pre-commit hook installed
- **WHEN** openlore drift --uninstall-hook is executed
- **THEN** The pre-commit hook is removed

### Requirement: DecisionApiWritesUseAtomicCompareAndSwap

The decision-writing API functions (`openloreRecordDecision`, `openloreConsolidateDecisions`,
`openloreSyncDecisions`) SHALL persist through the shared atomic compare-and-swap store path
(`updateDecisionStore`), so a write committed concurrently by another path (e.g. the MCP
`record_decision` handler or a background consolidation) is never silently clobbered.
`openloreRecordDecision` SHALL derive the decision id from the committed store's `sessionId`
so repeated records within a session deduplicate correctly.

#### Scenario: Concurrent API record and MCP record lose no decision
- **GIVEN** an `openloreRecordDecision` call and a concurrent `record_decision` against the same store
- **WHEN** both complete
- **THEN** the persisted store contains both decisions — neither write is lost
