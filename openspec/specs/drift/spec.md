# Drift Specification

> Generated by openlore v1.0.0 on 2026-04-05 10:50
> Source files: src/core/drift/git-diff.ts, src/core/drift/spec-mapper.ts, src/core/drift/drift-detector.ts, src/types/index.ts, src/core/decisions/memory-store.ts, src/core/services/mcp-handlers/memory.ts, src/core/services/mcp-handlers/orient.ts, src/core/services/tool-dispatch.ts, src/cli/commands/mcp.ts, src/core/services/mcp-handlers/reversals.ts, src/core/services/config-manager.ts, src/core/services/mcp-handlers/spec-store.ts, src/cli/commands/spec-store.ts, src/core/services/mcp-handlers/tool-contract.ts, src/core/services/mcp-handlers/live-data/tool-driver.ts, src/cli/install/index.ts, src/cli/install/adapters/claude-code.ts, src/cli/install/adapters/cursor.ts, src/cli/commands/connect.ts, src/constants.ts, src/cli/commands/serve.ts

## Purpose

Detects and analyzes specification drift between code changes and documentation.

## Entities

### GitDiffOptions

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Configuration options for generating git
diffs

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root directory of the git repository |
| baseRef | string | Git reference to compare against |
| pathFilter | string[] | Optional paths to include in the diff |
| includeUnstaged | boolean | Whether to include unstaged changes |

### GitDiffResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Result of a git diff operation

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| resolvedBase | string | The actual base reference used |
| files | ChangedFile[] | List of changed files |
| hasUnstagedChanges | boolean | Whether unstaged changes were included |
| currentBranch | string | Name of the current git branch |

**Relationships:**

- contains ChangedFile (GitDiffResult contains multiple ChangedFile entries)

### ChangedFile

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a file that has changed in a
git repository

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Path to the file |
| status | string | Change status (added, modified, deleted, renamed) |
| oldPath | string | Original path if file was renamed |
| isTest | boolean | Whether the file is a test file |
| isConfig | boolean | Whether the file is a configuration file |
| isGenerated | boolean | Whether the file is generated |
| extension | string | File extension |

### GitDiff

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the integration with Git to
detect changes in files between the working tree and a base reference.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | The root directory of the Git repository |
| baseRef | string | The base reference (branch, commit, or tag) to compare against |
| includeUnstaged | boolean | Whether to include unstaged changes in the diff |
| maxChars | number | The maximum number of characters to include in the diff output |

**Relationships:**

- has-many ChangedFile (GitDiff can produce multiple ChangedFile entities)

### SpecMapping

Represents the mapping between a domain and its associated source files and specifications.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| domain | string | The domain name |
| specPath | string | Path to the specification file |
| declaredSourceFiles | string[] | Source files explicitly declared in the spec header |
| inferredSourceFiles | string[] | Source files inferred from the spec content |
| allSourceFiles | string[] | Combined list of declared and inferred source files |
| requirements | string[] | List of requirements extracted from the spec |
| entities | string[] | List of entities extracted from the spec |

### SpecMap

Represents the bidirectional mapping between domains and source files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| byDomain | Map<string, SpecMapping> | Mapping from domain names to their specifications |
| byFile | Map<string, string[]> | Mapping from source files to their associated domains |
| domainCount | number | Number of domains in the mapping |
| totalMappedFiles | number | Total number of source files mapped |

**Relationships:**

- contains SpecMapping (SpecMap contains multiple SpecMapping instances)

### RepoStructureDomain

Represents a domain in the repository structure.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | The domain name |
| files | string[] | List of files associated with the domain |
| suggestedSpecPath | string | Suggested path for the specification file |

### DriftDetector

Detects discrepancies between code changes and specification updates, identifying gaps, stale specs,
uncovered files, and orphaned specifications.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| options | DriftDetectorOptions | Configuration options for drift detection |
| changedFiles | ChangedFile[] | List of files that have been modified |
| specMap | SpecMap | Mapping of specifications to their respective domains and files |
| adrMap | ADRMap | Mapping of Architecture Decision Records (ADRs) to their respective domains |
| changedSpecDomains | Set<string> | Set of domain names whose specifications were changed |
| openspecRelPath | string | Relative path to the OpenSpec directory |
| llm | LLM | Language model used for enhanced drift detection |
| rootPath | string | Root path of the project |
| baseRef | string | Base reference for comparison |
| maxLlmCalls | number | Maximum number of LLM calls allowed |

**Relationships:**

- uses ChangedFile (DriftDetector processes ChangedFile objects to detect drift)
- uses SpecMap (DriftDetector uses SpecMap to map specifications to domains and files)
- uses ADRMap (DriftDetector uses ADRMap to map Architecture Decision Records to domains)
- uses LLM (DriftDetector optionally uses an LLM for enhanced drift detection)

## Requirements

### Requirement: GitDiffOptionsValidation

The system SHALL validate GitDiffOptions according to these rules:
- rootPath must be a valid directory path
- baseRef must be a valid git reference or 'auto'
- includeUnstaged must be a boolean

#### Scenario: ConfigureGitDiffWithPathFilter
- **GIVEN** A git repository with multiple files
- **WHEN** GitDiffOptions is created with pathFilter
- **THEN** Only files matching the pathFilter are included in the diff

### Requirement: GitDiffResultValidation

The system SHALL validate GitDiffResult according to these rules:
- resolvedBase must be a valid git reference
- files must be an array of ChangedFile objects
- hasUnstagedChanges must be a boolean
- currentBranch must be a string

#### Scenario: GetDiffResultWithUnstagedChanges
- **GIVEN** A git repository with unstaged changes
- **WHEN** GitDiffResult is generated with includeUnstaged=true
- **THEN** The result includes both staged and unstaged changes

### Requirement: ChangedFileValidation

The system SHALL validate ChangedFile according to these rules:
- path must be a valid file path
- status must be one of: added, modified, deleted, renamed
- oldPath must be a valid file path if status is 'renamed'
- isTest, isConfig, isGenerated must be booleans
- extension must be a string

#### Scenario: ClassifyATestFile
- **GIVEN** A file with a test-related path or extension
- **WHEN** classifyFile() is called
- **THEN** The file is marked as isTest=true

### Requirement: GitDiffValidation

The system SHALL validate GitDiff according to these rules:
- rootPath must be a valid directory path
- baseRef must be a valid Git reference (branch, commit, or tag)
- maxChars must be a positive integer

#### Scenario: GetFileDiff
- **GIVEN** A Git repository with changes between the working tree and a base reference
- **WHEN** getFileDiff() is called with a file path and base reference
- **THEN** The unified diff content for the specified file is returned, truncated if necessary

#### Scenario: GetChangedFiles
- **GIVEN** A Git repository with changes between the working tree and a base reference
- **WHEN** getChangedFiles() is called with the base reference
- **THEN** A list of changed files with their addition and deletion statistics is returned

### Requirement: DriftDetectorValidation

The system SHALL validate DriftDetector according to these rules:
- Drift detection must be performed after code changes
- SpecMap must be up-to-date to accurately detect drift
- ADRMap must be up-to-date to accurately detect ADR-related drift
- LLM must be properly configured if used for enhanced drift detection

#### Scenario: DetectGapsInSpecifications
- **GIVEN** A set of changed files and an up-to-date SpecMap
- **WHEN** detectGaps() is called
- **THEN** A list of DriftIssue objects representing gaps in specifications is returned

#### Scenario: DetectStaleSpecifications
- **GIVEN** A set of changed files and an up-to-date SpecMap
- **WHEN** detectStaleSpecs() is called
- **THEN** A list of DriftIssue objects representing stale specifications is returned

#### Scenario: DetectUncoveredFiles
- **GIVEN** A set of changed files and an up-to-date SpecMap
- **WHEN** detectUncoveredFiles() is called
- **THEN** A list of DriftIssue objects representing uncovered files is returned

#### Scenario: DetectOrphanedSpecifications
- **GIVEN** An up-to-date SpecMap and the root path of the project
- **WHEN** detectOrphanedSpecs() is called
- **THEN** A list of DriftIssue objects representing orphaned specifications is returned

#### Scenario: EnhanceGapDetectionWithLlm
- **GIVEN** A list of DriftIssue objects representing gaps in specifications and an LLM
- **WHEN** enhanceGapsWithLLM() is called
- **THEN** The list of DriftIssue objects is enhanced with additional information from the LLM

### Requirement: Getgitdiff

The system SHALL retrieves the list of changed files between the base reference and the current state of the repository.

#### Scenario: ValidGitDiff
- **GIVEN** A valid git repository with changes between the base reference and the current state
- **WHEN** getGitDiff is called
- **THEN** The list of changed files is returned along with the resolved base reference, unstaged changes
status, and current branch

#### Scenario: InvalidGitRepository
- **GIVEN** An invalid git repository path
- **WHEN** getGitDiff is called
- **THEN** An error is thrown indicating the repository is not valid

### Requirement: Isgitrepository

The system SHALL checks if the given path is a valid git repository.

#### Scenario: ValidGitRepository
- **GIVEN** A valid git repository path
- **WHEN** isGitRepository is called
- **THEN** True is returned

#### Scenario: InvalidGitRepository
- **GIVEN** An invalid git repository path
- **WHEN** isGitRepository is called
- **THEN** False is returned

### Requirement: Getcurrentbranch

The system SHALL retrieves the current branch name of the git repository.

#### Scenario: ValidGitRepository
- **GIVEN** A valid git repository with a current branch
- **WHEN** getCurrentBranch is called
- **THEN** The current branch name is returned

#### Scenario: InvalidGitRepository
- **GIVEN** An invalid git repository path
- **WHEN** getCurrentBranch is called
- **THEN** An error is thrown indicating the repository is not valid

### Requirement: Validategitref

The system SHALL validates a user-supplied git reference to prevent unexpected git argument injection.

#### Scenario: ValidGitReference
- **GIVEN** A valid git reference
- **WHEN** validateGitRef is called
- **THEN** No error is thrown

#### Scenario: InvalidGitReference
- **GIVEN** An invalid git reference
- **WHEN** validateGitRef is called
- **THEN** An error is thrown indicating the reference is invalid

### Requirement: Resolvebaseref

The system SHALL resolves a base reference, falling back through main → master → head~1.

#### Scenario: ValidPreferredReference
- **GIVEN** A valid preferred reference
- **WHEN** resolveBaseRef is called
- **THEN** The preferred reference is returned

#### Scenario: InvalidPreferredReference
- **GIVEN** An invalid preferred reference
- **WHEN** resolveBaseRef is called
- **THEN** The fallback reference is returned

### Requirement: Classifyfile

The system SHALL classifies a file path as test, config, generated, or other.

#### Scenario: TestFile
- **GIVEN** A file path that matches a test pattern
- **WHEN** classifyFile is called
- **THEN** The file is classified as a test file

#### Scenario: ConfigFile
- **GIVEN** A file path that matches a config pattern
- **WHEN** classifyFile is called
- **THEN** The file is classified as a config file

#### Scenario: GeneratedFile
- **GIVEN** A file path that matches a generated pattern
- **WHEN** classifyFile is called
- **THEN** The file is classified as a generated file

### Requirement: Isskippablefile

The system SHALL checks if a file is a skippable binary/lock file.

#### Scenario: SkippableFile
- **GIVEN** A file path that matches a skippable pattern
- **WHEN** isSkippableFile is called
- **THEN** True is returned

#### Scenario: NonSkippableFile
- **GIVEN** A file path that does not match a skippable pattern
- **WHEN** isSkippableFile is called
- **THEN** False is returned

### Requirement: Parsespecheader

The system SHALL extracts source file references and generation date from a spec file header.

#### Scenario: ValidHeaderWithSourceFiles
- **GIVEN** Spec content with 'Source files:' line
- **WHEN** parseSpecHeader is called
- **THEN** Returns array of source files and null date if not present

#### Scenario: HeaderWithGenerationDate
- **GIVEN** Spec content with 'Generated on' line
- **WHEN** parseSpecHeader is called
- **THEN** Returns the parsed date string

### Requirement: Parsespecreferences

The system SHALL extracts file references, requirements, and entities from a spec file body.

#### Scenario: SpecWithImplementationBlock
- **GIVEN** Spec content with 'Implementation:' section
- **WHEN** parseSpecReferences is called
- **THEN** Returns files referenced in implementation block

#### Scenario: SpecWithRequirements
- **GIVEN** Spec content with 'Requirement:' headings
- **WHEN** parseSpecReferences is called
- **THEN** Returns array of requirement names

### Requirement: Inferdomainfrompath

The system SHALL attempts to infer a domain name from a file path based on known domains.

#### Scenario: MatchingDomainInPath
- **GIVEN** File path containing a known domain
- **WHEN** inferDomainFromPath is called
- **THEN** Returns the matching domain name

#### Scenario: NoMatchingDomain
- **GIVEN** File path with no known domains
- **WHEN** inferDomainFromPath is called
- **THEN** Returns null

### Requirement: Buildspecmap

The system SHALL builds a bidirectional map between spec domains and source files.

#### Scenario: ValidSpecsDirectory
- **GIVEN** Directory with valid spec files
- **WHEN** buildSpecMap is called
- **THEN** Returns populated SpecMap with domain and file mappings

#### Scenario: MissingSpecsDirectory
- **GIVEN** Non-existent specs directory
- **WHEN** buildSpecMap is called
- **THEN** Returns empty SpecMap

### Requirement: Getspeccontent

The system SHALL loads and truncates spec content for a given domain.

#### Scenario: ExistingDomain
- **GIVEN** Domain exists in specMap
- **WHEN** getSpecContent is called
- **THEN** Returns truncated spec content

#### Scenario: NonExistentDomain
- **GIVEN** Domain not in specMap
- **WHEN** getSpecContent is called
- **THEN** Returns null

### Requirement: Isspecrelevantchange

The system SHALL determines if a changed file is relevant for spec drift detection.

#### Scenario: RelevantChange
- **GIVEN** A file that matches the spec directory pattern
- **WHEN** isSpecRelevantChange is called
- **THEN** Returns true

#### Scenario: IrrelevantChange
- **GIVEN** A file that doesn't match the spec directory pattern
- **WHEN** isSpecRelevantChange is called
- **THEN** Returns false

### Requirement: Computeseverity

The system SHALL computes severity based on issue kind and change magnitude.

#### Scenario: HighSeverity
- **GIVEN** A high-value file with significant changes
- **WHEN** computeSeverity is called
- **THEN** Returns high severity

#### Scenario: LowSeverity
- **GIVEN** A low-value file with minor changes
- **WHEN** computeSeverity is called
- **THEN** Returns low severity

### Requirement: Extractchangedspecdomains

The system SHALL extracts domain names whose specs were also changed in this changeset.

#### Scenario: DomainsExtracted
- **GIVEN** Changed files that include spec files
- **WHEN** extractChangedSpecDomains is called
- **THEN** Returns a set of domain names

#### Scenario: NoDomainsExtracted
- **GIVEN** No spec files were changed
- **WHEN** extractChangedSpecDomains is called
- **THEN** Returns an empty set

### Requirement: Detectgaps

The system SHALL detects gaps: code changed in a domain but spec wasn't updated.

#### Scenario: GapsDetected
- **GIVEN** Code changes in a domain without corresponding spec updates
- **WHEN** detectGaps is called
- **THEN** Returns an array of drift issues

#### Scenario: NoGapsDetected
- **GIVEN** All code changes have corresponding spec updates
- **WHEN** detectGaps is called
- **THEN** Returns an empty array

### Requirement: Detectstalespecs

The system SHALL detects stale specs: spec references files that were deleted or heavily modified.

#### Scenario: StaleSpecsDetected
- **GIVEN** Specs reference deleted or heavily modified files
- **WHEN** detectStaleSpecs is called
- **THEN** Returns an array of drift issues

#### Scenario: NoStaleSpecsDetected
- **GIVEN** All specs reference existing and unchanged files
- **WHEN** detectStaleSpecs is called
- **THEN** Returns an empty array

### Requirement: Detectuncoveredfiles

The system SHALL detects uncovered files: new files that don't map to any spec domain.

#### Scenario: UncoveredFilesDetected
- **GIVEN** New files that don't map to any spec domain
- **WHEN** detectUncoveredFiles is called
- **THEN** Returns an array of drift issues

#### Scenario: NoUncoveredFilesDetected
- **GIVEN** All new files map to spec domains
- **WHEN** detectUncoveredFiles is called
- **THEN** Returns an empty array

### Requirement: Detectorphanedspecs

The system SHALL detects orphaned specs: spec declares source files that don't exist on disk.

#### Scenario: OrphanedSpecsDetected
- **GIVEN** Specs reference non-existent files
- **WHEN** detectOrphanedSpecs is called
- **THEN** Returns a promise resolving to an array of drift issues

#### Scenario: NoOrphanedSpecsDetected
- **GIVEN** All specs reference existing files
- **WHEN** detectOrphanedSpecs is called
- **THEN** Returns a promise resolving to an empty array

### Requirement: Extractchangedadrids

The system SHALL extracts adr ids that were changed in this changeset.

#### Scenario: AdrIdsExtracted
- **GIVEN** Changed files that include ADR files
- **WHEN** extractChangedADRIds is called
- **THEN** Returns a set of ADR IDs

#### Scenario: NoAdrIdsExtracted
- **GIVEN** No ADR files were changed
- **WHEN** extractChangedADRIds is called
- **THEN** Returns an empty set

### Requirement: Detectadrgaps

The system SHALL detects adr gaps: code changed in a domain referenced by an adr.

#### Scenario: AdrGapsDetected
- **GIVEN** Code changes in a domain referenced by an ADR
- **WHEN** detectADRGaps is called
- **THEN** Returns an array of drift issues

#### Scenario: NoAdrGapsDetected
- **GIVEN** No code changes in domains referenced by ADRs
- **WHEN** detectADRGaps is called
- **THEN** Returns an empty array

### Requirement: Detectadrorphaned

The system SHALL detects orphaned adrs: adr references domains that no longer exist in specs.

#### Scenario: OrphanedAdrsDetected
- **GIVEN** ADRs reference non-existent domains
- **WHEN** detectADROrphaned is called
- **THEN** Returns an array of drift issues

#### Scenario: NoOrphanedAdrsDetected
- **GIVEN** All ADRs reference existing domains
- **WHEN** detectADROrphaned is called
- **THEN** Returns an empty array

### Requirement: Enhancegapswithllm

The system SHALL post-processes gap issues using an llm to determine if code changes are significant enough to update specs.

#### Scenario: GapsEnhanced
- **GIVEN** Gap issues that can be validated by LLM
- **WHEN** enhanceGapsWithLLM is called
- **THEN** Returns an array of enhanced drift issues

#### Scenario: NoGapsEnhanced
- **GIVEN** No gap issues that can be validated by LLM
- **WHEN** enhanceGapsWithLLM is called
- **THEN** Returns the original array of drift issues

### Requirement: Detectdrift

The system SHALL runs all drift detection algorithms and produces a combined result.

#### Scenario: DriftDetected
- **GIVEN** Code changes that introduce drift
- **WHEN** detectDrift is called
- **THEN** Returns a promise resolving to a drift result with issues

#### Scenario: NoDriftDetected
- **GIVEN** Code changes that don't introduce drift
- **WHEN** detectDrift is called
- **THEN** Returns a promise resolving to a drift result without issues


## Sub-components

> `DriftDetector` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Extractchangedspecdomains

> Implements: `extractChangedSpecDomains`

Extracts domain names from changed files that have corresponding specifications.

#### Requirement: Extractdomains

The system SHALL the function shall extract domain names from changed files that have corresponding specifications.

#### Scenario: Extractdomainsfromchangedfiles
- **GIVEN** A set of changed files containing specifications
- **WHEN** The function is called with the changed files
- **THEN** The function SHALL return a set of domain names corresponding to the changed specifications
- **AND** The domain names SHALL be unique

### Sub-component: Detectgaps

> Implements: `detectGaps`

Detects gaps where code changes in a domain do not have corresponding specification updates.

#### Requirement: Detectgapsinspecs

The system SHALL the function shall detect gaps where code changes in a domain do not have corresponding specification updates.

#### Scenario: Detectgapsforchangedfiles
- **GIVEN** A set of changed files and a specification map
- **WHEN** The function is called with the changed files and specification map
- **THEN** The function SHALL return an array of drift issues representing gaps in specifications
- **AND** Each drift issue SHALL include the domain name and severity

### Sub-component: Detectstalespecs

> Implements: `detectStaleSpecs`

Detects stale specifications that reference files that have been deleted or heavily modified.

#### Requirement: Detectstalespecsinspecmap

The system SHALL the function shall detect stale specifications that reference files that have been deleted or heavily modified.

#### Scenario: Detectstalespecsforchangedfiles
- **GIVEN** A set of changed files and a specification map
- **WHEN** The function is called with the changed files and specification map
- **THEN** The function SHALL return an array of drift issues representing stale specifications
- **AND** Each drift issue SHALL include the domain name and severity

### Sub-component: Detectuncoveredfiles

> Implements: `detectUncoveredFiles`

Detects new files that do not map to any specification domain.

#### Requirement: Detectuncoveredfilesinspecmap

The system SHALL the function shall detect new files that do not map to any specification domain.

#### Scenario: Detectuncoveredfilesforchangedfiles
- **GIVEN** A set of changed files and a specification map
- **WHEN** The function is called with the changed files and specification map
- **THEN** The function SHALL return an array of drift issues representing uncovered files
- **AND** Each drift issue SHALL include the file path and severity

### Sub-component: Detectorphanedspecs

> Implements: `detectOrphanedSpecs`

Detects specifications that declare source files that do not exist on disk.

#### Requirement: Detectorphanedspecsinspecmap

The system SHALL the function shall detect specifications that declare source files that do not exist on disk.

#### Scenario: Detectorphanedspecsforspecmap
- **GIVEN** A specification map and the root path
- **WHEN** The function is called with the specification map and root path
- **THEN** The function SHALL return a promise that resolves to an array of drift issues representing orphaned
specifications
- **AND** Each drift issue SHALL include the domain name and severity

### Sub-component: Extractchangedadrids

> Implements: `extractChangedADRIds`

Extracts ADR IDs that were changed in the current changeset.

#### Requirement: Extractadrids

The system SHALL the function shall extract adr ids that were changed in the current changeset.

#### Scenario: Extractadridsfromchangedfiles
- **GIVEN** A set of changed files containing ADRs
- **WHEN** The function is called with the changed files
- **THEN** The function SHALL return a set of ADR IDs corresponding to the changed ADRs
- **AND** The ADR IDs SHALL be unique

### Sub-component: Detectadrgaps

> Implements: `detectADRGaps`

Detects ADR gaps where code changes in a domain referenced by an ADR do not have corresponding specification updates.

#### Requirement: Detectadrgapsinspecs

The system SHALL the function shall detect adr gaps where code changes in a domain referenced by an adr do not have corresponding specification updates.

#### Scenario: Detectadrgapsforchangedfiles
- **GIVEN** A set of changed files, an ADR map, a specification map, and a set of changed ADR IDs
- **WHEN** The function is called with the changed files, ADR map, specification map, and changed ADR IDs
- **THEN** The function SHALL return an array of drift issues representing ADR gaps in specifications
- **AND** Each drift issue SHALL include the domain name and severity

### Sub-component: Detectadrorphaned

> Implements: `detectADROrphaned`

Detects orphaned ADRs that reference domains that no longer exist in specifications.

#### Requirement: Detectadrorphanedinspecmap

The system SHALL the function shall detect orphaned adrs that reference domains that no longer exist in specifications.

#### Scenario: Detectadrorphanedforadrmap
- **GIVEN** An ADR map and a specification map
- **WHEN** The function is called with the ADR map and specification map
- **THEN** The function SHALL return an array of drift issues representing orphaned ADRs
- **AND** Each drift issue SHALL include the ADR ID and severity

### Sub-component: Enhancegapswithllm

> Implements: `enhanceGapsWithLLM`

Post-processes gap issues using an LLM to determine if code changes are relevant for specification updates.

#### Requirement: Enhancegapswithllmanalysis

The system SHALL the function shall post-process gap issues using an llm to determine if code changes are relevant for specification updates.

#### Scenario: Enhancegapswithllmforissues
- **GIVEN** An array of drift issues, LLM options, and other parameters
- **WHEN** The function is called with the drift issues and LLM options
- **THEN** The function SHALL return an array of enhanced drift issues with updated severity and relevance
- **AND** Each enhanced drift issue SHALL include the domain name, severity, and relevance

### Sub-component: Isspecrelevantchange

> Implements: `isSpecRelevantChange`

Determines if a changed file is relevant for specification drift detection.

#### Requirement: Determinespecrelevance

The system SHALL the function shall determine if a changed file is relevant for specification drift detection.

#### Scenario: Determinespecrelevanceforfile
- **GIVEN** A changed file
- **WHEN** The function is called with the changed file
- **THEN** The function SHALL return true if the file is relevant for specification drift detection, otherwise
false
- **AND** The function SHALL consider the file path and content
### Requirement: UseGitCommitAncestryForBitemporalMemoryValidityValidfromcommitDeterministicNoLlm

The system SHALL use git commit ancestry (merge-base --is-ancestor) rather than wall-clock timestamps for bitemporal memory valid-time comparison, ensuring reproducible history for a fixed repo state.

> Decision recorded: 48771c59
> Date: 2026-06-18
### Requirement: ExcludeSupersededDecisionsFromAuthoritativeRecallViaOneSharedSupersessionPredicate

The system SHALL exclude superseded decisions from authoritative recall and orient context using a single shared supersession predicate, surfacing them only as reversal warnings.

> Decision recorded: 6c32e6c6
> Date: 2026-06-19
### Requirement: LeanDefaultMcpSurfaceNavigationPresetFull62toolSurfaceIsOptinViaPresetFullAlltools

The system SHALL expose the navigation preset as the default MCP tool surface and require an explicit selector (--preset full or --all-tools) to activate the complete tool registry.

> Decision recorded: a6c916ed
> Date: 2026-06-22

## Technical Notes

- **Implementation**: `src/core/drift/git-diff.ts, src/core/drift/spec-mapper.ts, src/core/drift/drift-detector.ts`
- **Dependencies**: node:child_process, node:fs/promises, node:path, node:util, ChangedFile, logger, DIFF_MAX_CHARS, fs/promises, path, SpecMap, ADRMap, LLMProvider, DriftIssue, DriftResult, DriftDetectorOptions

## Decisions

### Use git commit ancestry for bitemporal memory validity (validFromCommit); deterministic, no LLM

**Status:** Approved
**Date:** 2026-06-18
**ID:** 48771c59

Memory records gain optional validFromCommit (HEAD SHA at record time), invalidatedAt, invalidatedByCommit, supersedes, and a closed-set type. asOf/changedSince recall resolve a commit-ish to a SHA and use `git merge-base --is-ancestor` for valid-time comparison rather than wall-clock timestamps, so history is reproducible for a fixed repo state with no LLM and no tuning constant. Supersession is an explicit caller act (remember gains supersedes), reusing the decisions supersedes semantics. Contradiction is a deterministic set intersection over resolved symbol anchors (unreconciled), surfaced in recall and orient. Dedup keys memory identity on hash(content+resolved anchors) instead of hash(content+timestamp). All fields additive/optional so legacy stores load without migration.

**Consequences:** recall/orient gain an opt-in git subprocess path only when asOf/changedSince is supplied (common path unchanged). New capability rides recall/remember params — no new tool, default and minimal surfaces unchanged. makeMemoryId signature changes from (content, recordedAt) to (content, anchors).

### Exclude superseded decisions from authoritative recall via one shared supersession predicate

**Status:** Approved
**Date:** 2026-06-19
**ID:** 6c32e6c6

A decision superseded by another (via record_decision with supersedes) remained draft/approved/verified until LLM consolidation flipped it to rejected — which never runs without an API key. This caused orient.pendingDecisions and recall authoritative to serve the superseded decision as current context while simultaneously surfacing it as a do-not-repeat reversal. Fix: a single shared predicate supersededDecisionIds() (where a superseder counts unless it is itself rejected/phantom, preserving the original if the supersession is declined) is used by both collectReversals (warn side) and the authoritative filter in orient.ts and memory.ts (exclude side), ensuring the two surfaces can never disagree, including in the pre-consolidation window.

**Consequences:** A superseded-but-still-active decision is now withheld from pendingDecisions/recall authoritative and surfaced only under reversals. The superseding decision stays authoritative. No LLM consolidation required for supersession to take effect.

### Lean default MCP surface = navigation preset; full 62-tool surface is opt-in via --preset full / --all-tools

**Status:** Approved
**Date:** 2026-06-22
**ID:** a6c916ed

OpenLore exposed all 62 MCP tools by default (selectActiveTools with no selector returned allTools; install wired `openlore mcp` with no preset), contradicting both the Spec 14 agent-benchmark result (the net win comes specifically from `--preset navigation`, a ~10-tool graph-traversal surface) and the mcp-quality MinimizeToolSurface rule (schemas for uncalled tools are pure per-request overhead and degrade tool-selection accuracy). Invert the default: no selector now resolves to the lean default = the existing `navigation` preset verbatim (orient, search_code, get_subgraph, trace_execution_path, analyze_impact, suggest_insertion_points, get_function_skeleton, get_landmarks, get_map, find_path). The full TOOL_DEFINITIONS surface stays reachable via `--preset full` (alias `all`) or `--all-tools`. Governance/memory/verify/federation remain opt-in named presets, unchanged. Shared default/full preset names live in src/constants.ts so install adapters need not import the heavy MCP module.

**Consequences:** No tool is removed; --preset full restores prior behavior exactly. The default-installed surface no longer includes governance tools (record_decision, check_spec_drift, detect_changes) — agents relying on the decisions pre-commit-gate workflow should install with --preset full or governance preset; documented with a migration note. The tool-count doc guard asserts the documented default count against the lean preset and the full count against TOOL_DEFINITIONS.length. New tools added to the registry default to opt-in (absent from the lean default) unless an evidence-backed decision adds them.

### Harden lean-default selector contract: unified resolution, pointer parity, canonical normalization, clean error exit

**Status:** Approved
**Date:** 2026-06-22
**ID:** 5fdc9da7

Adversarial e2e on the default-to-lean-tool-surface change (a6c916ed) found four contract gaps: (1) The breadth pointer was gated on 'no selector at all', so `openlore install` (which wires `--preset navigation` explicitly) suppressed the pointer — installed agents got the lean surface but were never told breadth exists. Now both selectActiveTools and leanDefaultActive resolve through a single resolvePresetName() helper, and the pointer fires whenever the resolved surface is the lean default regardless of how it was reached. (2) `openlore mcp --preset <bad>` threw uncaught (exit 1 + stack trace); now exits 2 with a clean stderr message. (3) The cursor install adapter early-returned when its preset-independent .mdc was unchanged, skipping MCP wiring and silently ignoring preset switches on re-install; now falls through to MCP registration. (4) Selector vocabulary parity: serve accepts `full` as alias of `all`; install/connect accept `--all-tools`; the `all` alias normalizes to canonical `full` in wired args so .mcp.json never stores two strings for one surface.

**Consequences:** Single source of truth (resolvePresetName) means the active tool set and the breadth pointer can never disagree. Installed agents on the lean default reliably see the breadth pointer. Bad presets fail as CLI usage errors (exit 2, no stack). Cursor users can switch presets on re-install. `full`/`all`/`--all-tools` are interchangeable across mcp/serve/install/connect, always wired as canonical `full`.
